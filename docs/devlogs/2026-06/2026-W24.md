# 2026-W24 Development Log

周期：2026-06-08 至 2026-06-14

## 本周当前摘要

- 2026-06-08 推进 Studio UI 实现第十七刀：底部 Workbench 从单一 drawer 收敛为左侧运行信息 tabs 与右侧 `状态汇总` 分栏。
- 本刀继续只消费既有 `window.status_summary`、Run Panel、current / stale / failure snapshot 和 canvas suggestions，不新增第二套结果、运行或诊断状态源。
- focused 回归已覆盖底部分栏渲染、当前快照后的状态汇总、无真实迭代次数时不伪造迭代数据，以及结果表内容测试与 bottom split 首屏裁切的职责分离。
- 同日继续推进 Studio UI 实现第十八刀：左侧栏顶层入口从 `项目 / 示例项目 / 放置` 收敛为 `模块 / 项目`；示例项目保留为 `项目` 内可展开入口，作者路径与建模 readiness 继续聚焦 `模块`。
- 同日继续推进 Studio UI 实现第十九刀：`模块` 面板内部按 `流股源 / 调节单元 / 汇合与分离` 组织现有受控 place-unit palette，并提供本地筛选；任务清单和画布建议保留在同一职责区。
- 同日继续推进 Studio UI 实现第二十刀：`项目` 面板内部整理为 `项目输入 / 示例入口 / 对象树 / 审阅状态`，继续只消费现有项目文档、canvas object list、current snapshot / failure 状态。
- 同日继续推进 Studio UI 实现第二十一刀：中央 Canvas 移除重复对象列表，改为 `画布状态` 概览，并继续承接工具条、选择、视口、图例、画布实体和受控建议。
- 同日继续推进 Studio UI 实现第二十二刀：右侧 `检查器 / 模块设置 / 模块结果` 新增同一 `画布选择` 上下文头，直接消费 Canvas current selection / command presentation，并保持模块面板 unit-only 边界。
- 同日继续推进 Studio UI 实现第二十三刀：中央 Canvas 首屏从独立 `选择 / 视口` 详情行收束为 `画布状态` 与 `画布操作`，把选择详情留给右侧 `画布选择` 上下文。
- 同日继续推进 Studio UI 实现第二十四刀：底部薄状态栏只保留运行 / 快照 / 单位 / 求解器 / 模式 / 当前选择扫读，完整状态汇总继续由底部右侧卡片承接。
- 同日继续推进 Studio UI 实现第二十五刀：中央 Canvas 顶部工具区移除重复放置 palette，收束为视图命令和当前建议命令；放置单元入口继续由左侧 `模块` 面板承担。
- 同日继续推进 Studio UI 实现第二十六刀：Workbench 真实首屏完成 1280x860 egui viewport 职责复核；顶部 `流程图工具栏` 的 Canvas 段不再重复放置 palette 或选中对象移动命令。
- 同日根据真实 UI 反馈推进 Studio UI 实现第二十七刀：普通空白项目创建后先进入 `物性` 页，Home 增加当前 workspace 返回 tile，避免未保存项目从 Home 无法回到案例。
- 同日继续推进 Studio UI 实现第二十八刀：梳理 `新建项目 -> 物性 -> 选择物性包 / 项目组分 -> 进入流程图建模` 主路径，`window.property_page`、`window.property_context_toolbar`、顶部 `流程图` 导航和 Home 当前 workspace 返回入口已共用同一 Property DTO readiness。
- 同日完成 CI/CD 口径治理：`dev` 明确为日常开发分支，不自动触发 CI/CD；自动 CI/CD 只在 `dev -> master/main` 稳定化 PR 触发，并把 Windows `.NET` CAPE-OPEN baseline 与 staging package 纳入默认分支门禁。
- 2026-06-09 推进 Studio UI 实现第二十九刀：普通空白项目 `Home -> 新建项目 -> 物性 -> 选择内置 package / 项目组分 -> 流程图建模` 主路径的 Workbench 首屏职责已由 focused test 锁定；内部进入流程图建模入口新增 Property readiness 守卫，避免非按钮路径绕过物性主路径。
- 同日根据真实 UI 反馈推进 Studio UI 实现第三十刀：Home 新增显式 `返回工作区` 操作，当前 MVP 的 `物性` 页移除无操作价值的左侧二级导航，`Cmd+Q` 接入现有脏工作区保存 / 放弃 / 取消关闭确认路径。
- 同日继续推进 Studio UI 实现第三十一刀：右侧 `模块设置` 增加参数摘要，直接从 active Inspector 字段、notice 和批量提交 / 放弃 command 派生字段数、草稿数、问题数、提示数与可提交状态。
- 同日根据人工 smoke 反馈推进 Studio UI 实现第三十二刀：关闭 macOS winit 默认 App 菜单，避免系统 `Cmd+Q` 直接触发原生 `terminate:` 绕过 Studio 的关闭确认；`Cmd+Q` 继续回到 shell 里的统一保存 / 放弃 / 取消关闭路径。
- 同日真实窗口复核通过：重启应用后脏项目 `Cmd+Q` 已进入与窗口关闭按钮相同的保存 / 放弃 / 取消确认路径。
- 同日继续推进 Studio UI 实现第三十三刀：底部结果表单元区改为消费 `review_summary.unit_results`，并让 `状态汇总` 从同一 review summary 扫读单元结果数量，保持右侧 `模块结果`、底部 `结果表` 和 `状态汇总` 的 latest unit result 口径一致。
- 同日继续推进 Studio UI 实现第三十四刀：底部 `结果表` 的 stream / unit 行点击改为复用正式 `inspector.focus_*` 定位；流股进入右侧 `检查器`，单元进入右侧 `模块结果`，底部保持 `结果表`。
- 同日根据真实 UI 截图反馈推进 Studio UI 实现第三十五刀：顶部 `结果工具栏` 不再展开所有 `Inspect Result Stream ...` 长按钮，保留审阅入口和结果状态扫读，把具体对象定位交给结果表、右侧相关 action、项目树和命令面板。
- 同日根据运行页真实截图继续推进 Studio UI 实现第三十六刀：顶部 `运行工具栏` 保留 Monitor 入口，但不再在每个按钮旁重复渲染 `可用 / 无 / 已收敛 / 诊断` 状态 chip，状态统一由下一行 `Mode / Run / Convergence / Steps / Diagnostics / Snapshot` 扫读。
- 同日继续推进 Studio UI 实现第三十七刀：左侧 `模块` 页去掉分类 detail 和模块 option detail 的常驻渲染，说明保留在 hover / DTO；底部 `状态汇总` 把 snapshot 一致性提前到标题行，确保低高度抽屉仍能看到快照状态。
- 2026-06-10 根据真实窗口 smoke 推进 Studio UI 实现第三十八刀：右侧 `模块结果` 不再复用完整 unit execution inspector 展开长 snapshot id 和完整执行句，改为紧凑展示单元、状态、step、短执行摘要和消费 / 产出流股 chip。
- 同日根据真实截图审阅推进 Studio UI 实现第三十九刀：official hydrocarbon 示例项目文件补齐 `flowsheet.thermo.property_package_id = binary-hydrocarbon-lite-v1`，左侧 `项目输入` 改为从当前 document / choice 状态显示本地化物性包名，避免出现“运行已收敛但物性包仍显示未选择”的状态冲突。
- 同日继续推进 Studio UI 实现第四十刀：Home recent / current / example case tile 的物性包摘要从当前 document / builtin package choice 映射为可读 label，中文界面显示“二元烃 Lite”；空白项目仍显示“未选择”，项目文件继续只保存稳定 id。
- 同日根据真实截图继续推进 Studio UI 实现第四十一刀：独立 `物性` 页 toolbar、摘要和包选择卡统一显示可读 package label，entitlement host schedule notice 改为用户可读摘要；稳定 package id 继续只留在项目文件、command id 和内部状态边界。
- 2026-06-14 根据开发节奏复盘，将后续推进方式从“Studio UI 第 N 刀”调整为 `docs/status/current.md` 总进度 + `docs/topics/` 专题体系；首批一级轨道专题覆盖 Studio 主路径、项目物性基础、流程图建模与求解、结果审阅诊断、项目生命周期和 CAPE-OPEN PMC 适配层。
- 同日继续补齐二级功能专题口径：Feed、Heater / Cooler、Flash Drum、Mixer、Valve、Material Stream、Control Plane 后端服务和 Control Plane Web UI 都有独立专题入口，避免具体单元或后端服务细节继续挤进一级轨道文档。

## 本周目标

- 继续沿 Studio UI 专题做 presentation / window model 小切片。
- 优先把 `studio-client-main.pen` 中已经评审稳定的 Workbench 区域映射到现有 shell / DTO / command surface。
- 不推进完整 ribbon、完整参数表、自由连线、完整报表、完整收敛曲线、完整建议系统或正式发布自动化。

## 完成情况

- 新增 `docs/topics/README.md` 和 `docs/topics/topic-template.md`，把功能、能力包或开发目标统一放入专题文档，要求每个专题写清层级、父专题、目标、用户路径、范围、非目标、设计边界、分阶段切片、验收标准和验证计划。
- 新增首批专题：`studio-main-workflow.md`、`property-basis-and-components.md`、`flowsheet-modeling-and-solve.md`、`results-review-diagnostics.md`、`project-lifecycle-storage.md` 和 `capeopen-pmc-adapter.md`。
- 新增首批二级功能专题：`unitops/feed-source.md`、`unitops/heater-cooler.md`、`unitops/flash-drum.md`、`unitops/mixer.md`、`unitops/valve.md`、`modeling/material-stream.md`、`platform/control-plane-service.md` 和 `platform/control-plane-web-ui.md`。
- `docs/status/current.md` 已重写为专题化总入口：只保留阶段结论、当前激活专题、当前策略、能力基线、下一步、验证节奏和暂不推进项。
- `docs/README.md` 与 `docs/radishflow-mvp-roadmap.md` 已同步专题化开发节奏，避免路线图和目录页继续暗示以零散 UI 切片作为默认推进单位。
- `render_bottom_workbench` 已拆成左侧运行信息区和右侧状态汇总区：左侧保留 `消息 / 运行日志 / 收敛 / 建议 / 诊断 / 结果表`，右侧固定展示 `Status Summary`。
- 右侧状态汇总只读取 `StudioGuiWindowStatusSummaryModel` 中的 `Case`、`Run`、`Convergence`、`Steps`、`Units`、`Diagnostics` 和 snapshot consistency；`Steps` 继续表达 `Sequential steps` / `N/A`，不伪造迭代次数。
- `收敛` tab 将当前求解摘要和 snapshot identity 提前展示，避免底部分栏后关键结果摘要被首屏裁切。
- 结果表内容测试改为直接渲染结果表本体，底部分栏布局由新增 focused tests 单独覆盖，避免把内容正确性与 drawer 首屏可见区域混在同一个断言里。
- 已同步 `docs/status/current.md`、`docs/architecture/studio-ui-topic-plan.md`、`docs/architecture/designs/studio-client-main-brief.md` 和 `docs/radishflow-mvp-roadmap.md`。
- 第十八刀已把左侧 shell tab 收敛为 `模块 / 项目`：`模块` 继续消费既有 Canvas place-unit palette、authoring checklist 和 suggestion action，`项目` 继续消费项目对象树、物性包 / 组分扫读，并内嵌可展开示例入口。
- 第十九刀已把 `模块` 面板从平铺放置按钮改为分类渲染：`Feed` 进入 `流股源`，`Heater / Cooler / Valve` 进入 `调节单元`，`Mixer / Flash Drum` 进入 `汇合与分离`；筛选只影响本地展示，按钮仍派发正式 palette option 的 command id。
- 第二十刀已把 `项目` 面板从线性列表整理为四个职责区：`项目输入` 扫读物性包和项目组分，`示例入口` 保留可展开内置示例，`对象树` 承接流股 / 单元导航，`审阅状态` 承接结果 / 诊断计数。
- 第二十一刀已把中央 Canvas 收束为建模主舞台：移除底部可筛选对象列表，新增从 canvas presentation 派生的 `画布状态` 数量概览；对象树导航继续由左侧 `项目` 面板承担，画布实体点击仍可聚焦右侧检查器。
- 第二十二刀已把右侧栏三入口与 Canvas 当前选择对齐：`render_right_sidebar_selection_context` 只读取 `window.canvas.widget.view().current_selection` 的 kind、target、summary、layout detail 和 command id；`检查器` 继续消费 active inspector detail，`模块设置` 继续消费 `window.module_settings`，`模块结果` 继续消费 `window.module_results`。当 Canvas 选择单元时三个入口都显示同一选择上下文；当 Canvas 选择流股时，模块设置 / 模块结果继续显示已有“未选择单元”状态，不伪造单元参数或结果。
- 第二十三刀已把中央 Canvas 首屏信息密度继续收束：`render_canvas_stage_summary` 合并对象数量、运行状态、视口模式、布局状态和 `适应内容` 命令；新增 `画布操作` 条承接当前选中对象的聚焦、移动、断开、重连和删除命令；toolbar 不再重复渲染选中对象 `Edit` 命令组；旧的独立 `选择` / `视口` 文本详情行已移除。右侧栏继续用 `画布选择` 显示当前对象语义和模块入口协同说明。
- 第二十四刀已把底部薄状态栏从完整 `Status Summary` 展开收束为窗口级扫读带：只展示 run 状态、snapshot 一致性、SI 单位、顺序模块法、流程图模式和当前 Canvas 选择；case、convergence、steps、diagnostics 等完整状态汇总继续留在底部右侧 `状态汇总` 卡片。新增 focused test 同时渲染中央 Canvas、右侧栏、底部 Workbench 与薄状态栏，锁定三者不互相重复职责。
- 第二十五刀已把中央 Canvas 顶部从 `Canvas / Place` 按钮组收束为 `画布工具`：工具条只渲染 `适应内容` 视图命令和当前建议命令；`画布状态` 行去掉操作按钮，保持对象数、建议数、运行状态、视口模式和布局状态 chip。新增 focused test 确认中心 Canvas 不再渲染 `放置进料 / 放置加热器 / 放置闪蒸罐` 等 palette label，左侧 `模块` 面板继续保留这些正式放置入口。
- 第二十六刀已用真实 Workbench 渲染顺序复核首屏职责分区：顶部导航、左侧栏、右侧栏、底部抽屉、薄状态栏和中央画布在同一 1280x860 viewport 中共同渲染；左侧 `模块 / 项目`、中央 `画布工具 / 画布状态 / 画布操作`、右侧 `画布选择 / 模块设置`、底部 `状态汇总 / 结果表` 和薄状态栏当前选择分别落在预期区域。
- 本刀同步收窄 `window.flowsheet_context_toolbar` 的 Canvas 命令来源：只保留当前可用的 suggestion / pending-edit command；`canvas.begin_place_unit.*` 继续由左侧 `模块` 面板提供，`canvas.move_selected_unit.*` 继续由中央 `画布操作` 提供，顶部 `流程图工具栏` 不再重复这些入口。
- 第二十七刀修正了 Home / 新建项目导航：`create_blank_project` 对普通空白项目进入 `Property` screen，保留 Mixer / Heater 作者入口进入 Workbench 的现有行为；Home 最近项目区会从当前 `workspace_document` 派生 `Current` tile，未保存的 `Blank Project` 显示为当前工作区但不写入 recent projects。
- Home 的 current tile 和保存过的 current recent tile 都会直接返回当前 workspace；若物性包或项目组分未完成则返回 `物性`，否则返回 `流程图`，避免把当前文件当作 recent path 重新打开并触发丢弃确认。
- 第二十八刀已把 Property 页到 Workbench 的交接收口到正式 window model：`StudioGuiWindowPropertyPageModel` 从当前 `workspace_document` 派生 `flowsheet_modeling_enabled / status / detail`，`window.property_context_toolbar` 增加 `Modeling` 分组和 `Enter Flowsheet Modeling` 入口；Property 页摘要区、顶部 `流程图` 导航和 Home 当前 workspace 返回都读取同一 readiness。
- 进入流程图建模时不新增命令或第二套状态，只切换到 `Workbench`；空 flowsheet 聚焦左侧 `模块` 放置入口，已有对象的 workspace 回到左侧 `项目` 语境，右侧和底部回到 `检查器 / 消息`。
- CI/CD 已按默认分支门禁重新分层：`PR Checks` 只响应 `pull_request -> master/main`，新增 `.NET Adapter Baseline` 与 `Windows Staging Package`；`Release Checks` 改为手动 staging preflight，不再响应 tag push。
- 新增 `scripts/check-dotnet-capeopen.ps1` 作为 Windows `.NET 10` CAPE-OPEN baseline 入口，封装 `rf-ffi` native build、`.NET` solution build、contract tests 与 smoke tests；`scripts/smoke-test.ps1` 从占位脚本补成正式 smoke 入口。
- 已同步 `.github/rulesets/master-protection.json`、`.github/rulesets/README.md`、`docs/adr/0001-branch-and-pr-governance.md` 与 `docs/architecture/versioning.md`，明确 `dev` 不触发 CI/CD、默认分支 PR 承担稳定化 gate、tag push 暂不自动触发发布检查。
- 第二十九刀已把普通空白项目主路径的最终交接继续收口到同一 Property DTO readiness：`enter_flowsheet_modeling_from_property` 在 package / 项目组分未完成时保持 `Property` screen，不直接切入 Workbench；准备完成后进入 Workbench 并默认聚焦左侧 `模块`、右侧 `检查器`、底部 `消息`。
- 新增 focused test 同时渲染 Workbench 顶部、左侧、中央、右侧和底部区域，锁定空 flowsheet 首屏职责：左侧 `模块` 承接放置入口，中央只显示 `画布工具 / 画布状态 / 图例 / 空画布提示`，右侧显示空 `画布选择 / 检查器` 语境，底部显示消息和状态汇总；顶部 `流程图工具栏` 不重复放置 palette，中央不重复对象树、右侧栏或结果表。
- 同步压缩 `docs/status/current.md`，把旧的逐刀流水移回周志和专题文档职责内；当前状态入口从约 13.9k 字符降到约 3.8k 字符。
- 第三十刀已把 Home 当前 workspace 返回从“最近项目卡片双击”提升为左侧开始区显式按钮；该按钮继续走同一 current workspace entry screen 判断，物性未完成时回到 `物性`，物性完成后回到 `流程图`，且不把未保存项目写入 recent projects。
- Home `返回工作区` 按钮的显示条件已收窄为本次会话已打开或新建 case 后才出现，避免刚启动软件时把默认加载的 workspace 误判为用户需要返回的工作区；当前 workspace tile 仍可作为状态展示。
- 当前 `物性` 页只保留真正可操作和可判断状态的两列：左侧 package / 项目组分选择，右侧摘要与进入建模 readiness；`参数 / 分析 / 来源` 等未来空间不再作为禁用按钮占据当前 MVP 首屏。
- `Cmd+Q` 已在 shell 快捷键层复用 `close_current_window_for_viewport_request`：脏 workspace 打开保存 / 放弃 / 取消关闭确认，干净 workspace 继续发送 viewport close，不新增第二套保存或退出状态。
- 第三十一刀已让 `StudioGuiWindowModuleSettingsModel` 暴露 `parameter_summary`，并由右侧 `模块设置` 在字段列表前展示 `参数摘要`、状态 chip、字段 / 草稿 / 问题 / 提示计数和 `可提交` 状态；摘要只消费既有 Inspector 字段、notice、batch commit / discard command，不新增 shell 私有参数状态。
- 第三十二刀修正了人工 smoke 发现的 macOS 系统级 `Cmd+Q` 缺口：`studio_native_options` 在 macOS 关闭 winit 默认 App 菜单，阻止默认 `Quit` 菜单项直接进入原生 `terminate:`；应用内 `Cmd+Q` 和窗口关闭按钮继续共享同一 `close_current_window_for_viewport_request` 与 `pending_close_window_confirmation`。
- 脏项目 `Cmd+Q` 真实窗口 smoke 已由用户复核通过，确认会进入与窗口关闭按钮相同的保存 / 放弃 / 取消确认路径。
- 第三十三刀已把 `StudioGuiWindowResultReviewUnitModel` 补齐为 latest unit step 的 `step_index / summary / status / consumed / produced` 摘要；底部结果表单元区改为消费该 review summary，`Status Summary` 的 `Units` metric 也从同一摘要派生。右侧 `模块结果` 继续消费 selected unit result，focused test 锁定二者 consumed / produced stream 口径一致。
- 第三十四刀已把底部 `结果表` 的 stream / unit 点击收口到正式 `dispatch_ui_command` 路径：stream 行触发 `inspector.focus_stream:*` 并切到右侧 `检查器`，unit 行触发 `inspector.focus_unit:*` 并切到右侧 `模块结果`，底部抽屉继续停留在 `结果表`。新增 focused test 锁定 result inspector 选择、active inspector target 和右侧 tab 三者同步更新。
- 第三十五刀已把 shell 顶部 context toolbar 的 `Result Context / Focus` section 作为可扫读 command surface 留在 window model，但不再在顶栏直接展开渲染；`Review` 的 `模块结果 / 结果表` 入口和 `Snapshot / Streams / Units / Diagnostics` 状态仍保留。
- 第三十六刀已把 shell 顶部 context toolbar 的 `Run Context / Monitor` item status 在顶栏渲染层收束；window model 仍保留 Monitor item 状态，顶栏只展示入口按钮，状态扫读继续由 `run_context_toolbar.status_items` 承担。
- 第三十七刀已把左侧模块 palette 的可见信息压到分类标题、数量、筛选框和放置按钮，模块说明仍在 hover 中可用；同时压紧底部右侧 `状态汇总` 标题行，让空项目首屏底部继续显示 `Snapshot` 一致性。
- 第三十八刀已把右侧 `模块结果` 的 current 结果态改为专用紧凑渲染：标题行保留 `模块结果 / 当前 / Unit`，正文只展示 `unit_id / 收敛状态 / #step`、短执行摘要和消费 / 产出流股 chip；旧结果、无结果和未选择单元仍保留原说明文本。
- 第三十九刀已把 official methane / ethane 示例项目的物性包选择写回项目文件：`Feed -> Heater/Cooler/Valve -> Flash Drum` 与 `Feed + Feed -> Mixer -> Flash Drum` 四个 official hydrocarbon 示例均持久化 `binary-hydrocarbon-lite-v1`；`项目` 侧栏的 `项目输入` 使用 `workspace_document.property_package_choices` 的 selected label 并经 shell locale 本地化，空白项目仍保持未选择状态。
- 第四十刀已把 Home recent / current / example case tile 的 `package_summary` 从 raw package id 收束为 builtin label；Home renderer 再经 shell locale 本地化，中文界面显示“二元烃 Lite”。Home 本地缓存提示也显示同一可读 label；项目文件、command id、运行请求和 property selection 状态仍保留稳定 `binary-hydrocarbon-lite-v1`。
- 第四十一刀已把独立 `物性` 页的 package metric 和 `window.property_context_toolbar` status value 改为 selected builtin label；左侧 package 选择卡不再常驻 raw id；entitlement host 不再把 `TimerElapsed` / `SystemTime` 调度结构写入 notice message，平台卡把 schedule notice 映射为中文可读授权检查摘要。

## 今日提交回顾

2026-06-08 共完成 12 个 Studio UI 实现提交，均沿 `studio-client-main.pen` / brief 的既有区域职责推进：

- `9c89845d ui(studio): split bottom status summary`：底部 Workbench 拆为运行信息 tabs 与右侧 `状态汇总`。
- `60f92e15 ui(studio): converge left sidebar roles`：左侧顶层入口收敛为 `模块 / 项目`。
- `13988589 ui(studio): organize module palette groups`：`模块` 面板按受控单元类型分类并支持本地筛选。
- `66479a67 ui(studio): structure project sidebar roles`：`项目` 面板整理为项目输入、示例入口、对象树和审阅状态。
- `7f77e890 ui(studio): 收束中央画布职责`：中央 Canvas 移除重复对象列表，改为建模主舞台和 `画布状态`。
- `4ba618fa ui(studio): 对齐右侧栏与画布选择`：右侧三入口共用 Canvas 当前选择上下文。
- `1f936034 ui(studio): 收束画布首屏状态`：Canvas 首屏收束为 `画布状态` 与 `画布操作`。
- `299e28b8 ui(studio): 收束底部状态栏职责`：底部薄状态栏只保留窗口级扫读信息。
- `6526a149 ui(studio): 收束画布工具区职责`：中央 `画布工具` 只承接视图和当前建议命令。
- `716994a1 ui(studio): 锁定工作台首屏职责`：用 1280x860 viewport focused test 锁定 Workbench 主要区域职责。
- `f78b287a ui(studio): 修正新建项目导航`：普通空白项目先进入 `物性`，Home 增加当前 workspace 返回 tile。
- `f1a56e82 ui(studio): 衔接物性页建模主路径`：Property page、Property toolbar、顶部 `流程图` 导航和 Home 返回共用物性到建模 readiness。

2026-06-09 共完成 10 个 Studio UI / macOS 退出确认提交，继续沿 `studio-client-main.pen` / brief、正式 DTO / command surface 和真实窗口反馈推进：

- `136f2539 ui(studio): 锁定空白项目建模首屏`：锁定普通空白项目从 `物性` readiness 进入空 Workbench 后的首屏职责。
- `fd10799e ui(studio): 修正工作区返回和退出确认`：Home 返回工作区、物性页主路径和 `Cmd+Q` 关闭确认接入同一 workspace 语义。
- `6c008402 ui(studio): 收窄首页返回工作区入口`：Home 显式 `返回工作区` 只在本次会话已有用户打开 / 新建 case 后出现，避免刚启动时误导。
- `0e6f08ac ui(studio): 完善模块设置参数摘要`：右侧 `模块设置` 消费 active Inspector 字段、notice 和 batch command 派生参数摘要。
- `9e98ed51 fix(studio): 修正 macOS Cmd+Q 退出确认`：关闭 macOS winit 默认 App 菜单，避免系统 `Quit` 绕过保存 / 放弃 / 取消确认。
- `1494bea5 ui(studio): 对齐结果区单元摘要`：底部结果表单元区改为消费 `review_summary.unit_results`，并与 `状态汇总` 的 unit count 同源。
- `4f312cfc ui(studio): 对齐结果表定位反馈`：底部结果表 stream / unit 点击复用正式 `inspector.focus_*` command，并路由到匹配右侧入口。
- `30f90ba6 ui(studio): 收束结果页顶部工具栏`：顶部 `结果工具栏` 不再展开所有 `Inspect Result Stream ...` 长按钮，只保留审阅入口和结果状态扫读。
- `b45c343c ui(studio): 收束运行页顶部工具栏`：顶部 `运行工具栏` 保留 Monitor 入口，但不在按钮旁重复展示状态 chip。
- `a8b9f37e ui(studio): 收束左侧模块页密度`：左侧 `模块` 页把分类 / option detail 收进 hover，并将底部状态汇总 snapshot 状态提升到标题行。

2026-06-10 已完成协作入口称呼同步、仓库级验证基线记录、右侧 `模块结果` 密度收束、official 示例物性包状态一致性修复、Home 物性包展示口径收束和独立 `物性` 页截图反馈修正：

- `8befebca docs: 更新称呼`：同步 `AGENTS.md` / `CLAUDE.md` 对话称呼规则。
- `39fb6694 docs(status): 记录仓库级验证基线`：记录 2026-06-10 `./scripts/check-repo.sh` 通过和当前验证基线。
- `be3f9851 ui(studio): 收束模块结果展示密度`：右侧 `模块结果` current 态隐藏长 snapshot id 和完整执行句，改为紧凑单元结果摘要与消费 / 产出流股 chip。
- `1a6e6421 fix(studio): 对齐示例项目物性包状态`：official hydrocarbon 示例项目写入 `binary-hydrocarbon-lite-v1`，左侧 `项目输入`、独立 `物性` 页和运行结果读取同一物性包事实源。
- `f977f67b ui(studio): 收束首页物性包展示`：Home recent / current / example case tile 和本地缓存提示显示可读 builtin label；保存文件、command id 和运行请求继续使用稳定 package id。
- `62c5e41e ui(studio): 收束物性页展示口径`：Property toolbar、摘要和 package 选择卡使用可读 label，entitlement schedule notice 不再外露 timer debug 文本。

同步检查（截至 2026-06-10）：

- `docs/status/current.md` 已记录 Studio UI 第十七到第二十八刀的当前事实源和下阶段边界。
- `docs/status/current.md` 已继续同步第二十九到第三十八刀的当前事实源、能力基线和下一步边界。
- `docs/status/current.md` 已同步第四十刀 Home / Project / Property 物性包展示口径、第四十一刀独立 `物性` 页 raw id / entitlement timer debug 展示治理，并写入 2026-06-11 明天事项。
- `docs/architecture/designs/studio-client-main-brief.md` 已同步 Home、独立物性页、流程图工作台、右侧栏、底部运行区、上下文工具栏、结果定位、左侧模块页密度、右侧模块结果紧凑展示、可读 package label 和 entitlement schedule notice 展示边界。
- `docs/architecture/studio-ui-topic-plan.md` 已同步运行 / 结果上下文工具栏收束、左侧 `模块` 页 hover 说明、底部状态汇总 snapshot 标题行扫读、右侧模块结果紧凑展示、Home / Property package label 口径和 entitlement host notice 边界。
- `docs/reference/solve-snapshot-results.md` 已同步结果表定位、正式 `inspector.focus_*` command 和顶部 `结果工具栏` 不展开全部 focus command 的消费边界。
- `docs/radishflow-mvp-roadmap.md` 已修正独立 `物性` 页与左侧 `项目` 面板的输入职责，不再保留旧“右侧物性包页”口径。
- `docs/architecture/studio-ui-topic-plan.md` 与 `docs/architecture/studio-ui-design-guidelines.md` 已补入物性页到流程图建模 readiness 的状态来源和主工作流位置。

## 验证

- 2026-06-14 文档治理验证：
  - `bash scripts/check-doc-size.sh`
  - `git diff --check`
- `cargo fmt --check`
- `git diff --check`
- `cargo test -p radishflow-studio studio_gui_shell::tests::basic`
- `cargo test -p radishflow-studio studio_gui_shell::tests::canvas`
- `cargo test -p radishflow-studio right_sidebar`
- `cargo test -p radishflow-studio module_tabs_keep_unit_only_content_when_canvas_selection_is_stream`
- `cargo test -p radishflow-studio workbench_first_viewport_keeps_selection_and_status_roles_separated`
- `cargo test -p radishflow-studio bottom_workbench_renders_status_summary_as_right_split_role`
- `cargo test -p radishflow-studio bottom_status_summary_split_tracks_current_snapshot_after_run`
- `cargo test -p radishflow-studio canvas_toolbar_keeps_place_palette_in_left_module_sidebar`
- `cargo test -p radishflow-studio canvas_stage_keeps_object_tree_in_project_sidebar`
- `cargo test -p radishflow-studio workbench_real_viewport_places_major_roles_in_expected_regions`
- `cargo test -p radishflow-studio flowsheet_context_toolbar_renders_existing_canvas_run_result_state`
- `cargo test -p radishflow-studio studio_gui_window_model_builds_flowsheet_context_toolbar_from_available_commands_and_state`
- `cargo test -p radishflow-studio create_blank_project_opens_untitled_blank_workspace_without_picker`
- `cargo test -p radishflow-studio home_dashboard_exposes_unsaved_current_project_return_tile`
- `cargo test -p radishflow-studio home_open_project_returns_to_current_workspace_when_current_tile_is_selected`
- `cargo test -p radishflow-studio home_open_project_returns_to_current_workspace_when_selected_recent_is_current`
- `cargo test -p radishflow-studio property_context_toolbar_renders_existing_package_component_commands_and_state`
- `cargo test -p radishflow-studio property_screen_renders_independent_property_page_from_window_model`
- `cargo test -p radishflow-studio property_main_path_readiness_tracks_property_package_and_components`
- `cargo test -p radishflow-studio property_page_main_path_enters_flowsheet_modeling_after_basis_selection`
- `cargo test -p radishflow-studio home_current_workspace_tile_returns_to_flowsheet_after_property_main_path_ready`
- `cargo test -p radishflow-studio blank_project_feed_outlet_run_requires_project_components_before_composition`
- `cargo test -p radishflow-studio blank_project_main_path_workbench_first_viewport_uses_modeling_roles`
- `cargo test -p radishflow-studio main_path`
- `cargo test -p radishflow-studio workbench_real_viewport_places_major_roles_in_expected_regions`
- `cargo test -p radishflow-studio home_current_workspace`
- `cargo test -p radishflow-studio current_workspace`
- `cargo test -p radishflow-studio property_screen_renders_independent_property_page_from_window_model`
- `cargo test -p radishflow-studio command_q_dirty_workspace_requires_explicit_confirmation`
- `cargo test -p radishflow-studio native_options_disable_macos_default_menu_so_command_q_uses_shell_close_flow`
- `cargo test -p radishflow-studio viewport_close_dirty_workspace_cancels_native_close_and_prompts`
- `cargo test -p radishflow-studio home_dashboard_hides_return_workspace_action_before_user_opens_or_creates_case`
- `cargo test -p radishflow-studio studio_gui_window_model_surfaces_module_settings_for_active_unit`
- `cargo test -p radishflow-studio studio_gui_window_model_surfaces_unit_parameter_constraint_for_invalid_heater_pressure`
- `cargo test -p radishflow-studio runtime_module_settings_tab_consumes_module_settings_dto_for_active_unit`
- `cargo test -p radishflow-studio module_settings`
- `cargo test -p radishflow-studio studio_gui_window_model_surfaces_current_module_results_for_active_unit`
- `cargo test -p radishflow-studio bottom_status_summary_split_tracks_current_snapshot_after_run`
- `cargo test -p radishflow-studio bottom_results_table_uses_localized_compact_phase_column`
- `cargo test -p radishflow-studio bottom_results_table_routes_stream_and_unit_focus_to_matching_right_tab`
- `cargo test -p radishflow-studio result_context_toolbar_renders_existing_result_commands_and_state`
- `cargo test -p radishflow-studio run_context_toolbar_renders_existing_run_commands_and_state`
- `cargo test -p radishflow-studio module_sidebar_groups_supported_palette_by_modeling_roles`
- `cargo test -p radishflow-studio canvas_toolbar_keeps_place_palette_in_left_module_sidebar`
- `cargo test -p radishflow-studio blank_project_main_path_workbench_first_viewport_uses_modeling_roles`
- `cargo test -p radishflow-studio canvas_feed_to_flash_explicit_suggestion_selection_can_run_after_parameters`
- `cargo test -p radishflow-studio module_results`
- `cargo test -p radishflow-studio bottom_results_table`
- `cargo test -p radishflow-studio bottom_status_summary`
- `cargo test -p radishflow-studio project_sidebar_separates_inputs_objects_examples_and_review_roles`
- `cargo test -p radishflow-studio studio_gui_window_model_groups_snapshot_into_window_regions`
- `cargo test -p radishflow-studio studio_gui_window_model_surfaces_bootstrap_workspace_results_and_diagnostics`
- `cargo test -p radishflow-studio studio_gui_window_model_builds_property_context_toolbar_from_property_page_commands`
- `cargo test -p radishflow-studio snapshot_host_state_marks_backoff_retry_when_scheduler_is_blocked`
- `cargo test -p radishflow-studio snapshot_host_state_exposes_scheduled_check_notice`
- `cargo test -p radishflow-studio property_screen_renders_independent_property_page_from_window_model`
- `cargo test -p radishflow-studio property_page_main_path_enters_flowsheet_modeling_after_basis_selection`
- `cargo test -p radishflow-studio home_window_model_maps_recent_projects_to_case_tiles`
- `cargo test -p radishflow-studio home_dashboard_renders_recent_case_tiles_from_window_model`
- `cargo test -p radishflow-studio home_dashboard_exposes_unsaved_current_project_return_tile`
- `cargo test -p radishflow-studio bootstrap_keeps_untitled_blank_project_thermo_basis_unselected`
- `cargo test -p radishflow-studio bootstrap_runs_sample_workspace_from_main_entry_boundary`
- `cargo test -p radishflow-studio gui_host_dispatches_property_package_selection_command`
- `cargo test -p radishflow-studio blank_project_selects_thermo_basis_saves_reopens_and_runs_feed_flash_path`
- `cargo test -p radishflow-studio`
- `./scripts/check-repo.sh`
- `pwsh -NoProfile -Command "[scriptblock]::Create((Get-Content -Raw scripts/check-dotnet-capeopen.ps1)) | Out-Null; [scriptblock]::Create((Get-Content -Raw scripts/smoke-test.ps1)) | Out-Null"`
- `bash -n scripts/check-doc-size.sh`
- `bash scripts/check-doc-size.sh`

备注：focused test 和仓库级验证通过；`block v0.1.6` future-incompat warning 仍来自已知外部 `eframe -> wgpu -> metal` 依赖链，本轮不处理。文档体量检查报告受约束文档均在目标上限内。

## 关键决策

- 后续开发采用 `status/current.md` 总进度 + `topics/` 一级轨道专题 + 二级功能专题。代码实现前必须先确认归属专题；若没有清晰归属，应先补专题文档，而不是继续围绕局部 UI 或实现细节打补丁。
- `docs/topics/` 承载功能、能力包和开发目标的设计、范围、非目标、验收和验证；一级轨道专题管跨功能边界，二级功能专题管具体单元、建模对象、服务或页面；`docs/devlogs/` 只记录历史推进，不承担“下一步怎么做”的默认入口。
- Studio UI 已完成的多组收束不再用“第 N 刀”作为默认推进单位；后续 UI 改动必须绑定到 Studio 主路径、物性基础、建模求解、结果诊断或项目生命周期等具体专题。
- 底部右侧 `状态汇总` 是 `window.status_summary` 的展示分栏，不是新的运行控制台、收敛曲线、结果报表或 snapshot 历史管理入口。
- 底部结果表仍是轻量结果审阅入口；完整报表、跨快照报表、模板、打印和批量导出继续留在暂不推进项。
- 底部结果表的单元区应消费 `snapshot.review_summary.unit_results`，不再在 shell renderer 内重新扫描 steps；`review_summary` 是 Module Results、结果表和状态汇总之间的 case-level 同源摘要，不是第二套结果计算。
- 底部结果表的定位只表达“把当前结果对象带到正确右侧消费面”：stream 使用 `inspector.focus_stream:*` 并进入 `检查器`，unit 使用 `inspector.focus_unit:*` 并进入 `模块结果`；结果表本身不维护第二套对象导航状态。
- 右侧 `模块结果` current 态只做选中单元的紧凑审阅，不复用完整 result inspector 的长 snapshot id 和完整执行句；完整步骤、诊断和结果表仍走现有折叠区、底部结果表和正式 focus command。
- 顶部 `结果工具栏` 只承担结果页入口和状态扫读，不承担当前 snapshot 所有 stream / unit 的对象导航清单；长列表导航继续走结果表、右侧相关 action、项目树或命令面板。
- 顶部 `运行工具栏` 只承担运行页控制和底部运行信息入口；Monitor 按钮旁不重复展示状态 chip，运行状态继续由下一行状态扫读和底部右侧状态汇总承担。
- `Status Summary` 可以展示从当前 review summary 派生的 unit result count；它只做扫读，不承担完整结果表、完整报表或跨快照历史。
- 对内容 renderer 的 focused test 与整体 Workbench 布局 focused test 分离，避免布局视口裁切误伤内容语义回归。
- `模块` 面板的筛选文本是临时 UI 状态，不是项目、命令、模块库或结果的真相源；分类依据来自现有 `StudioGuiCanvasPlaceUnitKind`。
- `模块` 面板的分类和选项说明不应回到首屏常驻文本；它们可以通过 hover / DTO 保留语义，首屏优先展示可执行放置入口。
- `项目` 面板分区只是现有项目 / canvas / snapshot 状态的职责组织，不新增完整项目浏览器、项目状态缓存或结果状态。
- 中央 Canvas 不再承担对象树 / 对象过滤导航；它只消费既有 canvas presentation 显示建模主舞台、轻量状态概览和受控建议，避免与左侧 `项目` 和右侧 `检查器` 重复。
- 右侧栏的 `画布选择` 是 Canvas presentation 的上下文提示，不是新的对象状态源；模块设置 / 模块结果仍只对 active unit DTO 生效，流股选择只进入检查器和现有 stream focus command。
- 中央 Canvas 顶部工具区只承接视图和 suggestion 工具；选中对象的移动 / 连接恢复等命令归入 `画布操作`，放置入口归入左侧 `模块` 面板，对象语义详情归入右侧 `画布选择`，避免首屏重复展示同一选择状态。
- 底部薄状态栏是窗口级扫读带，不再承担完整状态汇总；完整 case / convergence / steps / diagnostics 仍由底部右侧 `状态汇总` 卡片消费同一 `window.status_summary`。
- 中央 Canvas 顶部工具区不再承接放置 palette；左侧 `模块` 面板是受控单元放置入口，中央只保留视图命令、当前建议命令和选中对象操作。
- 顶部 `流程图工具栏` 是 screen-level 上下文入口，不是第三个画布工具条；Canvas 段只保留已有 suggestion / pending-edit command，放置入口归左侧 `模块`，选中对象移动 / 连接动作归中央 `画布操作`。
- 普通空白项目的正式建模顺序是先 `物性` 再 `流程图`；Home 的当前 workspace tile 是当前文档的返回入口，不是 recent projects 持久化项，也不是第二套项目状态。
- `物性` 到 `流程图` 的 readiness 只表达 UI 主路径是否选齐 package 和项目组分；它不替代正式 run package resolution，也不改变 modeling readiness 对缺项目组分、缺 composition、参数缺失等求解前输入缺口的拦截边界。
- 内部进入流程图建模入口也必须服从同一 Property readiness；按钮禁用、Home 返回和直接入口都不应绕过 `物性` 主路径。
- 普通空白项目进入空 Workbench 后的首屏职责按区域固定：放置入口归左侧 `模块`，对象树归左侧 `项目`，选择语义归右侧 `画布选择 / 检查器`，消息和状态汇总归底部，结果入口只表达当前缺少 snapshot，不提前展示完整报表。
- Home 显式 `返回工作区` 是当前已打开项目的继续入口；最近项目卡片仍可作为状态和选择列表，但不要求用户通过双击当前卡片来表达“回到工作区”。
- Home recent / current / example case tile 只展示从当前 document / builtin package choice 映射出的可读 label；稳定 package id 仍只留在项目文件、command id、运行请求和内部状态边界，不从运行结果反推第二套物性状态。
- 独立 `物性` 页 toolbar、摘要和 package 选择卡同样只展示可读 package label；entitlement timer 的具体事件和 `SystemTime` 结构属于内部调度信息，host notice 和主界面只展示用户可判断的授权检查摘要。
- 当前 MVP 的 `物性` 页不渲染只有未来含义的二级导航；长期物性工作区空间仍保留在设计文档中，但当前实现只呈现受控 package / component 选择和摘要。
- `Cmd+Q` 与窗口关闭按钮必须共用同一关闭确认路径，避免在 macOS 退出入口绕过保存 / 另存为确认。
- macOS 默认 App 菜单的 `Quit` 项会绕过 egui shell 快捷键层；当前阶段禁用 winit 默认菜单，让 `Cmd+Q` 由 Studio 自己的关闭确认路径处理。
- `dev` 是日常开发分支，不自动触发 CI/CD；CI/CD 自动入口只放在 `dev -> master/main` 稳定化 PR，默认分支门禁同时覆盖 Rust workspace、Windows `.NET` CAPE-OPEN baseline 和 Windows staging package。
- `Release Checks` 当前只保留手动 staging preflight，不因 tag push 自动运行；正式 GitHub Release、安装包发布、COM 注册和 PME 自动化仍不进入当前阶段。

## 风险与未完成项

- 首批专题文档是节奏治理和边界入口，不代表对应专题全部完成；后续仍需要按当前激活专题补主路径 smoke 记录模板，并把代码切片绑定到明确退出标准。
- 现有历史结论仍分布在 `mvp/scope.md`、路线图、Studio UI 专题计划和周志中；后续应按真实改动逐步迁移到对应专题，避免一次性大搬家造成文档噪声。
- 本刀只完成底部左右分栏第一刀，没有做视觉精修、可拖拽高度、完整底部区域响应式方案或完整收敛曲线。
- 左侧 `模块` 面板已完成分类与本地筛选第一版；完整模块库、自由连线、自动布线、完整拖拽布局和更复杂的模块详情卡仍未推进。
- 左侧 `项目` 面板已完成职责分区第一版；更细的对象过滤、搜索、树形折叠、项目级状态卡视觉仍未推进。
- 中央 Canvas 只做职责收束和状态概览；真实窗口 smoke 已覆盖当前主路径截图，但仍未做完整浮动工具条体系、画布标签页视觉细化或完整视图工具。
- 右侧栏已完成选择协同上下文和 `模块结果` current 态密度收束；仍未做完整右侧栏视觉重排、模块详情页、完整参数表、完整模块结果页或 help command。
- 中央 Canvas 本轮没有推进画布标签页、自由连线、完整拖拽布局或完整工具条体系；`画布操作` 仍只暴露现有正式 command surface。
- 真实窗口 smoke 已复核 `结果` 顶部、`运行` 顶部、左侧 `模块` 页、底部 `状态汇总` 和右侧 `模块结果`；后续若继续做密度治理，仍应沿正式 DTO / command surface 小切片推进。
- 除用户提供的真实窗口 smoke 截图外，本轮验证仍以 focused tests 为主；后续若继续推进 Workbench 视觉密度，仍应先复用现有 window model / command surface。
- Home 当前 workspace tile 只解决返回当前案例入口；未推进完整 Home tile gallery 视觉重排、工作区历史管理或未保存项目持久化。
- Property 页本轮只补主路径交接入口和状态一致性，没有推进完整组分数据库、第三方物性包、完整参数表、物性分析页或完整视觉重排。
- 人工 smoke 已确认 Home 返回按钮、物性页两列密度、空 Workbench 密度和模块设置摘要没有新问题；`Cmd+Q` 系统级 Quit 路径补修已在重启后的真实 macOS 窗口里复核通过。
- Windows `.NET` baseline 和 staging package 需要在 GitHub Windows runner 或本机 Windows 环境最终验证；本机 macOS 只能做脚本语法和仓库文本级验证，不能执行 `net10.0-windows7.0` contract tests。

## 后续事项（2026-06-14 后）

1. 优先为 `docs/topics/studio-main-workflow.md` 补主路径 smoke 记录模板，覆盖 Home -> 物性 -> 流程图 -> 运行 -> 结果审阅 -> 保存 / 重开 / rerun。
2. 下一轮代码切片必须绑定到一个一级轨道专题和一个二级功能专题的阶段目标与退出标准；没有专题归属的新功能先补文档。
3. 继续不扩完整模块库、完整项目浏览器、完整参数表、自由连线、自动布线、完整报表、跨快照报表或发布自动化；`block v0.1.6` warning 暂不处理，除非进入独立依赖治理任务。
