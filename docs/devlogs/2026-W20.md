# 2026-W20 Development Log

周期：2026-05-11 至 2026-05-17

## 本周当前摘要

- 本周开场继续沿 `docs/status/current.md` 的数值主线推进，优先检查 solver / ui step 上残留的 `id-only` 辅助字段，而不是回到 Canvas、布局或结果报表扩张。
- 2026-05-11 已先收口 `rf-solver::UnitSolveStep` 的并行真相源：内部 `consumed_stream_ids / produced_stream_ids` 已删除，step 输入/输出当前只保留结构化 stream 快照，summary、diagnostic related streams 和 FFI 导出所需 id 列表都改为从这组 stream DTO 现场派生。
- 同日又同步清理 Studio 最新单元结果 DTO：`StudioGuiWindowUnitExecutionResultModel` 不再额外缓存 `consumed_stream_ids / produced_stream_ids`；Active Inspector、Result Inspector 与 diagnostic action 继续只消费 stream result reference / action 列表，不再在窗口结果面再保留一份并行 id-only 字段。

## 本周目标

- 继续把数值主线和 `SolveSnapshot` 结果消费面收口到单一结构化 DTO 真相源。
- 保持 `Feed/Heater/Cooler/Valve/Mixer -> Flash` near-boundary 回归与 Studio 只读消费边界稳定。

## 当前完成情况

- 2026-05-11：`rf-solver` 已移除 `UnitSolveStep` 内部 `consumed_stream_ids / produced_stream_ids` 存储；solver step summary、diagnostic `related_stream_ids` 和 step execution failure context 仍保持既有语义，但改为从 `consumed_streams / produced_streams` 派生。
- 2026-05-11：`rf-ffi::FfiUnitSolveStepJson` 继续保留现有 `consumedStreamIds / producedStreamIds` 导出字段，以兼容当前 JSON 消费面；这些字段现在只从 `rf_solver::UnitSolveStep` 的结构化 stream 快照派生，不再依赖 solver 内部并行 id-only 存储。
- 2026-05-11：Studio 窗口模型中的最新单元结果已同步改为以 `consumed_stream_results / produced_stream_results` 为真相源；相关 focused tests 也改为从 stream reference 列表派生并断言 ids，而不是读取单独缓存字段。
- 2026-05-11：继续沿同一条 snapshot boundary 收口后，`StudioGuiWindowSolveStepModel` 自身也已移除 `consumed_streams / produced_streams` 缓存；当前 Result Inspector / Active Inspector 的 step 过滤、step summary 与 diagnostic action 都改为从 stream result reference 直接派生 id，不再在窗口 step presentation 再保留并行 id-only 字段。
- 2026-05-11：继续补齐 `rf-ffi` 验证基线时，定位到 demo package 与 runtime package test fixture 仍沿用旧 Antoine 边界斜率，导致 `feed-1` solved path 在 fixed-pressure bubble/dew temperature 估算时直接报 `Antoine correlation produced a non-finite saturation temperature`；现已把 `rf-ffi` 两套 fixture 对齐到当前数值基线，`cargo test -p rf-ffi` 已恢复通过。
- 2026-05-11：继续清点 legacy fixture 分叉后，`examples/flowsheets/failures/*.rfproj.json` 已统一从旧 `component-a / component-b` 切到 official `methane / ethane` 组件目录；`flowsheet_examples`、`studio_solver_bridge` 与 `studio_workspace_control` 里直接消费这些 failure 样例的测试路径，以及相关 cached-package helper，也已同步切到 `binary-hydrocarbon-lite-v1` official hydrocarbon 语义，不再依赖旧 synthetic package 语义碰巧兼容。
- 2026-05-11：继续收口 helper / package id 语义时，`tests/rust-integration` 里旧 `feed-*-flash.rfproj.json` synthetic demo 族已从 official hydrocarbon 包 id 下拆开；对应 helper 现已改成显式 synthetic demo 命名，并切到独立 `binary-hydrocarbon-synthetic-demo-v1` package id。`rf-ffi` 内建 demo package 也已同步切到同一条 synthetic demo 语义，不再和 `binary-hydrocarbon-lite-v1` official hydrocarbon 包目录撞车。
- 2026-05-11：继续收掉残余命名歧义后，`tests/rust-integration::write_cached_package` 已改名为显式 `write_official_binary_hydrocarbon_cached_package`；旧 `feed-heater-flash` / `feed-cooler-flash` / `feed-valve-flash` / `feed-mixer-flash` / `feed-mixer-heater-flash` synthetic 示例也统一改成 `*-synthetic-demo.rfproj.json`，并同步更新 `rf-solver`、`rf-ffi`、Studio example catalog、集成测试与相关文档引用。
- 2026-05-11：继续把同类语义收口到底后，`apps/radishflow-studio` 本地测试模块里 residual `write_cached_package` wrapper 也已统一改成显式 official 命名；`app_facade`、`run_panel_driver` 与 `workspace_control` 现在都与 `tests/rust-integration` 共享同一条 official hydrocarbon helper 口径，不再在 Studio crate 内保留最后一层泛名缓存 writer。
- 2026-05-11：继续把 shared helper public surface 也收清楚后，`apps/radishflow-studio::test_support` 现在已只对外暴露显式 official binary hydrocarbon provider / in-memory provider / cached-package writer；底层仍需 remap `package_id` / component ids 的 official sample-derived payload helper 已退回私有实现，`solver_bridge`、`workspace_solve_service`、runtime 与 window-model 测试不再直接依赖带 `component_specs` 的 `binary_hydrocarbon_lite_*` 泛称 API。
- 2026-05-11：继续把这条线从“改名”推进到“去重复脚手架”后，`apps/radishflow-studio::test_support` 已新增 shared `OFFICIAL_BINARY_HYDROCARBON_PACKAGE_ID` 常量与 `write_default_official_binary_hydrocarbon_cached_package`；`app_facade`、`run_panel_driver`、`workspace_control` 三处本地默认 wrapper 已删除，`solver_bridge` 与 `workspace_solve_service` 也同步切到这组 shared constant/default helper，不再重复手写 default `timestamp/expires_at` 或散落 `binary-hydrocarbon-lite-v1` literal。
- 2026-05-11：继续回到 near-boundary / snapshot consumer 主线后，Studio 侧又已补两层正式回归：`studio_gui_shell::tests::basic` 现在会锁 official `Feed/Heater/Cooler/Valve/Mixer -> Flash` 场景下 stream / comparison / unit selector 的 shell state 切换语义；`studio_gui_shell::tests::runtime` 则继续把同一路径前推到最终 `Result Inspector` 渲染面，验证 base stream 切到原 compared stream 时 comparison 会按 shell state 规则清空，重新选择 compared stream 后会恢复同一份 flash outlet comparison / unit view consumer 结果。

## 关键决策

- solver step 与 Studio 单元结果的 id 列表如果仍有展示或兼容价值，应视为“由结构化 stream snapshot 派生的 presentation/interop 字段”，而不是第二份状态真相源。
- 当前不继续扩大到重构 `StudioGuiWindowSolveStepModel` 的所有 id 列表字段；先优先清掉真正跨层并行存储的 `consumed_stream_ids / produced_stream_ids`，保持变更聚焦并锁定回归。
- 对历史 failure fixture 的治理优先级应高于继续堆更多 synthetic fallback：若样例真正承载的是连接 / 诊断 / 恢复语义，而不是有意验证 synthetic 物性行为，就应尽量统一到 official hydrocarbon fixture 与正式 package id 语义，减少测试基线分叉。
- 对 intentional synthetic demo 族，也应给出显式 package id 与 helper 命名，而不是继续借用 official hydrocarbon id；否则即使数值本身正确，也会把测试载荷语义继续混淆在一起。
- 对 Studio crate 内 shared fixture helper 的后续治理，也应优先把“默认 official cache fixture”这种稳定脚手架收进公共 helper，而不是让多个测试模块各自重复补 `timestamp/expires_at` 默认值；否则命名即使已澄清，维护成本仍会继续分散在调用面。
- 当前 Studio near-boundary consumer 回归若继续推进，应优先沿 selector state、focus command、diagnostic action 和 runtime 渲染这条正式消费链补薄弱环节；同样的语义不要再额外造一层 shell 私有 fallback 或测试专用状态面。

## 验证记录

- `cargo fmt --all`
- `cargo test -p rf-solver`
- `cargo test -p radishflow-studio studio_gui_window_model`
- `cargo test -p rf-rust-integration --test flowsheet_examples`
- `cargo test -p rf-rust-integration --test studio_solver_bridge`
- `cargo test -p rf-rust-integration --test studio_workspace_control`
- `cargo test -p rf-solver -p rf-ffi -p rf-ui -p rf-store -p radishflow-studio --no-run`
- `cargo test -p rf-ffi`
- `cargo test -p radishflow-studio solver_bridge`
- `cargo test -p radishflow-studio workspace_solve_service`
- `cargo test -p radishflow-studio app_facade`
- `cargo test -p radishflow-studio workspace_control`
- `cargo test -p radishflow-studio run_panel_driver`
- `cargo test -p radishflow-studio result_inspector_state_tracks_official_near_boundary_flash_selector_transitions -- --nocapture`
- `cargo test -p radishflow-studio runtime_panel_renders_official_near_boundary_result_inspector_selector_transitions -- --nocapture`
- `cargo test -p radishflow-studio official_near_boundary -- --nocapture`
- `cargo fmt --all --check -- crates/rf-solver/src/lib.rs crates/rf-solver/src/tests.rs crates/rf-ffi/src/engine.rs apps/radishflow-studio/src/studio_gui_window_model/mod.rs apps/radishflow-studio/src/studio_gui_window_model/result_inspector.rs apps/radishflow-studio/src/studio_gui_window_model/tests.rs tests/rust-integration/tests/flowsheet_examples.rs`

补充说明：

- `cargo fmt --all --check -- ...` 当前仍会顺带暴露工作区内若干与本轮任务无关的既有格式差异，因此本轮只对 touched 文件做 focused code/test 验证，不顺手改写无关文件。

## 风险与后续

- `rf-ffi` 当前仍需后续单独复核 thermo solve 失败基线，避免把与 step DTO 无关的既有环境/fixture 问题误记为本轮回归。
- 若继续做全仓 fixture/provider 语义收口，当前 official/synthetic 命名歧义与默认 official cache fixture scaffolding 已基本清掉；后续若还要继续，可转向剩余的 test boilerplate 去重，或直接回到 near-boundary / snapshot consumer 主线，而不是继续围绕 helper 身份语义打转。
