# 2026-W14 Development Log

周期：2026-03-30 至 2026-04-05

## 本周目标

- 收口本地客户端与远端控制面的系统架构口径
- 明确桌面交付形态、远端授权边界与受控资产分发方式
- 在控制面边界冻结后继续恢复 `rf-thermo` / `rf-flash` 数值主线推进

## 完成情况

- 更新 `radishflow-architecture-draft.md`，把目标系统正式收口为“Rust 客户端 + `.NET 10` CAPE-OPEN 桥接 + 外部 `ASP.NET Core / .NET 10` 控制面”
- 更新 `architecture/overview.md`，明确外部控制面是系统级正式组成，但当前不作为本仓库内实现目录
- 更新 `architecture/auth-entitlement-architecture.md`，明确控制面优先采用 `ASP.NET Core / .NET 10`，资产分发优先采用对象存储 / CDN / 下载网关 + 短时票据
- 更新 `mvp/scope.md` 与 `radishflow-mvp-roadmap.md`，补齐桌面交付形态、远端控制面技术栈与当前仓库职责边界
- 更新 `docs/README.md` 与开发日志索引，确保文档阅读入口与本周记录同步
- 调整 `adapters/dotnet-capeopen` 占位目录结构，使 `RadishFlow.CapeOpen.sln`、`Directory.Build.props` 与各 `.NET` 占位项目目录直接同级放置
- 在 `rf-thermo` 中实现基于 Antoine 相关式的饱和蒸气压计算与理想体系 `K` 值估算，并补充对应单元测试
- 在 `rf-flash` 中实现基于 `K` 值估算与 Rachford-Rice 的最小二元 `TP Flash` 求解，并补充两相/单液相测试
- 在 `tests/thermo-golden` 与 `tests/flash-golden` 中建立首批可执行黄金样例，并接入 `rf-thermo` / `rf-flash` 集成测试
- 在 `rf-unitops` 中建立内建单元规范、统一流股输入输出接口，并实现 `Feed`、`Mixer`、`Flash Drum` 的最小行为边界
- 在 `rf-flowsheet` 中建立首轮材料端口连接校验，覆盖 canonical port signature、流股存在性与“一股一源一汇”约束
- 在 `rf-solver` 中建立首轮无回路顺序模块法，支持内建 `Feed`、`Mixer`、`Flash Drum` 的标准材料流股执行
- 在 `examples/flowsheets` 中新增 `feed-mixer-flash.rfproj.json`，并补齐“加载项目 -> 求解 -> 读取结果”的最小端到端测试
- 在 `rf-unitops` 中补上 `Heater/Cooler` 的最小行为，采用 outlet 流股模板承载当前阶段温压设定
- 在 `rf-solver` 中接入 `Heater/Cooler`，并把单元执行失败包装为带 unit id / kind 的诊断错误
- 在 `examples/flowsheets` 中新增 `feed-heater-flash.rfproj.json`，把第二条 `Feed -> Heater -> Flash Drum` 端到端回归纳入测试基线
- 在 `rf-unitops` 中补上 `Valve` 的最小行为，当前采用 outlet 流股模板承载目标压力，并拒绝升压型默认配置
- 在 `rf-solver` 中接入 `Valve`，并新增 `Feed -> Valve -> Flash Drum` 的第三条内核闭环样例与项目文件回归
- 把 `tests/rust-integration` 从占位目录升级为正式 workspace crate，并把三条示例 `*.rfproj.json` 接入仓库级端到端求解回归
- 为 PR / release workflow 补上 Windows 侧 `check-repo.ps1 -SkipTextFiles` baseline，避免正式 CI 只覆盖 Linux 路径
- 在 `rf-solver` 中补上最小求解诊断层，让 `SolveSnapshot` 附带 summary / diagnostics / step 明细，并让单元执行失败至少带 step 序号和 inlet stream 上下文
- 在 `rf-ui` 中补上从 `rf-solver::SolveSnapshot` 到 UI 层 `SolveSnapshot` 的映射桥接，为后续应用状态接入真实求解结果做准备
- 在 `apps/radishflow-studio` 中补上工作区求解桥接，可通过 `PropertyPackageProvider` 或本地 `StoredAuthCacheIndex` 执行真实 solve，并把结果回写到 `rf-ui::AppState`
- 在 `apps/radishflow-studio` 中继续把这条桥接上收为 `WorkspaceSolveService`，统一默认 `snapshot_id` / `sequence` 生成、显式运行请求和工作区求解入口
- 在 `apps/radishflow-studio` 中继续把 `WorkspaceSolveService` 收口为手动/自动两类运行触发入口，并把 `SimulationMode` / `pending_reason` 的运行门控上收到应用层
- 在 `apps/radishflow-studio` 中新增 `WorkspaceRunCommand`，把“触发类型 + package 选择”从底层求解 API 中抽出，并冻结当前默认包选择为保守的“唯一候选才自动选中”策略
- 形成一组连续的 Studio 应用层收口提交：`442f216`、`6a2eeb7`、`d01e6da`、`0f93c92`，分别覆盖求解 bridge、求解 service、运行 dispatch 与运行命令层
- 在 `apps/radishflow-studio` 中新增 `StudioAppFacade`，把 auth cache、`WorkspaceRunCommand`、结果派发摘要和后续异步执行边界收口为明确的桌面应用命令入口
- 调整 `WorkspaceRunCommand` 自动触发路径，在命中 `HoldMode` / `NoPendingRequest` 时先返回 skip，再决定是否需要 preferred package 解析，避免多包缓存场景下的无意义失败
- 在 `apps/radishflow-studio` 中新增 `run_studio_bootstrap` 与 `src/main.rs` 最小 bootstrap 入口，把示例项目加载、本地样例 auth cache 构造与最小桌面运行命令触发点接通
- 为 Studio bootstrap 入口补上测试，覆盖手动运行、Preferred 自动选包与 Automatic/Hold skip，并让 skip 结果也写入 `AppLogFeed`
- 扩 `StudioWorkspaceRunDispatch`，把最新 snapshot 摘要、日志条数和最新日志项统一收口为结构化结果派发，避免入口层直接翻读完整 `AppState`
- 扩 `StudioAppCommand`，补上 `ResumeWorkspace` 与 `SetWorkspaceSimulationMode`，并新增 `StudioWorkspaceModeDispatch` 作为运行控制结果派发
- 为 `Hold -> Active` 恢复路径补测试，确认 `ResumeWorkspace` 会先切到 `Active` 再按 Automatic 语义发起运行
- 新增 `workspace_control` 模块，补 `WorkspaceControlAction` / `WorkspaceControlState` / `dispatch_workspace_control_action_with_auth_cache(...)`，把运行栏 / 状态栏动作入口和状态摘要收口为单独契约
- 让 `run_studio_bootstrap` 与 `main.rs` 直接消费 `WorkspaceControlState`，作为当前最小桌面入口对运行栏契约的第一处真实使用
- 在 `rf-ui` 中新增 `RunPanelState`，并由 `AppState` 基于 `SolveSessionState`、最新 `SolveSnapshot` 与最新日志自动刷新运行栏摘要
- 在 `apps/radishflow-studio` 中补 `WorkspaceControlState -> rf_ui::RunPanelState` 映射，让 Studio 侧控制面摘要正式写回 UI 运行栏状态
- 在 `rf-ui` 中补出 `RunPanelIntent` / `RunPanelPackageSelection`，并在 Studio 中补对应映射与分发入口，形成不反向依赖 Studio 类型的最小 UI 运行命令接口
- 让 `run_studio_bootstrap` 改为直接消费 `RunPanelIntent`，使当前最小桌面入口也遵守 UI 意图 -> Studio 控制动作的正式边界
- 在 `rf-ui` 中补出 `RunPanelCommandModel`，把 `Run/Resume/Hold/Active` 的按钮可见性、可用性和默认主动作冻结到 UI 层
- 在 `rf-ui` 中补出 `RunPanelViewModel`，把主按钮/次按钮槽位和最小运行栏渲染 DTO 冻结到 UI 层
- 让 `run_studio_bootstrap` 与 `apps/radishflow-studio/src/main.rs` 直接消费 `RunPanelViewModel`，作为当前第一处真实使用 `RunPanelCommandModel` 的最小运行栏入口
- 在 `rf-ui` 中进一步补出 `RunPanelTextView`，把 bootstrap/main 的最小文本渲染组织也收回 UI 层，避免 Studio 再持有一套运行栏展示拼装逻辑
- 在 `rf-ui` 中继续补 `RunPanelViewModel::{dispatchable_primary_intent, dispatchable_intent}`，把“可触发动作 -> `RunPanelIntent`”解析也收回 UI 层
- 在 `rf-ui` 中新增 `RunPanelPresentation`，把 `view + text + dispatchable intent` 收口为单一运行栏组件入口，并让 bootstrap/main 改为直接消费它
- 在 `rf-ui` 中新增 `RunPanelWidgetModel` / `RunPanelWidgetEvent`，把主按钮激活、禁用动作和缺失动作的最小交互结果也冻结为 UI 组件语义
- 在 `apps/radishflow-studio` 中新增 `dispatch_run_panel_widget_event_with_auth_cache(...)`，把 `RunPanelWidgetEvent` 正式桥接回 `WorkspaceControlAction`
- 在 `apps/radishflow-studio` 中新增 `StudioBootstrapTrigger::{Intent, WidgetPrimaryAction, WidgetAction}`，并让 bootstrap 默认改走主按钮触发路径
- 在 `apps/radishflow-studio` 中新增 `run_panel_driver`，把运行栏 widget 的构建、激活和事件分发回收为单独应用层入口
- 进一步把 bootstrap 默认触发改为 `StudioBootstrapTrigger::WidgetPrimaryAction`，明确表达最小入口走的是“点击主按钮”而不是裸意图
- 在 `rf-ui::EntitlementState` 中新增独立 `notice` 与 non-blocking error 记录口径，让 entitlement / offline lease 的联网失败不再混入运行栏 notice
- 在 `apps/radishflow-studio` 中新增 `entitlement_control` 模块，并把 `SyncEntitlement` / `RefreshOfflineLease` 正式接入 `StudioAppFacade` 的 command/result 体系
- 在 `rf-ui` 中新增 entitlement panel 的 state / presenter / text / widget 组件链，并在 `apps/radishflow-studio` 中补上 `entitlement_panel_driver`，把 entitlement widget event 正式桥接回 control plane command
- 让 `run_studio_bootstrap` 与 `apps/radishflow-studio/src/main.rs` 直接消费 entitlement panel widget，使最小桌面入口不再通过裸 command 演练 entitlement，而是通过正式 UI 动作模型触发
- 在 `apps/radishflow-studio` 中新增 entitlement preflight 策略，让 bootstrap 启动阶段可按“缺失快照先 sync、离线租约临近过期先 refresh”的最小规则自动调度 control plane 动作
- 在 `apps/radishflow-studio` 中继续把 entitlement preflight 推进为会话内 scheduler：新增 entitlement snapshot 临近过期同步、联网失败退避、下一次建议检查时机计算，并让 bootstrap / `main.rs` 输出 session schedule 摘要
- 在 `apps/radishflow-studio` 中新增 `entitlement_session_driver`，把 session tick、entitlement panel 手动动作和 session state 回写收口为统一应用层入口，并让 bootstrap 改走这层会话宿主
- 在 `apps/radishflow-studio` 中继续把会话宿主事件显式化，新增 `EntitlementSessionEvent`，用 `SessionStarted` 与 `EntitlementCommandCompleted` 等事件承接会话自动 tick 和命令完成后的 session state 推进
- 在 `apps/radishflow-studio` 的 bootstrap / `main.rs` 最小入口中补出显式 `LoginCompleted` / `TimerElapsed` session event 触发口径，并让 bootstrap 报告区分应用命令结果与 entitlement session event 结果，避免后续 GUI 接线时把定时器事件硬塞进运行命令通道
- 在 `apps/radishflow-studio` 中新增 `entitlement_session_host`，把 entitlement session event、panel 主动作和指定动作统一收口为宿主入口，并让 bootstrap 改为复用这层，而不是继续手写 runtime 分发
- 在 `apps/radishflow-studio` 中继续扩 `entitlement_session_host`，补出 `NetworkRestored` / `WindowForegrounded` 生命周期事件，并先映射到既有 session tick 语义，为后续真实 GUI 生命周期宿主接线留统一入口
- 在 `apps/radishflow-studio` 中继续把 `entitlement_session_host` 往宿主侧再收一步，新增 timer arm 摘要，让 `next_check_at` / backoff / 立即检查语义可直接被 GUI 定时器宿主消费
- 在 `apps/radishflow-studio` 中继续把 `entitlement_session_host` 往宿主提示口径再收一步，补出 host-derived notice，让“自动检查已安排”“退避后重试”不必继续挤进 control plane command 自身 notice
- 在 `apps/radishflow-studio` 中继续补 host notice 到 entitlement panel 的宿主侧覆盖口径：只有 runtime 自身没有 notice 时才采用 host notice，不把这类宿主提示回写进 `EntitlementState`
- 在 `apps/radishflow-studio` 中继续把 `entitlement_session_host` 收到最终宿主执行口径，新增 `Schedule / Reschedule / Keep / Clear` 定时器决策，避免 GUI 自己再比较前后 timer 状态
- 在 `apps/radishflow-studio` 中继续把 `entitlement_session_host` 收到统一宿主快照口径，新增 `EntitlementSessionHostSnapshot`，把 schedule、timer arm、timer command、host notice 与 panel driver state 聚合为单一消费对象，并让 bootstrap / `main.rs` 改为直接消费它
- 在 `apps/radishflow-studio` 中继续把 bootstrap 最小入口向真实宿主行为靠拢，保留当前已挂 entitlement timer 上下文，让后续 `TimerElapsed` / `NetworkRestored` 一类宿主事件能直接产出 `Reschedule / Keep` 级别的决策，而不是每次都从 `Schedule` 起步
- 在 `apps/radishflow-studio` 中继续把宿主上下文记忆逻辑从 bootstrap 私有实现收回 `entitlement_session_host`，新增 `EntitlementSessionHostContext` 统一承接当前 timer 与上一份 host snapshot 的推进，避免真实 GUI 宿主再次复制这套状态管理
- 在 `apps/radishflow-studio` 中新增 `EntitlementSessionHostPresentation / TextView`，把 `main.rs` 里散落的 entitlement host schedule/timer/notice 文本输出正式收口回宿主模块，并补测试覆盖 presentation/text view
- 修正 offline refresh 后本地 cache record 与 manifest 文件过期时间不同步的问题，避免刷新后再进入求解路径时误报 `LocalCacheUnavailable`
- 形成连续提交 `69a0733`、`0ecca90`、`b47e5b0`、`cf6b300`、`da4bc83`，分别覆盖运行结果分类、运行栏提示上浮、entitlement control 应用层命令接入、bootstrap entitlement trigger 与 entitlement panel widget flow；本轮继续在此基础上推进 entitlement preflight 与 offline refresh 根因修复
- 扩 `run_studio_bootstrap`，新增 entitlement sync / offline refresh 两类 trigger，并内置 sample control plane client，让 `main.rs` 最小入口能真实演练控制面授权链路
- 把 `DWSIM` 纳入正式外部参考，补充其在 `Interfaces / FlowsheetBase / FlowsheetSolver / UnitOperations` 拆分、自动化测试入口和图形对象/求解对象分离方面对当前架构的启发
- 新增 `docs/architecture/open-source-references.md`，系统整理可直接借鉴代码的开源热力学/算法库与只借鉴架构行为的流程模拟参考项目

## 关键决策

- RadishFlow 当前正式采用“本地 Rust 客户端 + 远端 `.NET 10` 控制面”的系统路线
- 远端控制面当前正式推荐采用 `ASP.NET Core / .NET 10`，不额外引入 Go 作为新的后端主语言
- 桌面端交付当前正式采用“压缩包展开后直接启动”的原生客户端形态，不追求单文件产物
- 远端控制面与资产分发属于系统外部依赖；当前仓库继续聚焦 Rust 客户端与 `.NET 10` CAPE-OPEN 适配层，不把服务端代码强行并入当前 workspace
- `rf-unitops` 第一轮统一接口当前正式收口为“标准 `MaterialStreamState` 输入输出 + 必要热力学服务注入”，不提前混入 flowsheet 调度和 FFI 细节
- `rf-flowsheet` 第一轮连接校验当前正式收口为 canonical material 端口签名、流股存在性与“一股一源一汇”，终端产品流暂允许只有 source、没有 sink
- `rf-solver` 第一轮当前正式收口为“无回路 + 内建单元 + 标准材料流股 + 最小 `SolveSnapshot`”，不提前引入 recycle、tear stream 或复杂诊断层
- Studio 当前正式收口出一条“加载物性包 -> 调用 solver -> 回写 `AppState`”的最小真实求解链路，但暂不把它误当成最终 UI 运行入口
- Studio 当前先把工作区求解入口正式收口到 `WorkspaceSolveService`，底层 `solver_bridge` 继续只负责显式 request 桥接与 provider/solver 接线
- Studio 当前已把“是否应运行”的基础判断收口到 `WorkspaceSolveService`，为后续桌面命令绑定减少 UI 侧分支判断
- Studio 当前已具备一层更接近 UI 的运行命令对象，但暂不在多包场景下隐式猜测 package，避免后续桌面行为与授权/缓存状态脱节
- Studio 当前已补出 `StudioAppFacade` 这一层显式 app command 入口，并已接到 `main.rs` 的最小 bootstrap 触发点；但最终 UI 触发点、后台任务调度和更细的运行事件流仍待继续冻结
- Studio 当前已把 `ResumeWorkspace` 与 `SetWorkspaceSimulationMode` 收口为应用命令，但真正的运行栏 / 状态栏按钮绑定口径仍未冻结
- Studio 当前已补出 `WorkspaceControlAction` / `WorkspaceControlState` 这一层运行栏契约，`rf-ui` 也已正式消费最小 `RunPanelState`；当前又进一步冻结到 `RunPanelWidgetModel` 并接到 bootstrap/main，且 widget event 已能回桥到 Studio 命令链、`run_panel_driver` 已收住最小驱动流程；但完整桌面事件流和最终 GUI 控件仍待继续收口
- entitlement / offline lease 当前正式走 `StudioAppFacade` 的独立 command/result 通道，并在 `rf-ui::EntitlementState` 中保留独立 notice；当前不再把控制面失败继续塞入运行栏 notice
- bootstrap/main 当前已能通过 entitlement panel widget + sample control plane client 真实演练 entitlement sync / offline refresh，但仍只是最小入口，不代表最终 GUI 已冻结真实联网任务调度方式
- entitlement 当前已形成首版会话内 scheduler 口径：缺失快照优先 sync、离线租约临近过期优先 refresh、entitlement snapshot 临近过期补 sync；连接/服务失败走短退避，鉴权/本地状态失败走长退避，并显式计算下一次 session check 时机
- entitlement 当前已补出独立 `entitlement_session_driver`，bootstrap 的自动 tick 与手动 entitlement widget 触发都通过这层回写 session state，为后续 GUI 生命周期事件与定时器宿主复用同一入口
- bootstrap / `main.rs` 当前已能显式演练 `LoginCompleted` / `TimerElapsed` 两类 entitlement session event，不再只有 `SessionStarted` 这一条最小入口
- Studio 当前也已补出 `entitlement_session_host`，作为后续 GUI 生命周期事件、定时器宿主和 panel 动作的统一接线面
- `entitlement_session_host` 当前也已显式覆盖 `NetworkRestored` / `WindowForegrounded` 两类 GUI 生命周期事件，不再只有登录完成和定时器到点
- `entitlement_session_host` 当前也已能把下一次检查时机收口成 timer arm，对 GUI 宿主不再只暴露裸 `next_check_at`
- `entitlement_session_host` 当前也已能导出宿主侧建议 notice，帮助 GUI 在不污染 `EntitlementState.notice` 的前提下呈现自动检查/退避状态
- bootstrap 当前也已开始通过宿主侧覆盖把 host notice 显示到 entitlement panel，而不是要求 control plane command 自己承担这类提示
- `entitlement_session_host` 当前也已能产出显式 timer command，为后续真实 GUI 宿主直接挂/重挂/清理定时器提供统一输出
- `entitlement_session_host` 当前也已把 schedule、timer arm/command、host notice 与 panel driver state 收口成统一 `EntitlementSessionHostSnapshot`，bootstrap / `main.rs` 不再各自散落读取多组 entitlement host 字段
- bootstrap 当前也已开始保留当前已挂 entitlement timer，并把它继续传回宿主快照计算，最小入口里不再只能看到“从无到有的 Schedule”，也能真实演练 `Reschedule / Keep`
- `entitlement_session_host` 当前也已补出可复用 `EntitlementSessionHostContext`，把 timer/snapshot 上下文推进重新收回宿主层，bootstrap 只是第一个复用者，不再持有专用私有实现
- `entitlement_session_host` 当前也已补出 `EntitlementSessionHostPresentation / TextView`，宿主 schedule/timer/notice 的文本组织继续留在 Studio 宿主层，而不是散回 bootstrap / `main.rs`
- `DWSIM` 当前正式作为外部架构参考来源之一，但只吸收其模块边界、自动化入口和 solver 组织经验，不直接迁移 GPL 实现代码

## 风险与未完成项

- 外部控制面当前只有系统设计口径，服务端代码仓库归属、部署方式和运维边界仍待后续收口
- 离线租约吊销、设备绑定和审计留存策略仍未冻结到可实现细节
- `rf-solver` 当前仍未覆盖 recycle、tear stream、复杂单元参数和更细的诊断输出
- 当前端到端样例已扩到 `Feed1 + Feed2 -> Mixer -> Flash Drum`、`Feed -> Heater -> Flash Drum` 与 `Feed -> Valve -> Flash Drum` 三条基线，但示例与测试覆盖面仍偏窄
- `apps/radishflow-studio` 当前虽然已有 `StudioAppFacade`、`WorkspaceRunCommand`、`WorkspaceSolveService`、`run_studio_bootstrap`、`run_panel_driver`、`ResumeWorkspace`、`WorkspaceControlState` 与最小 `RunPanelState` 回写，但还没冻结最终的 UI 运行命令绑定、结果派发与 `Hold/Active` 完整事件流
- entitlement refresh 虽已能从 bootstrap/main 的 widget 入口触发，且首版 session scheduler 已收住 snapshot 过期同步、失败退避与下一次检查时机，但真实 GUI 生命周期中的定时 tick、后台任务宿主和 notice 呈现位置仍待继续收口
- 虽然 entitlement 宿主当前已形成单一 `EntitlementSessionHostSnapshot` 消费面，但真实 GUI 定时器实例的持有、跨事件重挂与后台任务调度宿主仍未接通到具体桌面框架
- 虽然宿主当前已具备 `EntitlementSessionHostSnapshot + EntitlementSessionHostContext` 这一组复用契约，但真实桌面框架里的定时器句柄生命周期、窗口前后台事件来源与后台任务调度宿主仍未正式接线

## 下周建议

- 在已接通的客户端控制面编排之上，继续细化授权刷新后的 UI 事件流、联网失败提示与离线刷新策略
- 在已接通的 `main.rs` bootstrap 入口、`RunPanelWidgetModel` 与 `run_panel_driver` 基础上，继续把运行命令、结果派发和后续异步执行接到更完整的 UI 触发点
- 在不扩张远端服务范围的前提下，继续扩 `rf-solver` 的内建单元覆盖，并补更完整的端到端回归样例
- 待桌面端求解闭环更稳定后，再单独整理外部控制面的仓库归属、部署拓扑与最小项目骨架
