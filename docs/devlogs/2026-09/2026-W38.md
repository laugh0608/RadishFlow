# 2026-W38 Development Log

周期：2026-09-14 至 2026-09-20

## 摘要

用途：记录插件、分层许可与公共 API 访问控制的规划补充及本周后续推进。
读者：仓库维护者、评审者与后续 AI / Agent。
不包含：已交付插件运行时、公共服务或新许可条款的声明。

## 本周目标

- 补齐已确认的扩展与访问控制规划，保持基础功能优先的近期顺序。
- 后续按当前状态继续 B1-4 快捷键切片及必要的基础路径验证。

## 2026-09-14 插件与授权规划

- 开始时 `dev` 与 `origin/dev` 同步、工作区干净；按项目所有者确认的建议修改规划。
- 在 [模拟平台规划](../../architecture/simulation-platform.md#插件系统) 补充可安装插件目标、Manifest、Host API、权限、运行时信任、数值调用粒度、生命周期及缺插件数据保留。
- 在 [认证授权架构](../../architecture/auth-entitlement-architecture.md#软件许可产品授权与操作权限) 区分软件许可、产品 / 资产授权、操作权限和配额；核心 source-available、开放 SDK / 协议与独立插件许可作为规划方向。
- 新建 [公共 API 专题](../../topics/platform/public-api-and-access-control.md)，覆盖本地 / 远端身份、个人 token / 服务 API key / OAuth / 插件能力、资源权限、撤销、计算配额、幂等、审计及 M0-M3 验收。
- 同步路线图的扩展点到首个插件、访问控制、许可准备、公开 SDK 的依赖切片，以及控制面父专题、索引和当前状态。

## 关键决策

- 公共 API 不等于匿名公网接口；本地库、CLI、COM 不强制依赖云端 API key，远端请求需要凭据、主体、scope、资源及适用权益共同校验。
- 插件权限声明必须有运行时和宿主边界执行，进程内原生代码不宣称沙箱；高频数值调用不强制远端 RPC。
- 基础本地运行与受控资产租约分离；免费书面授权、无席位限制、具体组件许可证和授权模板仍待产品决策，根 `LICENSE` 未修改。
- 参考 RadishNexus 本地 `docs/licensing-strategy.md` 与 `docs/architecture/plugin-system.md`，读取日期 2026-09-14；只参考分层思路，未修改兄弟项目，也未把其文档规划写成运行时交付证据。
- 本轮仅修改文档，不改变产品代码、依赖、现有 PKCE / Bearer、资产缓存契约或 P1 / B1-4 的优先级，不触发提交、发布或部署。

## 验证记录

- `cargo test --locked --offline -p xtask`：12 项测试通过。
- `cargo run --locked --offline --quiet -p xtask -- check-repository-governance`：通过，检查 3,209 个文件，包含 Markdown 相对链接和根协作入口一致性。
- `cargo run --locked --offline --quiet -p xtask -- check-text-files`、`bash scripts/check-doc-size.sh` 与 `git diff --check`：通过，受约束文档均在体量目标内。
- 本轮仅验证文档与治理；未启动 GUI / UTM、服务或插件运行时，未执行产品全仓与真实认证测试。

## 风险与未完成项

- 插件运行时、具体开放组件许可证和使用授权政策、服务身份 / scope schema、配额数值、撤销传播与在途任务处理仍需在相应切片确认。
- 未实施或验证插件装载、真实凭据服务、远端任务和公网部署；既有 GUI、COM 与全仓测试不计为本轮证据。

## 后续建议

- 继续当前基础编辑闭环；真实扩展消费者出现时先明确 Host API 和首个插件验收，不提前建设市场或全部运行时。
- 公共 API 纳入实现时从专题 M0 开始；首次对外授权和开放 SDK 发布前分别完成许可政策与组件边界确认。

## 2026-09-14 B1-4 平台快捷键与 B2 首轮复核

### 实现与范围

- 先按所有者要求提交此前规划：`f1756a7b docs(planning): 补充插件、分层许可与公共 API 规划`；随后获准推进 B1-4。实现从干净 `dev` 开始，未创建分支或推送。
- shortcut 模型拆到同域 `shortcuts.rs`，显式区分平台 `Primary` 和物理 `Ctrl`；registry 与 Canvas 共用标签格式化。macOS 主绑定为 ⌘S / ⌘Z / ⇧⌘Z，重做保留 ⌘Y 别名；Windows / Linux 保持 Ctrl+S / Ctrl+Z / Ctrl+Y，物理 Ctrl+Tab 保留。
- 按键收集读取事件自身修饰键，保留额外 Control / Command，释放修饰键不改写已发生的组合；同一组合每帧最多派发一次。主键和别名均进入同一现有命令链，未改变工程 schema、历史事务或求解模型。
- 新建、打开、保存覆盖、关闭和删除确认状态优先于文本 / 文档焦点；避免确认期间快捷键修改背景工程。文本历史、Escape 留草稿和显式丢弃沿用原语义。
- 新增 5 项测试，覆盖三平台绑定 / 标签 / 别名 / 焦点矩阵、事件修饰键与重复事件、物理键位、文档单次修订 / 保存重开及确认隔离。原有测试改用明确的 Primary 语义，Canvas Control 保留。

### 验证记录

- `cargo test --locked --offline -p radishflow-studio shortcut`：23 项 lib、7 项 binary 测试通过，共 30 项。
- 沙盒 `CARGO_NET_OFFLINE=true ./scripts/check-repo.sh` 在既有 3 项 HTTP transport 测试处因禁止本地监听端口失败；日志 `/private/tmp/radishflow-b14-check-repo.log`，未修改这些测试或放宽验证。
- 获准真实环境离线复验后，全部 1,147 项测试通过；严格 clippy 首轮发现新增的无效 `as_ref()`，已删除并通过 `cargo clippy --locked --offline --workspace --all-targets -- -D warnings`。最终 `CARGO_NET_OFFLINE=true ./scripts/check-repo.sh` 在真实环境全部通过，包含 1,147 项测试、格式、构建、严格 clippy、治理和文本检查；日志 `/private/tmp/radishflow-b14-check-repo-final.log`。文档体量与 `git diff --check` 通过。
- `cargo build --locked --offline -p radishflow-studio` 通过。依赖和 lockfile 未改；`block 0.1.6` 既有 future-incompatibility 警告保留。

### macOS 实窗

- 事先告知后使用既有临时 `RadishFlow-B0.app` 识别外壳与 debug 二进制；独立 `/private/tmp/radishflow-b14-gui/preferences.json`、`快捷键验收.rfproj.json` 和布局副本。未安装应用或启动 UTM。CUA 首次库存读取超时后改为直接连接应用；默认启动窗口先退出，再以独立偏好启动，未编辑默认项目。
- 打开此前普通空白来源 Feed–Heater–Flash，revision 29，求解形成当前快照。名称 `Focus Draft` 改为 `B14 Heater` 草稿，⌘Z / ⇧⌘Z 只往返文本，工程和快照不变；Enter 提交到 revision 30，结果旧化。
- 项目树焦点 ⌘Z 到 revision 31、⇧⌘Z 到 32；画布焦点 ⌘Z 到 33、⌘Y 到 34。每步只增加一次 revision，名称与历史一致，旧结果不恢复为当前。快速连续注入 ⌘Y / ⌘K 时面板先打开并拦截命令，随后分步注入验证兼容键通过，不将注入时序计为独立缺陷。
- 命令面板显示 Save (⌘S)、Undo (⌘Z)、Redo (⇧⌘Z)。名称 AX 点击未获得实际编辑焦点导致粘贴超时，改用截图中的输入框坐标完成键盘验收；未将 AX 注入路径差异描述为产品编辑故障。
- 温度 350 -> 360 草稿的 ⌘Z / ⇧⌘Z 仅影响文本，Enter 提交到 revision 35。Home 新建出现未保存确认，⌘Z 不改变工程，取消后仍为 `B14 Heater` / 360 K；⌘S 保存的文件 revision 为 35，确认无背景历史提交。
- 关闭进程并以相同独立偏好重开，文件保留名称、360 K、两个端口绑定和布局位置 `(272.8, 50.2)`；rerun 已收敛，Heater Outlet 360 K、1 mol/s、3138.888 J/mol，Flash 两出口总流量 1 mol/s。仅证明演示路径的软件状态与输出一致性。
- 最终临时配置测试经“舍弃并关闭”结束，两个自行启动的进程均确认退出。原验收项目保留在临时目录，未覆盖前轮副本。Windows / Linux 原生键盘、IME 候选同帧、长按、跨窗口及全部文件确认组合仍待实窗验证。

### B2 首轮复核与下一切片

- 新建普通空白项目：未选择物性 / 组分，建模禁用；仅选 package 仍禁用；选择 Methane 后可进入建模，移除后恢复禁用。现有单包门禁与专题一致。
- 在已重开的工程中输入非法 -1 K 并按 Enter，输入保持无效、无应用入口，当前结果不变；显式丢弃恢复 360 K。此次未完整复验真实求解失败后的诊断恢复。
- 默认结果页可审阅当前流股表与单元结果，但没有直接的轻量复制 / 导出入口。代码已有 Runtime 区域按钮、`light_text_export()` 和写文件实现；原生导出选择器仅 Windows 接入，macOS / Linux 返回 `None` 并被上层当作取消。
- 将上述真实差距登记为 [B2-1 轻量结果输出候选](../../topics/results-review-diagnostics.md#b2-1轻量结果输出闭环)，范围包括默认入口、current / stale / missing、macOS 导出、平台不支持诊断及安全落盘。此次不实施该切片，不扩成完整报表或公共 API。
- 同步建模、主路径、物性、结果专题、App 架构、快速指南和当前待办；历史复核表保留原日期和当时事实，仅修复指向当前章节的链接。


## 2026-09-14 B2-1 轻量结果输出闭环

### 完成范围

- 按所有者“提交更改，继续推进下一切片”的要求，先提交 B1-4：`7fd5f929 fix(studio): 统一平台快捷键与文档重做`，继续在 `dev` 实施 B2-1，未推送或创建分支。
- 默认结果页显示复制 / 导出当前结果，Runtime 旧入口共用同一实现。输出职责从较大的 shell `app.rs` 拆到 `result_export.rs`，使用最新 host 数据，点击、选路后及确认覆盖时重新核对 snapshot ID / sequence / revision；missing / stale 禁用。
- 文本头增加 `document_revision`；macOS 复用现有 `rfd` 保存对话框。Linux 明确提示文件导出暂不支持，取消与不支持分开；扩展名归一化后才进行应用覆盖确认。
- 从 `rf-store::json` 提取共用 staged write，文本和 JSON 复用同步写入、平台替换和 Windows 备份恢复。保留独占创建的临时文件句柄至写入结束；未确认覆盖采用 hard-link 提交，拒绝后来出现的目标，不直接截断原文件。不支持 hard-link 的文件系统返回错误，Windows 双重恢复失败保留原备份信息。
- 不改依赖、lockfile、工程 schema、Core / COM 边界或保存事务；不建设完整报表和公共 API。

### 验证

- 新增 5 项 shell 测试：current / stale / missing / rerun 替换、覆盖取消及确认时重验、原生取消 / 不支持 / 写入失败、扩展名归一化与中文空格路径、重新读取正式结果及 SI 字段。现有默认工具栏测试增加输出入口断言。
- 新增 2 项存储测试：拒绝未授权覆盖并清理 staging、部分写入故障后保留原文件；既有 4 项 Windows 替换 / 恢复测试原样移入共用模块。
- `cargo test --locked --offline -p rf-store`：31 项通过；`cargo test --locked --offline -p radishflow-studio --bin radishflow-studio`：232 项通过。`cargo build --locked --offline -p radishflow-studio` 通过。
- 沙盒 `check-repo` 首次发现移动源码后的 Markdown 链接失效，修正生命周期专题链接；之后在既有 3 项 HTTP transport 测试遇到本地端口权限限制。真实环境 `CARGO_NET_OFFLINE=true ./scripts/check-repo.sh` 完整通过，1,154 项测试、严格 clippy、格式、构建、治理与文本门禁通过；日志 `/private/tmp/radishflow-b21-check-repo-real.log`。
- 文档收口：12 项 `xtask` 测试、3,215 个文件的治理检查、文本门禁、严格文档体量与 `git diff --check` 全部通过；App 架构入口压缩为专题链接，避免越过体量目标。
- `block 0.1.6` 既有未来兼容性警告保留；不以降级检查或修改网络测试掩盖沙盒限制。

### macOS 实窗与文件读回

- 事先告知后，以已有临时应用外壳启动 debug 二进制；独立目录 `/private/tmp/radishflow-b21-gui`，使用 B1-4 普通空白来源 Feed–Heater–Flash 工程和布局副本。未安装应用、改动其他项目或启动 UTM。
- 重开 revision 35，未求解时两个输出按钮禁用；运行后显示当前结果，复制反馈 ID 为 `blank-project-1789303363-rev-35-seq-1`。macOS 原生取消后显示“未写入结果文件”。
- 原生保存 `/private/tmp/radishflow-b21-gui/当前结果 验收.txt`；自动化 paste 未触发面板读取，改用 AX `setValue` 完成文件名和路径输入。文件读回包含 revision 35、360 K、101325 Pa、1 mol/s、3138.888 J/mol 以及 Review / Streams / Units / Steps / Diagnostics；导出前后工程和布局逐字节一致。
- 再选相同目标，经原生提示进入应用内具体路径确认；取消后结果文件 SHA-256 不变。将 Heater 温度从 360 K 提交为 365 K 后 revision 36，结果变 stale 且输出禁用；保存后 rerun 形成 `rev-36-seq-2`。
- 确认覆盖后读回 revision 36 / sequence 2、365 K / 3392.638 J/mol，原结果哈希变化，与结果表和模块结果一致。工程保持已保存状态；验证进程正常退出。
- 验收仅证明软件状态与演示结果输出一致，不替代独立物理准确性验证。Windows / Linux 原生结果路径、磁盘写满与恢复失败实窗未执行，故障语义由上述定向测试覆盖。

### 后续

- B2-1 已完成当前 macOS 用户闭环；下一候选 B2-2 复核运行失败诊断、对象定位、修复后重跑及当前结果恢复。
- 其余单元逐项输出和跨平台原生验证继续按实际优先级补齐；完整报表、插件运行时和公共 API 保持独立规划。


## 2026-09-14 B2-2 运行失败诊断与恢复

### 实现与验证

- 所有者要求继续下一切片，从干净 `dev` 开始，基线为 B2-1 `176c2dde`；继续既有基础功能优先级，未扩展求解规则或公共 API。
- 先核对现有失败生命周期测试，确认组分 readiness、单元参数、连接引用、重复源 / 目标、孤立流股与拓扑恢复已有覆盖。新回归首先复现恢复未请求画布定位、删除孤立流股后旧导航残留；命令、F8 和直接 widget recovery 事件统一读取实际 `RunPanelRecoveryOutcome.applied_target`，删除目标时清理旧反馈。
- 新建同域 `failure_recovery.rs`，从较大的 `app.rs` 收口运行 / 恢复反馈。默认顶部提供当前 recovery action，区分定位与可撤销修改；底部恢复动作提前并移除详细目标组内重复恢复按钮。未改诊断分类、命令 ID、恢复事务、工程 schema、依赖或 lockfile。
- 实窗发现 F5 成功后仍停留在旧面板；新增回归复现，并将原先仅命令字符串路径触发的面板切换移至正式 dispatch 出口。成功、失败、恢复与快捷键使用同一状态判断，不从日志反解析状态。
- 共新增 5 项测试：三种恢复入口、只定位不改文档、修复单次 revision / Undo / Redo 与禁用重试、删除目标清理、顶部行为标签与修复后清除、F5 成功 / 失败面板。定向失败恢复测试通过，最终 shell 237 项测试通过。
- 沙盒 `check-repo` 在既有 3 项 HTTP transport 测试因本地端口限制失败；获准真实环境离线复验后通过。增加 F5 修复后最终 `CARGO_NET_OFFLINE=true ./scripts/check-repo.sh` 全部通过：1,159 项测试、严格 clippy、格式、构建、治理与文本检查；日志 `/private/tmp/radishflow-b22-check-repo-final.log`。
- 文档收口：12 项 `xtask` 测试、3,216 个文件的治理检查、文本门禁、严格文档体量和 `git diff --check` 通过；当前状态中的 B0 / B1 历史验证归并为周志链接。
- 构建 `cargo build --locked --offline -p radishflow-studio` 通过；`block 0.1.6` 既有未来兼容性警告保留。未执行 UTM、Windows / Linux 原生窗口或 `.NET` / COM / PME。

### macOS 用户路径

- 事先告知后使用既有临时应用外壳和独立 `/private/tmp/radishflow-b22-gui/preferences.json`，复制 B2-1 普通空白来源工程和布局为 `诊断恢复验收.*`，不改前轮副本。
- revision 36 基线运行后，把 Feed 源压力从 101325 Pa 提交为 90000 Pa，revision 37；求解报 `solver.step.parameter`：Heater 出口 101325 Pa 高于入口 90000 Pa。旧结果不再作为当前结果，恢复类型为“定位问题（不修改模型）”。
- F8 和顶部 `Inspect unit parameters` 均选中 Heater；最终窗口显示 `Canvas object located`、heater-1 高亮和参数检查器。仅定位后仍为 Error / revision 37；将 Heater 出口压力修正为 90000 Pa，revision 38，重跑收敛。
- 首轮较长错误信息把底部恢复按钮挤到可见范围外，因此补了顶部同源入口；修改后的实窗确认该入口直接可见。恢复动作的详细英文标签沿用现有 presentation，中文行为提示已明确效果。
- 选择 Heater Outlet，点击“断开目标端”，revision 39；运行报 `solver.connection_validation.unbound_inlet_port`，顶部 `Inspect inlet path` 定位到 flash-1。回到同一流股点击“重连流股”，恢复 heater-1:outlet -> flash-1:inlet，revision 40；按钮运行后收敛并保存。
- 最终关闭并重开已保存 revision 40，F5 自动切换到模块结果 / 结果表。Heater Outlet 为 365 K、90000 Pa、1 mol/s、3392.638 J/mol；进入结果页复制成功，快照 ID 为 `blank-project-1789303363-rev-40-seq-1`。求解和输出一致性不替代独立物理准确性验收。
- 三次自行启动的临时窗口均已正常退出。故障参数、连接及修复过程均由 UI 提交，未通过手改工程文件制造所测失败。

### 未测与下一步

- Windows / Linux 原生 F5 / F8、极端远距离视口和所有恢复 mutation 逐项实窗仍待验证；mutation 事务 / 撤销重做由自动化覆盖，本轮实窗聚焦普通参数修正与显式重连。
- 下一优先项为 Valve 普通空白建模、参数联动、保存重跑与结果输出；随后补 Mixer / Cooler 用户路径。完整报表、插件运行时和公共 API 保持独立规划。


## 2026-09-14 B2-3 Valve 空白建模与参数联动

### 实现与验证

- 按所有者“继续推进下一步”，从干净 `dev`、B2-2 `9bcf8570` 开始，推进 Valve 普通空白用户路径，不增加同构 Home 入口或 PH Flash / Cv / 控制阀模型。
- 既有空白 Feed–Heater/Cooler/Valve–Flash 保存重开重跑测试先通过；实窗发现跨对象保留的压力草稿未随入口约束变化重验。新测试复现有效草稿在降压后仍有效、无效草稿在升压后仍无效两个方向；首轮 1 通过 / 2 失败，日志 `/private/tmp/radishflow-b23-red.log`。
- 在 `WorkspaceState::commit_inspector_document_change` 统一刷新保留的单元数值草稿，与输入更新复用同一值 / 约束计算；覆盖 Feed 参数和 stream 输入提交。保留用户原始字符，不自动提交，不额外增加修订 / 历史；缺显式参数的有效同值仍可应用。连接和文档历史的既有草稿清理行为保持。
- 新增 3 项压力草稿测试，覆盖 Valve / Heater / Cooler / Mixer、两类上游编辑、有效 / 无效切换、无效数值、显式默认值、单次提交与 Undo / Redo；`rf-ui` 117 项通过。单入口流程矩阵将已有结果导出断言扩展到 Valve / Cooler，Studio shell 237 项通过。
- 沙盒 `check-repo` 在既有 3 项 HTTP transport 监听测试遇到端口权限限制；获准真实环境离线复验后，1,162 项测试、严格 clippy、格式、构建、治理和文本门禁通过，日志 `/private/tmp/radishflow-b23-check-repo-real.log`。`block 0.1.6` 既有未来兼容警告保留；依赖 / lockfile、工程 schema 和求解模型未变。
- 文档收口：12 项 `xtask` 测试、3,217 个文件的治理检查、文本门禁、严格文档体量与 `git diff --check` 通过。

### macOS 实窗与结果

- 事先告知，使用独立 `/private/tmp/radishflow-b23-gui/preferences.json` 和既有临时应用外壳。通过普通新建项目选择二元烃 Lite、Methane / Ethane，放置 Feed、Valve、Flash Drum 并逐次接受连接与出口建议；工程不是示例副本，未手改文件制造错误。
- Feed 显式提交 310 K / 130000 Pa，组成各 0.5、流量 1 mol/s。Valve 未显式提交压力时 F5 提示补齐；150000 Pa 草稿无效、无应用按钮，丢弃后显式应用 90000 Pa。Flash 显式 300 K / 80000 Pa，revision 17 收敛。
- Feed 降为 85000 Pa，revision 18 结果旧化，运行报 `solver.step.parameter`：Valve 90000 Pa 高于入口；F8 聚焦 Valve。保留 95000 Pa 无效草稿，把 Feed 升为 120000 Pa（revision 19），首次看到上限提示更新但草稿仍无效，确认根因。
- 原生保存 `Valve 空白建模验收.rfproj.json`，未提交草稿不入文件；关闭首轮进程。更新后二次启动并重开：Valve 正式参数仍 90000 Pa；输入 95000 Pa 草稿，Feed 降为 85000 Pa（revision 20），回到 Valve 无效且不可应用；Feed 升回 120000 Pa（revision 21）后同一草稿恢复可应用，未重输字符。
- 显式应用后 revision 22，F5 收敛、保存；当前结果复制反馈 `blank-project-1789391563-rev-22-seq-1`。原生导出 `Valve 当前结果.txt`，读回文本包含同一 revision / snapshot 和 Review / Streams / Units / Steps / Diagnostics，工程仍已保存。
- Valve 产出与 Flash 消费均为 310 K、95000 Pa、1 mol/s、601.388 J/mol，组成各 0.5；Flash 液相零流量、气相 1 mol/s / 300 K / 80000 Pa。当前模型保持入口温度，焓相同不能作为已实现 PH 等焓节流的证据。
- 最后使用原生“打开项目”重载磁盘 revision 22，确认 Snapshot missing；F5 重跑收敛、Valve 参数 / 结果一致且工程已保存。两次自行启动的验证进程均正常退出。

### 后续边界

- 下一优先项 Mixer 双进料空白建模、参数 / 结果联动，随后 Cooler。Windows / Linux 原生输入、完整多入口实窗及跨平台输出仍待验证；本轮不启动 UTM 或执行 `.NET` / COM / PME。
- 插件运行时、分层授权、公共 API 和完整报表保持独立规划，未并入基础 Valve 切片。


## 2026-09-14 B2-4 Mixer 双进料建模与结果联动

### 推进范围

- 按所有者“继续推进下一步”，从干净 `dev`、B2-3 `0dd5dbb3` 开始。既有 Mixer 空白保存重跑测试先通过；本轮补双进料不同组成 / 不等流量、最低入口压力和缺入口恢复证据，未发现需改动产品逻辑的新缺陷。
- 将较大 `canvas.rs` 内既有 Mixer 建模 / 保存 / 结果回归移入 `tests/canvas/mixer.rs`，提取其正式空白建模准备供同域测试复用，不删除既有断言。
- 新增一项 shell 回归：两股 300 / 330 K、甲烷 0.5 / 0.8，流量 2:1 时得到 310 K / 3 mol/s / 甲烷 0.6；第一股改为 1 后，旧结果不可导出，保存重开 rerun 得到 315 K / 2 mol/s / 甲烷 0.65。核对组分物料平衡、Mixer 产出与 Flash 消费引用、结果表及正式文本输出，导出前后工程文件不变。
- 新增一项 `rf-ui` 回归：两入口交替成为最低压力来源，保留 110000 Pa 草稿随约束有效 / 无效变化；只升高另一入口不会解禁，最终等于最低入口压力可提交，刷新自身不额外递增 revision 或改写正式参数。复用 B2-3 实现。
- 定向 Mixer 验证通过：4 项 Studio lib、8 项 shell；压力草稿 4 项通过。未修改求解模型、依赖、lockfile、工程格式、公共 API 或插件运行时。

- 沙盒 `check-repo` 在既有 3 项 HTTP transport 本地监听测试遇到权限限制；获准真实环境离线复验后，1,164 项测试（含 shell 238、rf-ui 118）、严格 clippy、格式、构建、治理和文本门禁通过。日志 `/private/tmp/radishflow-b24-check-repo-real.log`；`block 0.1.6` 既有未来兼容警告保留。
- 文档收口：12 项 `xtask` 测试、3,218 个文件的治理检查、文本门禁、严格文档体量与 `git diff --check` 通过。

### macOS 普通用户路径

- 事先告知，使用独立 `/private/tmp/radishflow-b24-gui/preferences.json` 和既有临时应用外壳；全部建模从普通新建项目开始，没有通过作者快捷入口或手改工程文件构造流程。
- 选择二元烃 Lite、Methane / Ethane。第一股 300 K / 150000 Pa / 2 mol/s、组成各 0.5；只有一股来源时放置 Mixer，没有入口连接建议。补 Feed 2 出口后出现两条明确建议，分别连接 `inlet_a` / `inlet_b`；第二股 330 K / 120000 Pa / 1 mol/s，乙烷 0.2、甲烷 0.8 通过“全部应用”提交。
- 两入口绑定后才建议创建混合出口；压力上限取 120000 Pa。未提交 Mixer 压力时 F5 提示并定位；输入 130000 Pa 无效、不可应用，丢弃后显式提交 90000 Pa。接入 Flash，显式 300 K / 80000 Pa，运行收敛。
- 断开 Feed 2 Outlet 的目标端，F5 报 `solver.connection_validation.unbound_inlet_port`，目标为 `mixer-1:inlet_b`；F8 选中 Mixer。选择原流股点击“重连流股”，恢复 `feed-2:outlet -> mixer-1:inlet_b`，重跑收敛。
- 原生保存 `Mixer 双进料验收.rfproj.json`，revision 26；复制反馈快照 `blank-project-1789392674-rev-26-seq-2`。原生导出 `Mixer 当前结果.txt`，读回同一 ID / revision 和混合组成 0.6 / 0.4，Mixer 产出与 Flash 消费均为 310 K、90000 Pa、3 mol/s、567.615 J/mol；Flash 气相 3 mol/s、液相零流量。
- 原生“打开项目”重载磁盘 revision 26，确认 Snapshot missing；选中 Mixer 后 F5 rerun 收敛，模块结果列出两股输入和同一混合出口，工程已保存。验收进程正常退出。
- 当前出口温度按流量加权；这批软件状态与结果透传证据不等于绝热混合总焓守恒或独立物理准确性验证。

### 下一步

- 继续 Cooler 普通空白建模、温压编辑、保存重跑与结果复核。Mixer 任意 N 入口、零流量逐项实窗、复杂压降与能量模型不并入；Windows / Linux 原生、UTM 和 `.NET` / COM / PME 本轮未执行。


## 2026-09-14 日终回顾与明日事项

按所有者要求暂停继续实现，回顾本地时区 2026-09-14 的全部既有提交，对照改动代码审阅相关文档，再以单独文档提交收尾。开始时 `dev` 干净、领先 `origin/dev` 6 个提交；本轮不推送，不启动 GUI 或 UTM。

### 今日提交核对

| 提交 | 代码 / 规划变化 | 文档核对结论 |
| --- | --- | --- |
| `f1756a7b` `docs(planning): 补充插件、分层许可与公共 API 规划` | 插件生命周期与 Host API、参考 RadishNexus 的许可分层、API key / 权限 / 配额切片；无运行时改动 | 路线图、平台 / 授权架构、公共 API 专题及索引已同步；明确 Draft / 未排期，不将现有 Bearer 客户端视为已交付公共服务，不改变根 LICENSE |
| `7fd5f929` `fix(studio): 统一平台快捷键与文档重做` | Primary 与物理 Ctrl 分离，平台重做主键 / 别名同源，文本 / 文档焦点及确认状态隔离 | 建模专题与指南已覆盖 macOS ⇧⌘Z / ⌘Y、Escape 保留草稿和显式丢弃；历史缺口表保留原日期，不改写历史事实 |
| `176c2dde` `feat(studio): 完成当前结果复制与安全导出` | 默认结果入口，输出时重验 snapshot ID / sequence / revision，macOS 选择器，共用 staged write | 结果、存储专题与结果参考已对齐；补结果审阅 / 小案例指南的现行按钮名、current / stale / missing、平台支持、确认覆盖和取消 / 失败说明 |
| `9bcf8570` `fix(studio): 完善运行失败恢复与快捷键反馈` | 正式 dispatch 出口统一 F5 / F8 与按钮反馈，按实际 recovery target 定位或清理导航 | 代码只切换右侧 / 底部面板；修正 App 架构、Canvas 契约及首次运行 / 快速开始指南中“自动切顶部结果 / 运行页”的旧描述，失败面板准确写为检查器 / 消息 |
| `0dd5dbb3` `fix(studio): 随入口变化重验压力草稿` | Inspector 提交后重验保留的数值草稿，四类受约束单元共用规则；单入口结果输出矩阵扩展 | App 架构与 Valve 专题已同步；补首次运行指南的上游压力联动说明，并更新 Heater / Cooler 的共享草稿规则、自动化输出覆盖与实窗待验边界 |
| `dfcbd188` `test(studio): 完成 Mixer 双进料闭环验收` | 拆出同域 Mixer 测试，新增不等流量保存重跑 / 输出与双入口最低压力回归；无产品逻辑改动 | Mixer 专题与本周证据吻合；修正主路径概览仍把 macOS 导出及 Valve / Mixer 统称待验的旧摘要，明确下一项 Cooler |

### 明日事项：2026-09-15

- 下一优先项为 [B2-5 Cooler 空白建模与温压结果闭环](../../topics/unitops/heater-cooler.md#b2-5cooler-空白建模与温压结果闭环)，已写入 [当前状态](../../status/current.md)。
- 先核对已有自动化，再从普通空白项目完成物性 / 组分、连接、显式温压、无效草稿 / 上游联动、失败定位修复、保存重开重跑与当前结果输出。仅对发现的真实缺口补实现和回归，验收时读回工程与文本文件。
- 本轮安排未构成 Cooler 实窗通过；明日按实际影响执行验证。Windows / Linux 原生路径及远距离视口继续待验证。严格换热模型、完整报表、插件运行时、具体许可政策和公共 API 不并入此基础功能切片。

### 日终文档验证

- 本轮仅更新已有文档，未修改产品代码、测试、依赖、lockfile、工程格式或根协作约定。
- `cargo test --locked --offline -p xtask`：12 项通过；仓库治理、文本检查、严格文档体量与 `git diff --check` 通过。最终文档补齐后再次检查治理 / 文本 / 体量及 diff。
- 产品验证沿用当天 B2-4 的 macOS 全仓 1,164 项测试与严格 clippy 记录，本次未重复运行全仓或原生窗口；未把自动化结果扩大为跨平台实窗或数值准确性证明。


## 2026-09-15 B2-5 Cooler 空白建模与温压结果闭环

### 推进范围与回归

- 按所有者批准，从干净 `dev`、`83359b78` 开始。先复跑既有单入口空白保存重开 / rerun 矩阵与 4 项压力草稿回归，全部通过；未发现需要修改产品运行逻辑的新缺陷。
- 新增 `tests/canvas/cooler.rs` 温度编辑回归，复用正式空白建模准备：`0`、`-1`、`NaN`、`inf`、非数字草稿不改变 revision、正式参数或保存的 outlet 模板；286 K 改为 275 K 后旧快照不可导出，保存重开重跑重新计算焓、Cooler 产出与 Flash 消费一致，组分物料平衡与正式文本输出通过，导出不改项目文件。
- 单入口矩阵、压力草稿与新增回归均通过。沙盒全仓检查的 3 项既有 HTTP 本地监听测试因端口权限失败；获准保持 Cargo 离线后，真实环境 `check-repo` 全部通过：1,165 项测试（Studio lib 543 / shell 239，rf-ui 118）、严格 clippy、格式、构建、治理和文本门禁。日志 `/private/tmp/radishflow-b25-check-repo-real.log`，沙盒日志 `/private/tmp/radishflow-b25-check-repo.log`。
- 未修改求解模型、依赖、lockfile、工程格式、公共 API 或插件运行时。既有 `block 0.1.6` 未来 Rust 兼容性警告保留。

### macOS 普通空白项目实窗与文件读回

- 事先告知并获准启动，使用独立 `/private/tmp/radishflow-b25-gui/preferences.json` 与既有临时应用外壳。原生控制首次连接较慢，后续完成正常交互。全部建模经普通新建、物性选择与模块放置，不使用示例副本或手改工程制造错误。
- 选择二元烃 Lite、Methane / Ethane；Feed 显式 310 K / 130000 Pa，出口默认 1 mol/s、组分各 0.5。依次建立 `Feed -> Cooler -> Flash Drum` 与两个 Flash 出口。
- Cooler 未提交参数时 F5 提示出口温度缺失；`-1` K、150000 Pa 草稿无效且没有应用按钮；修正并显式提交 286 K / 90000 Pa。Flash 显式 280 K / 80000 Pa，revision 18 收敛；结果表与 Flash 消费显示同一 Cooler 出口温压、流量与焓。
- 保留 95000 Pa Cooler 草稿，Feed 降至 85000 Pa（revision 19）后自动失效，无效草稿阻止运行；Feed 升回 120000 Pa（revision 20）后原草稿恢复可提交，无需重新输入。原生保存 `Cooler 空白建模验收.rfproj.json` 并读回：正式 Cooler 参数和 outlet 模板仍为 90000 Pa，未提交草稿不入项目。
- 提交草稿后文档 Undo 恢复 90000 Pa、Redo 恢复 95000 Pa；上游再降至 85000 Pa（revision 24），无草稿的正式运行报 `solver.step.parameter`，F8 聚焦 Cooler。修正 Cooler 至 80000 Pa（revision 25）后 F5 重跑收敛，失败标记清除。
- 保存 revision 25，复制反馈快照 `blank-project-1789475524-rev-25-seq-2`；原生导出 `Cooler 当前结果.txt`，读回同一 snapshot / revision 和 Review / Streams / Units / Steps / Diagnostics。Cooler 产出与 Flash 消费均为 286 K、80000 Pa、1 mol/s、-616.612 J/mol、组分各 0.5；Flash 液相零流量、气相 1 mol/s，280 K / 80000 Pa。
- 原生打开同一磁盘项目，显示 Snapshot missing；选择 Cooler 后 F5 重跑，参数、布局和结果一致，工程保持已保存。本次启动的验证进程正常退出，窗口日志 `/private/tmp/radishflow-b25-gui/window.log`。

### 结论与后续

- B2-5 当前 T/P 调节模型的软件闭环完成；软件状态、显热结果和物料透传证据不等于独立物理准确性或严格换热能量模型验证。
- 后续按基础功能台账补 Windows / Linux 原生键盘与保存 / 输出，以及远距离视口。P1 不因单元路径通过而整体收口；B3 / P2 首个切片按剩余缺口与真实消费者确定。
- 本轮未执行 UTM、Windows / Linux 原生、`.NET` / COM / PME 或发布；严格换热模型、插件运行时、许可政策与公共 API 保持独立规划。

- 文档收口：12 项 `xtask` 测试、3,219 个文件的治理检查、文本门禁、严格文档体量与 `git diff --check` 通过；更新当前状态、Studio / 结果专题及单元专题后续衔接。

## 2026-09-15 B3-1 变量与动作描述及只读浏览

### 决策与实现

- 经项目所有者批准，从 `dev` 的 `89168e30` 推进 B3-1；Windows / Linux 原生路径累积到阶段节点集中验证，不再作为本切片前置条件，P1 未测项继续保留。
- `rf-ui::variable_browser` 借用正式文档及同一工作区快照，提供对象枚举、按身份查询、搜索和端口引用。变量使用文档 / 对象 / 分区 / 类型化字段身份，描述类型、SI 单位、来源、约束和既有写入入口；重用现有 Inspector 的单元字段与压力校验。
- 覆盖六类单元实际支持的温压参数，以及流股 T/P/F、摩尔组成和快照相结果。未指定参数不借用默认值，草稿不冒充正式输入，模板与结果明确区分；过期结果保留来源并隐藏数值。单值有效不构成全流程 readiness 或组成总和有效的承诺。
- 描述现有参数、重命名、创建、连接、断开和同步运行行为，不提供动作执行器。Studio 在工具菜单增加模态浏览窗口，搜索和端口引用可选择对象，通过既有命令定位 Inspector；模态窗口接管快捷键，不让 Delete / Undo 落入背后画布。
- 新增 5 项应用查询测试和 1 项 Studio 跨层回归：六类字段、未指定 / 非法输入、动态压力上限、模板来源、同名 / 改名身份、删除 / 跨项目 / 消失字段、current / stale / missing、动作契约、草稿隔离、保存重开及重跑。没有新增依赖、lockfile、工程格式、COM / 公共传输接口或后台任务。

### macOS 实窗与文件读回

- 使用独立 `/private/tmp/radishflow-b31-gui` 项目副本和偏好文件；副本来自已验收的 B2-5 普通空白 Cooler 工程。本轮验证浏览功能，不重复宣称从空白完成建模。
- 普通最近项目入口打开 revision 25，经“工具 → 变量浏览器”查看：Cooler 出口模板 286 K / 80000 Pa / 0 mol/s、组成未指定；运行前结果明确为缺少结果。首轮发现对话框高度随内容过度收缩，调整稳定最小高度后重启实窗复核通过。
- 运行后查询同一出口结果为 286 K / 80000 Pa / 1 mol/s、两组分各 0.5、总体摩尔焓约 -616.6125 J/mol；模板保持原始输入。搜索 `outlet temperature` 得到 3 个单元温度字段，可进入 Cooler、展开参数动作并定位正式 Inspector。
- Inspector 提交 275 K 后 revision 26，浏览器显示已提交新温度；通过 outlet 引用返回流股，旧结果温压流量、组成和焓均隐藏数值，来源明确为快照 `blank-project-1789475524-rev-25-seq-1`、修订 25、序号 1。
- 保存并重跑，浏览结果恢复为 275 K / 80000 Pa / 1 mol/s，快照 `blank-project-1789475524-rev-26-seq-2`、修订 26、序号 2；磁盘读回正式 Cooler 参数与出口模板均为 275 K，项目 schema 保持 1。本次两轮测试进程均正常退出，窗口日志为 `window.log` / `window-final.log`。

### 验证与后续

- 沙盒 `check-repo` 的 3 项既有本地 HTTP 测试因端口权限失败；真实环境离线复验的全仓测试通过，首次严格 clippy 指出一处可合并条件，修正后最终 `check-repo` 全部通过：1,171 项测试（Studio lib 543 / shell 240，rf-ui 123）、严格 clippy、格式、构建、治理及文本门禁。最终日志 `/private/tmp/radishflow-b31-check-repo-final.log`；早期日志保留同目录 `check-repo.log` / `check-repo-real.log` 前缀文件。
- B3-1 完成，P2 其余内容不视为交付。下一候选为 B3-2 统一命令写入：围绕现有字段校验、文档事务、undo / redo、错误与结果旧化形成可复用调用边界；外部适配、录制和任务取消继续按依赖分开推进。
- 未执行其他平台原生、UTM、`.NET` / COM / PME、发布或推送。既有 `block 0.1.6` 未来 Rust 兼容性警告保留。
- 文档收口：12 项 `xtask` 测试、3,225 个文件的治理检查、文本、严格文档体量与 `git diff --check` 通过；App 架构仅保留查询边界摘要，具体范围归位 Studio 专题。

## 2026-09-15 B3-2 统一变量写入与输入事务

### 范围与实现

- 经项目所有者批准，从干净 `dev` 的 `d405ea22` 继续 B3-2；其他平台原生路径仍累积到阶段节点集中验证。
- 新增 `rf-ui::variable_commands`：`AppState::write_variable` 接受稳定变量身份、必填预期修订和类型化 SI 值；覆盖现有名称、温压流量及项目组分摩尔分数。返回当前修订和实际命令，跨项目 / 旧修订、只读结果、缺失对象 / 字段、草稿冲突、类型错误与领域拒绝使用结构化错误，领域错误保留 source。
- 将单元参数 / 改名、流股单字段 / 批量、组成归一化 / 添加项的准备与提交收敛到 `WorkspaceState::commit_input_document_command`，复用原字段校验、出口模板同步、历史事务与结果旧化。先在文档副本完成校验，拒绝不产生半次提交；校验后完全相同的文档不新增修订与历史。已有低层接口、连接 / 删除路径保持各自职责。
- 非 GUI 写入不改变对象焦点，不消耗任何保留草稿；调用者先显式提交或丢弃，再以当前修订提交。浏览窗口继续只读，没有新增公共传输协议、COM、任务执行器、依赖或项目 schema。

### 验证与结论

- 新增 8 项 `rf-ui` 回归：六类单元和流股各字段的 GUI / 非 GUI 文档、命令及求解状态一致；非法值、身份 / 修订 / 字段错误与结果写入原子拒绝；草稿保留、入口压力约束、缺失项目组分赋值、相同值仍校验、当前结果保护、Undo / Redo 和批量后续字段失败完整回滚。
- 新增 1 项 Studio 生命周期回归：使用正式 Heater 工程及物性 provider，按变量身份写入 330 K，旧化原快照；通过正式保存入口写磁盘后重开，身份与输入保持一致，重新求解后 Heater 产出与 Flash 消费流股相同，运行不改磁盘项目字节。
- 定向测试通过；沙盒 `check-repo` 的 3 项既有本地 HTTP 测试因端口权限失败，经批准在真实环境保持 Cargo 离线复验全部通过：1,180 项测试（Studio lib 544 / shell 240，rf-ui 131）、严格 clippy、格式、构建、治理与文本门禁。日志 `/private/tmp/radishflow-b32-check-repo.log`、`/private/tmp/radishflow-b32-check-repo-real.log`。
- 本轮没有新增 UI 操作，使用现有 shell 建模、草稿、保存重跑与浏览回归验证共享路径，未重新启动原生窗口；未执行 Windows / Linux 原生、UTM、`.NET` / COM / PME 或发布。既有 `block 0.1.6` 未来 Rust 兼容性警告保留。
- B3-2 首个输入写入切片完成；下一候选 B3-3 明确受控创建 / 连接 / 运行的应用内动作调用链，再按依赖推进外部适配、录制与任务取消，不把本轮视为完整 P2 自动化交付。
- 文档收口：12 项 `xtask` 测试、3,228 个文件的治理检查、文本、严格文档体量与 `git diff --check` 通过；当前状态精简旧验证摘要，详细证据保留周志。

## 2026-09-15 B3-3 受控创建、连接与运行调用

### 范围与实现

- 经项目所有者批准，从干净 `dev` 的 `b228fd8d` 继续 B3-3；Windows / Linux 原生验证保持阶段集中执行。
- 新增 `radishflow-studio::modeling_actions`：类型化创建 / 连接 / 运行请求携带文档身份和预期修订，统一拒绝跨项目、旧修订、未处理字段草稿或画布编辑。返回修订及动作结果，领域拒绝保留错误 source。
- 画布创建和应用调用共享 `create_builtin_unit`，沿用六类内置单元、标准端口与会话身份分配。GUI 建议接受和应用连接共享 `commit_material_connection`，先在副本完成端点验证再提交既有文档命令、历史和修订。
- 连接执行重新枚举本地规则候选，按流股与源汇端点身份唯一匹配，不消费界面旧建议或调用者的流股创建内容。保留原受控建模范围，不将候选生成冒充完整拓扑验证。
- Run 复用正式 workspace control 与物性缓存上下文，保留同步求解、readiness、结构化阻塞 / 失败和诊断；receipt 不等于求解成功。没有新增外部传输、COM、后台任务、录制、依赖、lockfile 或项目 schema。
- 动作描述同步调用参数；实际持久化 `DocumentCommand` 不变。窗口仍只读浏览动作，GUI 焦点和交互日志由原入口管理。

### 验证与后续

- 新增 8 项定向回归全部通过：六类创建与画布文档 / 历史一致、删除后身份不复用、请求与草稿冲突完整保留状态、旧建议与重复连接拒绝、连接 Undo / Redo、多上游歧义拒绝、非法生成端点不留下流股，以及运行阻塞 / 求解失败反馈与原入口一致。
- 无窗口闭环从空白文档通过现有入口配置物性与两组分，再按统一动作创建 Feed–Heater–Flash、B3-2 写入 SI 参数、受控连接并求解；Heater 当前结果为 330 K / 90000 Pa / 1 mol/s。正式运行入口比较一致，文档与命令历史不受运行影响。
- 未新增 UI 执行操作，沿用 shell 建模、保存与运行回归；本轮未重新启动原生窗口，未执行 Windows / Linux 原生、UTM、`.NET` / COM / PME、发布或推送。
- 下一候选 B3-4 为首个无界面消费者，先明确命令、文件输入输出及错误契约，再复用项目装载、变量与动作入口验证参数化运行和结果读取。完整 P2、COM、录制及任务取消仍未交付。
- 正式验证：沙盒的 3 项既有 HTTP 测试因本地端口权限失败；真实环境离线复验全仓测试通过，首轮严格 clippy 要求将一处手写 Option 匹配改为 map。修正后最终 `check-repo` 全部通过：1,188 项测试（Studio lib 552 / shell 240，rf-ui 131）、严格 clippy、格式、构建、治理与文本门禁。最终日志 `/private/tmp/radishflow-b33-check-repo-final.log`，前两轮记录为同目录 `radishflow-b33-check-repo.log` / `radishflow-b33-check-repo-real.log`；既有 `block 0.1.6` 未来兼容性警告保留。
- 文档收口：12 项 `xtask` 测试、3,230 个文件的治理检查、文本、严格文档体量和 `git diff --check` 通过；具体接口归位 Studio 专题，App 架构保留路由，当前状态同步下一候选。

## 2026-09-15 B3-4 首个无界面参数化运行消费者

### 范围与实现

- 经项目所有者批准，从干净 `dev` 的 `8d4be4ef` 继续 B3-4。现有可执行程序新增 `--headless inspect <project>` / `--headless run <request.json>`，在 GUI 初始化前分流；不增加二进制目标、依赖或 lockfile。
- 将 bootstrap 中的项目装载转换移入 `load_project_app_state`，GUI 与 CLI 共用正式 `rf-store` 读取、校验及元数据 / 修订恢复。CLI 独立创建本次内存会话，使用显式本地缓存，不使用 GUI 的示例缓存播种、账户生命周期、偏好或 sidecar。
- CLI 请求 DTO 单独定义版本 1，严格解析已知字段与类型；按文档身份和预期修订校验输入，按顺序调用 B3-2 写入，再通过 B3-3 运行并由 B3-1 查询结果。相对路径基于请求文件目录；写入不保存磁盘，任一拒绝停止后续步骤，receipt 说明此前在本次内存完成的写入。
- 退出码与 JSON 区分成功、调用错误、阻塞和求解失败；仅在当前修订有收敛快照时读取请求结果，查询列表先全部验证再发布。保留输入 / 模板 / 求解来源、缺值状态及诊断对象 / 端口；标准输出故障返回独立非零码。
- 新增 [命令与协议参考](../../reference/headless-cli.md) 和正式 Heater 330 K 请求示例。未接入外部网络、COM、创建 / 连接脚本、录制回放、后台任务或保存覆盖；项目 schema 不变，完整 P2 未交付。

### 验证与结论

- 3 项库回归覆盖严格嵌套字段 / 类型 / 非有限值拒绝、标准输出写入与 flush 失败；8 项真实可执行进程回归覆盖 inspect→复制变量身份→修改→运行→读回、参数与身份冲突、原文件字节保护、缓存缺失 / 资产缺失、readiness 阻塞、求解诊断与非法查询。定向测试全部通过，日志 `/private/tmp/radishflow-b34-headless-tests.log`。
- 成功案例由正式请求示例产生 330 K 当前结果，模板流量保持 0 mol/s、求解流量为 5 mol/s；与原 workspace control 入口结果一致，两次独立进程响应一致。中文 / 空格路径、请求相对路径和无桌面偏好 / 布局写入均有进程证据。
- 首轮测试修正了对现有失败语义的错误预期：未连接入口属于求解连接校验失败，组成未归一化属于 readiness 阻塞，Valve 参数诊断沿用 `solver.step.parameter`。并行测试发现仅用系统时间命名临时目录可能碰撞，加入测试进程内唯一序号和独占建目录；生产路径未添加兼容兜底。
- 本轮未启动原生窗口；未执行 Windows / Linux 原生、UTM、`.NET` / COM / PME、安装、发布或推送。CLI 仍链接现有桌面依赖，不把无窗口执行等同于无图形依赖产物。
- 下一候选 B3-5 将现有受控创建 / 连接接入无界面消费者，先明确身份引用和逐步错误契约；COM、录制及任务取消继续按依赖切片。
- 正式基线：沙盒仍仅有 3 项既有 HTTP 测试因本地端口权限失败；真实环境保持 Cargo 离线复验 `check-repo` 全部通过，1,199 项测试（Studio lib 555 / shell 240 / CLI 进程 8，rf-ui 131）及严格 clippy、格式、构建、治理与文本门禁通过。日志 `/private/tmp/radishflow-b34-check-repo.log` / `/private/tmp/radishflow-b34-check-repo-real.log`；既有 `block 0.1.6` 未来兼容性警告保留。
- 文档收口：12 项 `xtask` 测试、3,236 个文件的治理检查、文本、严格文档体量与 `git diff --check` 通过；CLI 命令归位 reference，专题保留实施边界，状态与路线图同步后续候选。

## 2026-09-15 日终回顾与明日事项

按项目所有者要求结束今日实现，核对本地时区 2026-09-15 的全部 5 个既有提交（所有本地引用与 `dev` 记录一致），逐项对照代码及相关文档。开始时 `dev` 干净、领先 `origin/dev` 5 个提交；本轮只做文档收尾，不推送。

### 今日提交核对

| 提交 | 代码变化 | 文档核对结论 |
| --- | --- | --- |
| `89168e30` `test(studio): 完成 Cooler 温压闭环验收` | 新增非法温度、旧结果拒绝、保存重开重跑和结果透传回归；无求解模型改动 | Heater / Cooler 专题及实窗记录吻合；主路径概览补齐建模 / 参数验收进度，保留逐单元删除 / 重命名及跨平台未测边界 |
| `d405ea22` `feat(studio): 增加变量与动作只读浏览器` | 应用变量枚举、身份查询、搜索、来源区分，工具菜单只读模态浏览及检查器定位 | 快速开始、Studio 和结果专题补齐可达入口；缺值、旧化隐藏数值和模板 / 结果区分保持一致 |
| `b228fd8d` `feat(ui): 统一变量写入与输入事务` | 结构化变量写入和共享输入事务，修订 / 草稿校验、相同有效值无变更处理 | App 架构原“每次成功提交都递增”不再准确，修正为实际文档变更才推进历史 / 修订；写入与运行派发分开，指南和建模专题同步 |
| `8d4be4ef` `feat(studio): 统一受控建模动作调用` | 六类创建、实时连接候选复验、共享事务及正式运行反馈 | 应用层与 `rf-ui` 职责摘要补齐；连接候选不是完整拓扑验证，receipt 不等于收敛，B3-3 专题无需改动 |
| `5571a134` `feat(studio): 接入无界面参数化运行` | CLI inspect / run、独立 JSON DTO、共享项目装载与真实进程验证 | 生命周期专题补齐 GUI / CLI 共用装载及不保存边界；快速开始增加接口路由。CLI 参考、请求示例、退出码、路径规则与实现一致，保留原文 |

### 明日事项：2026-09-16

- 优先候选为 B3-5：将现有受控创建 / 候选连接接入无界面消费者；已写入 [当前状态](../../status/current.md#下一步)。
- 先明确请求范围、创建返回身份的后续引用、逐步失败反馈与兼容边界，再实现工程结构修改 → 参数写入 → 运行 → 结果读取。复用现有身份分配、实时候选、修订检查和文档事务，不另建任意反射写入通道。
- 今晚仅登记明日建议，不实现 B3-5，也不宣称已经验收。Windows / Linux 原生继续累积到阶段节点；COM、录制、任务 / 取消、插件与公共服务仍按依赖独立切片。

### 日终文档验证

- 本轮仅修改已有文档，未改产品代码、测试、依赖、lockfile、项目格式或根协作约定。快速开始与 App 架构精简重复文字，保留各自篇幅门禁。
- 产品验证沿用今日 B3-4 最终 macOS 离线 `check-repo` 的 1,199 项测试与严格 clippy；本次不重复全仓、原生窗口、UTM 或 `.NET` / COM / PME 验证。
- 文档验证通过：12 项 `xtask` 测试、3,236 个文件的仓库治理、文本格式、严格篇幅与 `git diff --check`；提交范围仅含 9 篇既有文档。

## 2026-09-16 单位、建模辅助、设备工程与 UI 规划整理

### 范围与决策

- 项目所有者确认上一轮方案，并要求贯通总体规划、一级 / 二级专题和当前开发顺序，同时重新考虑原 `.pen` 的功能分区。开始时 `dev` 与 `origin/dev` 同步、工作区干净。
- 新增单位系统、规格分析与建模辅助、设备设计与校核三个一级专题，以及气液分离器尺寸 / 校核、换热器设计 / 校核两个子专题；均标明 Draft / 规划已确认，不冒充实现。
- 单位定义 / 变量元数据 / 单位集 / 统一转换分别归责；核心 SI 保持，输入单位与显示单位分开，仿射与需上下文转换区别处理。显示偏好不引起物理输入修订或结果过期，存储与协议在实施切片明确。
- 近期智能化为确定性规则和工作流；readiness、规格检查、结构自由度与数值诊断分层。未来 Agent 消费统一元数据、命令、诊断和任务接口，当前不引入大模型、聊天入口或空 AI 框架。
- 设备工程可独立运行或关联流程，工艺计算、初步尺寸、设计、校核分别定义；候选与采纳、源工况失效和动态复用有明确边界。不等待完整动态，物性及工程准确性按方法建立独立依据。
- 路线图保留 B3-5 为下一实现切片，随后单位基础与规格 / 推荐，再按依赖进入首个可信设备任务；完整自由度、工作流与 Agent 分别随能力推进。
- UI 计划与主稿 brief 改为 R0 信息架构、R1 Pencil 代表交互、R2 分切片实现。旧主稿保留历史范围，新功能分区待评审；旧固定八导航、设备排除和侧栏限制不再约束新任务。视觉风格与状态 / 命令同源继续遵守。
- 同步根 README、文档与专题索引、平台 / App / 当前架构、单位 / CLI 参考、热力学限制及现有单元 / 流股 / 结果 / 存储 / 物性专题；历史 MVP 和过去验收保留，避免把新目标倒写为已交付能力。

### 验证与未完成项

- 本轮仅变更 Markdown；未改产品代码、依赖、lockfile、项目 schema、根协作文件和 `.pen` 本体，未执行 GUI / UTM / COM / PME / 打包发布。
- 新信息架构尚未经过画板评审，未通过 Pencil 读取、重绘或视觉验收；后续实际重设计须通过 Pencil MCP 核对旧稿并更新对应画板。
- 文档验证通过：12 项 `xtask` 测试；仓库治理检查通过（3,241 个文件，含 Markdown 路径链接与根协作文件同步）；文本格式、严格篇幅和 `git diff --check` 通过。另核对 32 篇变更文档中的 63 个本地标题锚点，全部可解析，确认没有非 Markdown 变更。篇幅脚本为非可执行文件（Git mode 100644），使用 `bash scripts/check-doc-size.sh --fail-on-exceeded` 正常通过。
- 下一步保持 B3-5；单位 U1 和 UI R0 可先细化设计，新增接口 / 存储 / 依赖按切片审定。本轮不提交或推送。

## 2026-09-16 工程工作流建议归位

### 范围与决策

- 在项目所有者要求将七项建议加入文档后继续整理；开始于 `dev` 的 `547a89fc`，工作区干净，领先远端 1 个提交。该提交已完成上一轮 32 篇规划文档的本地提交，未推送。
- 新增一级 Draft 专题“工程设计基础、工况与数据来源”，拥有共同条件、显式覆盖、来源 / 假设、不确定度语义、工况矩阵、工程位号和复现依据；不复制到物性、存储或设备各自成为另一份真相。
- 设计规格反算归建模求解，区别于规格检查和优化；限定目标、可调变量、范围、试算隔离及候选采纳，未找到解与数值失败不冒充无可行解证明。
- 设备大专题及分离器 / 换热器子专题补齐同几何多工况、控制条件、候选权衡、压力与公用工程；失败、未运行和不支持工况不得在汇总中隐去。
- 结果专题补分层工程检查、轻量流股 / 设备计算表、假设清单、差异及复现记录；物性与设备验证区分算法、实验、供应商、现场和跨软件证据，记录来源 / 许可与误差范围。
- 平台规划、路线图、索引、状态、单位 / 物性 / 生命周期和 Studio / UI 计划同步路由。优先 G0 工程基础、工况矩阵与输入来源设计，G1 / G2 按依赖接入；B3-5→单位→规格 / 推荐→首个可信设备任务的代码顺序保持。
- UI 增加项目 / 基础 / 工况 / 设备 / 结果的状态与代表路径；不改 `.pen`，不声称已完成新画板或功能。

### 验证与边界

- 本轮只修改 Markdown，不改代码、依赖、lockfile、项目格式、根协作文件或设计文件；未运行 GUI / COM / PME 或发布。
- 文档验证通过：12 项 `xtask` 测试；仓库治理检查通过（3,242 个文件，含 Markdown 路径链接）；文本格式、严格篇幅和 `git diff --check` 通过。另核对 20 篇变更文档中的 69 个本地标题锚点，全部可解析，确认没有非 Markdown 变更。
- 本轮新增规划尚未实现，具体接口 / schema 和专业方法在对应切片审定；未提交或推送本轮文档变更。
