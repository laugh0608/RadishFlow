# 2026-W36 Development Log

周期：2026-08-31 至 2026-09-06

## 摘要

用途：记录 2026-09-06 全面只读审阅及随后获准的文档完善。
读者：仓库维护者、审查者与后续 AI / Agent。
不包含：业务功能恢复、代码修复完成声明、Windows / GUI 复验或对外发布承诺。

- 审阅基于本地 `dev`、`d22e7a55`，开始时工作区干净，领先本地 `origin/dev` 引用 1 个提交；未联网核对远端状态。
- 完整 Rust 仓库基线通过；文档修改集中于能力解释、模型限制、实际架构、历史规划和风险归属。
- 当前停更边界保持不变，原生句柄、Windows 保存、工具链和数值问题仅记录事实或待评估建议。

## 本周目标

- 对照实现审阅现状、结构、规划与验证质量。
- 按项目所有者要求把有依据的结论归入已有文档，减少入口中的重复历史流水。

## 完成情况

- 精简架构总览、MVP 范围和路线图，保留实现边界、里程碑证据与历史材料引用。
- 更新热力学模型，明确 Antoine 形式与单位、Mixer / Valve 近似、常热容显热、演示样例身份和独立数值验证缺口；引用 NIST 与 IDAES 说明物理解释边界。
- 将相同解释通过链接接到根 README、使用指南、结果参考、物性专题和样例 README；未更改样例数据或测试容差。
- 标记功能专题的历史组织状态，区分目标设计、实际实现、已通过的历史验收与未排期建议。
- 将 `rf-thermo` 缓存依赖、`rf-canvas` 占位和 Studio 同步求解归入架构说明；修正字体资源描述。
- 将 native engine 生命周期和 Windows staged write 双重失败风险归入对应专题，注明静态发现、未复现、未修复。
- 对齐版本文档中的 `Candidate Quality` 聚合门禁说明；记录现有内部 CI artifact 与公开交付的区别，未修改 workflow 或 ruleset。
- 更新当前状态中的工具链差异与验证摘要，并同步文档和周志索引。

## 关键决策

- 本次授权为文档完善，不修改 Rust / C#、Cargo 声明、lockfile、样例结果、测试、CI 或根协作规则。
- 数值回归、跨层一致性、用户路径 smoke 与独立物理准确性是不同证据，不用测试数量代替工程适用性说明。
- 后续投入顺序仅作决策参考，不以更新专题恢复开发；新的业务实现仍需项目所有者明确调整停更边界。
- 静态风险与已复现问题分开记录；本轮不创建一次性审阅报告或新的并行真相源。

## 验证记录

### 修改文档前的全面审阅

- 环境：macOS，Rust `1.96.0 (ac68faa20 2026-05-25)`，Cargo `1.96.0 (30a34c682 2026-05-25)`。
- `CARGO_NET_OFFLINE=true ./scripts/check-repo.sh`：沙盒内治理、文本、格式与编译通过，Studio 3 项本地 HTTP 测试因监听端口 `PermissionDenied` 失败。
- 同一命令在获准的真实环境保持 Cargo 离线复验：通过；累计 1,122 项 Rust 测试通过，workspace fmt / check / test / clippy 与治理基线通过。
- 仍有 `block 0.1.6` 的 future-incompatibility 警告，依赖路径来自 macOS `eframe / wgpu / metal`；未执行升级。
- 核对当前锁定依赖的已缓存包元数据：`time 0.3.47`、`image 0.25.10` 都声明 Rust `1.88.0`，与 workspace `rust-version = "1.86"` 不一致；没有安装旧工具链或测定完整最低支持版本。
- 未启动 GUI，未执行 Windows `.NET` / COM / PME、保存故障注入、专项安全审计或性能测量。

### 文档修改后的定向检查

- `cargo test --offline -p xtask`：12 项测试通过。
- `CARGO_NET_OFFLINE=true cargo run --quiet -p xtask -- check-repository-governance`：通过，检查 3,190 个文件，覆盖相对链接、根协作文件同步和治理配置契约。
- 篇幅检查初次发现 App 架构文档略超 30k 字符，已精简重复的历史建议；使用 `bash scripts/check-doc-size.sh --include-advisory` 复核。复核后受约束文档均未超限，仅保留已有历史周志的 advisory 提示。
- `CARGO_NET_OFFLINE=true cargo run --quiet -p xtask -- check-text-files`、`git diff --check` 与根协作文件一致性检查通过；本轮涉及的 11 个本地页内链接也已定向核对。
- 架构总览、MVP 范围和路线图三个入口合计从约 6.2 万字符收敛至约 1.1 万字符；历史过程继续通过原有周志和路线图分册查询。
- 所有工作区改动均为 Markdown；业务测试未因纯文档变更重复执行。

## 风险与未完成项

- native 句柄失效 / 并发调用与 Windows 保存恢复风险尚未修复，见对应专题。
- 工具链声明与锁定依赖不一致尚未修改；完整 MSRV、依赖更新策略与浮动 stable 的可复现性仍待评估。
- 真实物性来源、适用范围、焓参考态、能量闭环和独立基准尚未补齐。
- 当前文档不将未来兼容警告、静态风险或历史 smoke 重新标记为已修复或本次运行态通过。

## 下周建议

- 继续按当前维护范围按需维护文档与工具链，不启动产品功能排期。
- 若项目所有者授权工具链维护，先确认声明与锁定依赖的支持范围，再决定是否调整版本或检查入口。
