# 2026-W37 Development Log

周期：2026-09-07 至 2026-09-13

## 摘要

用途：记录账户专题、工具链与可靠性验证、恢复开发、长期模拟平台规划和 B0 基础能力复核。
读者：仓库维护者、评审者与后续 AI / Agent。
不包含：身份服务实现、跨项目修改或真实登录联调完成声明。

## 本周目标

- 参考 Radish 的统一身份体系和 RadishMind 的本地账户 / 联合登录做法，形成 RadishFlow 的未排期专题。
- 对齐 Rust 支持声明、固定工具链、锁定依赖和 CI 验证职责。

## 完成情况

- 2026-09-10 新建 [账户、登录与 Radish 联合身份专题](../../topics/platform/account-and-federated-login.md)，记录参考快照、现有代码入口、候选方案、用户路径、分阶段退出标准与验收矩阵。
- 区分 Radish 外部认证、候选产品账户映射、业务授权、桌面凭据与离线资产租约；明确旧主体字段和缓存版本的兼容评估要求。
- 同步专题与文档索引、认证架构、控制面父专题和当前状态路由；未来规划与停更前历史专题分开列示。
- 2026-09-12 完整核对 469 个包的锁定依赖元数据；最高已声明 Rust 要求为 1.88.0，127 个包未声明版本，不能据此推导完整 workspace 的实际最低编译器版本。
- 将 workspace 支持下限设为 Rust 1.96，日常工具链与两个正式 workflow 固定到已具备验证记录的 1.96.0；`Cargo.lock` 和依赖版本保持不变。
- 为正式 Rust 检查及 CI 的 native / staging 构建入口补上 `--locked`；新增仅手动触发的三平台 `Rust Compatibility`，显式用 `RUSTUP_TOOLCHAIN=stable` 检查待评估版本，不参与 `Candidate Quality`。

## 关键决策

- 2026-09-10 的账户专题授权当时仅为未来规划；2026-09-12 所有者已取消项目停更限制。账户专题仍需完成 M0 范围与架构决策，现行认证架构在该决策前保持有效。
- 候选首版复用 Radish.Auth，并评估 RadishMind 式产品账户映射与独立业务授权；产品账户、独立密码登录和产品 session 的取舍须在 M0 确认，不将草案建议写成正式决策。
- 不迁移兄弟项目源码、身份库或技术栈；参考材料来自本地文件，未核验远端状态，未修改兄弟项目。
- 工具链维护选择有验证依据的支持下限，不为历史 Rust 1.86 声明新增兼容维护线；1.88 仅是依赖元数据线索，未承诺可构建。
- 固定编译器与锁定 Rust 依赖组合，不承诺 runner、系统库、Actions、`.NET` SDK 或产物逐字节可复现；此次不触发远端 CI、打包或发布。

## 验证记录

### 2026-09-10 账户专题规划

- `cargo test --offline -p xtask`：12 项测试通过。
- `cargo run --offline --quiet -p xtask -- check-repository-governance`：通过，检查 3,192 个文件，覆盖 Markdown 相对链接、根协作入口同步和仓库治理契约。
- `cargo run --offline --quiet -p xtask -- check-text-files`、`bash scripts/check-doc-size.sh` 与 `git diff --check`：通过；受约束文档均在体量目标内。
- 本轮仅修改 Markdown，未执行产品 GUI、真实 OIDC、Windows / COM 或完整业务测试。

### 2026-09-12 工具链维护

- `cargo metadata --locked --format-version 1`：成功获取完整依赖图；离线首轮缺少跨平台包，获准联网下载已锁定版本后完成，未更新锁文件。
- `cargo test --offline --locked -p xtask`：12 项通过。
- 沙盒 `check-repo` 在 3 项绑定本机回环端口的测试处遇到 `PermissionDenied`；获准在真实环境保持 Cargo 离线复验后，1,122 项测试通过。
- 提高 `rust-version` 后，clippy 激活 33 处 `collapsible_if` 建议；按建议合并嵌套条件，保持既有分支行为，不增加 lint 豁免。
- 修正并格式化后，真实环境 `CARGO_NET_OFFLINE=true ./scripts/check-repo.sh` 完整通过：仓库治理检查 3,193 个文件，文本、格式、workspace check、1,122 项测试及 `clippy --locked --workspace --all-targets -- -D warnings` 全部通过。
- 三个 workflow 的 YAML 解析与定向结构断言通过：12 处正式 setup 均为 1.96.0，与最低版本声明一致；兼容性检查仅手动触发、显式覆盖工具链且覆盖三平台；原有 workflow 触发条件和六组件聚合依赖保持一致。未使用远端 Actions 运行结果替代本次静态检查。
- `bash -n` 检查改动的 shell 入口通过；文档体量、治理与文本复核、`git diff --check` 通过，`Cargo.lock` 与 `HEAD` 内容一致。

### 2026-09-12 UTM 跨平台复验

- 按项目所有者要求串行使用 UTM：先验证 Debian，再关闭 Debian 并启动 Windows；任一时刻最多运行一台虚拟机。
- 两端均从同一个 Git bundle 检出提交 `a5f8d966a19d1c5d89e16ddc750f777cf0558a3d`；传输后校验 bundle SHA-256，锁文件 SHA-256 为 `2af0b0c404f2b96dd6be6d2309855ff71902a61b57a4448bc2d33861d13db6e4`。
- Debian 13.6 ARM64、Rust / Cargo 1.96.0（`aarch64-unknown-linux-gnu`）：`./scripts/check-repo.sh` 完整通过，包含 1,120 项测试与严格 clippy；验证后工作区干净。与 macOS 的两项数量差异来自 macOS 专属窗口菜单和 Metal 配置测试。
- Windows 11 Pro ARM64（10.0.26200.9168）、Rust / Cargo 1.96.0（`aarch64-pc-windows-msvc`）：在已登录用户会话中以 `CARGO_NET_OFFLINE=true` 执行 `powershell.exe -NoProfile -ExecutionPolicy RemoteSigned -File scripts\check-repo.ps1`，治理、文本、格式、workspace check、1,120 项测试与严格 clippy 全部通过；退出码为 0，工作区干净，锁文件校验一致。执行策略仅对该进程生效。
- Windows 初轮由 QEMU guest agent 的 SYSTEM 会话启动，构建在 `ring 0.17.14` 处因缺少 ARM64 Clang 失败；通过既有 Visual Studio 2022 Build Tools 安装器补装官方 `Microsoft.VisualStudio.Component.VC.Llvm.Clang`，安装退出码为 0，最终使用 ARM64 Clang 19.1.5 复验通过。没有更改项目代码或依赖；安装组件保留在验证虚拟机中。
- Windows 使用虚拟机现有 Windows PowerShell 5.1，未验证 CI 的 PowerShell 7 或 x64 runner；本轮只补充 ARM64 本地仓库级证据，不替代远端 CI、浮动 `stable`、GUI 或 `.NET` / COM / PME 验证。
- 完整日志保存在本机 `/private/tmp/radishflow-utm-a5f8d966/linux-check-repo.log` 与 `/private/tmp/radishflow-utm-a5f8d966/windows-check-repo.log`，属于临时证据文件，可能被系统清理；关键结论和环境已在本文记录。
- 收尾确认全部 UTM 虚拟机均为 `stopped`，临时文件传输服务已关闭，未留下临时计划任务；虚拟机保留此次验证 checkout 与构建缓存。记录回写后，12 项 `xtask` 测试、仓库治理、文本、文档体量和 `git diff --check` 通过。

### 2026-09-12 恢复正常开发与迭代

- 项目所有者明确取消停止更新的相关要求，项目恢复业务功能开发与正常迭代；此前只允许外围维护、拒绝普通产品问题和外部业务 PR 的限制失效。
- 同步根 README、当前状态、路线图、专题状态、架构与 MVP 入口、贡献与安全说明；既有实现、数值限制、许可证和发布验收仍按各自真相源管理。
- 普通 Issue 配置恢复开放，PR 模板恢复功能与修复贡献流程；`xtask` 治理契约同步检查当前开发范围和开放的普通 Issue 配置，不再强制停更文案。
- W34 / W36 保留当时事实，并显式注明旧决定已被替代。账户与控制面仍按 Draft / Backlog 前置决策推进，不把恢复开发写成尚未实现能力的交付记录。
- 验证：12 项 `xtask` 测试、仓库治理（3,193 个文件）、文本、Rust 格式、文档体量与 `git diff --check` 通过；本轮未修改产品实现，未启动虚拟机或重跑 GUI / PME 验证。

### 2026-09-12 第一轮可靠性修复

- 按项目所有者授权处理审阅发现：Flash 非有限流量、Windows 保存双重失败恢复信息、公开 Adapter 的 native 生命周期。
- Flash 拒绝 NaN、正负无穷与负值，保留零值和正常流量；两项参数化回归通过。
- 保存恢复提供结构化 `FileRecoveryContext` 与可诊断消息；四个故障注入场景通过。全 crate 测试发现测试目录时间戳碰撞，已用测试内序号隔离目录并复验通过。
- Adapter 使用实例锁协调调用与 Dispose，P/Invoke 接收 SafeHandle；四个生命周期 / 并发场景接入原有 Adapter smoke，macOS 真实 native smoke 通过。
- macOS `.NET` 初轮沙盒构建无输出阻塞，结束该进程后在真实环境禁用构建服务，使用 `--no-restore -p:EnableComHosting=false` 编译通过（0 warning / error）；该参数仅用于本机跨平台 Adapter 验证，不替代 Windows COM 构建。
- macOS 完整 `CARGO_NET_OFFLINE=true ./scripts/check-repo.sh` 通过：1,128 项 Rust 测试、治理、文本、格式、workspace check 与严格 clippy 全部通过；测试代码排版收尾后再次构建并运行 Adapter smoke，通过。
- Windows 11 ARM64（10.0.26200.9168）、Rust 1.96.0、Clang 19.1.5：以 QEMU guest agent 的 SYSTEM 会话在 Windows PowerShell 5.1 中离线执行 `scripts/check-repo.ps1`，1,126 项 Rust 测试及严格 clippy 全部通过，退出码 0；包含 Windows 保存正式分支和四项故障注入。随后 `cargo build --locked -p rf-ffi` 通过。
- 虚拟机未安装 .NET SDK；从 Microsoft 官方下载 `.NET SDK 10.0.300 win-arm64` ZIP，按官方发布元数据校验 SHA-512 后解压到独立 `rf-validation-dotnet/10.0.300` 验证目录；仅为验证进程设置 PATH / DOTNET_ROOT，未变更系统 PATH、项目依赖声明或 `Cargo.lock`。
- Windows 使用 `check-dotnet-capeopen.ps1 -SkipNativeBuild -SkipSmoke` 完成原生库以外的正式构建和 35 项 contract，再单独调用 `smoke-test.ps1 -SkipBuild` 完成默认 Adapter smoke（含新增 4 个生命周期场景）；这是为了使用现有 PowerShell 5.1，避免脚本嵌套调用未安装的 `pwsh`。解决方案启用原有 COM hosting 构建，0 warning / error，contract 与 smoke 退出码均为 0；未把默认 Adapter smoke 记为独立 UnitOp smoke。
- 验证源以 `a5f8d966` guest checkout 为基础，覆盖主机 `c57c090f` 及本轮工作区改动，共校验 52 个传输文件的 SHA-256；Rust 检查后和 .NET 检查后均通过清单校验，生命周期测试仅排版调整后已重新传输并更新对应摘要。最终文档结果在主机另行回写并执行治理检查。
- 日志：主机 `/private/tmp/radishflow-reliability-check-repo.log`、`/private/tmp/radishflow-lifetime-build.log`、`/private/tmp/radishflow-lifetime-smoke.log`；Windows 取回到 `/private/tmp/radishflow-reliability-vm/windows.log`、`dotnet-setup.log` 与 `dotnet-check.log`。这些是可清理的临时证据，正式结论保存在本文。
- 本轮始终只运行一台 UTM 虚拟机；结束后正常关闭 Windows，确认全部虚拟机为 `stopped`。SDK、验证 checkout 与缓存保留在虚拟机内；未执行 COM 注册、PME / Studio GUI、独立 UnitOp smoke、打包或发布。

### 2026-09-13 基础功能优先与长期平台规划

- 项目所有者授权调整开发顺序：先完善基础功能与使用闭环，再集中深化算法和数值能力；单位、输入、状态、保存及现有模型的基本正确性持续验证。
- 新增 [模拟平台长期规划](../../architecture/simulation-platform.md)，定义 17 个领域子系统、弱耦合的数据 / 状态 / 计算边界、可独立调用能力和统一对外 API 目标；补充油品表征、反应与控制、设备校核、工程研究、报告与可追溯性。
- 稳态、动态、瞬态以及序贯模块、联立模块、联立方程纳入长期方向。明确物理问题与求解策略独立、动态与瞬态概念重叠、分布参数模型有额外离散与验证要求；不以给稳态模型增加时间循环代替动态实现。
- 查阅 Aspen Plus、HYSYS Dynamics、AVEVA PRO/II、KBC Petro-SIM / Reactor Suite、SLB OLGA 官方资料，以及联立模块论文摘要、SUNDIALS 与 FMI 文档；参考与设计推论在长期规划中分别标明。未复制商业实现、数据或引入任何依赖。
- 当前状态与路线图改为 P1 基础功能、P2 运行与扩展基础，随后按依赖推进稳态数值、多策略、动态、瞬态和工程生态；允许满足前置条件的小切片提前验证，不要求所有能力串行做完。
- Studio 专题定义 B0-B3 基础功能闭环，首个候选为流程图基础编辑与连接管理。代码确认已有连接、断开、删除等文档命令；本轮未执行 B0 的 GUI 复核，不把现有命令记为新交付，也不把未验证项当成缺陷。
- 同步 README、AGENTS / CLAUDE、架构、MVP、热力学、专题与索引；长期定位变化只在根协作文件更新原有定位一条，详细能力留在专题。
- 本轮仅调整 Markdown 规划；产品代码、运行时、API、项目格式、依赖与 lockfile 未变更，未启动 UTM、GUI、COM 或发布操作。12 项 `xtask` 测试、仓库治理（3,197 个文件）、文本、文档体量与 `git diff --check` 通过，AGENTS / CLAUDE 保持全文一致。

### 2026-09-13 求解层级、变量树与脚本补充

- 按项目所有者补充，将递归分块的序贯求解列为现有求解器的明确演进方向：组合块、原子求解块、局部循环收敛与执行依赖分开定义；记录进料 / 加热器 / 闪蒸罐 / 塔示意及原子性相对于执行层级的含义。文献中的 Simultaneous Modular 仍单独保留，避免混用名称。
- 统一 API 从高层领域服务进一步明确为工程对象 / 变量 / 动作树，补齐身份、元数据、读写条件、自由度状态、动作发现、事务、结果来源和 Windows COM Automation；COM 自动化与 CAPE-OPEN PMC 契约分开，Rust 继续不承担 COM。
- 增加语义操作录制、可编辑 / 参数化脚本、重复工况回放；区分文档 Undo / Redo、自动化动作和动态检查点，现有命令历史不被记为已完成录制器。
- 在首轮 17 个领域基础上独立列出间歇 / 半间歇流程与操作录制，共 19 个领域；补齐配方阶段、仿真时间、事件切换和批次收支，并区分多案例批量计算。
- 扩展 UniSim Design、CHEMCAD、ProMax、gPROMS、Symmetry、COFE、DWSIM 与 APC 参考。gPROMS 作为方程 / 自定义模型 / 动态 / 优化重点；APC 暂按先进过程控制领域理解。查阅官方资料及大学站点托管的 Aspen Plus 历史原厂指南，文档只据其证明相关机制，不推导所有版本功能一致。
- P2 按变量发现、命令写入 / 外部适配、录制回放逐步交付；P3 明确递归分块与循环块，P5 包含间歇配方，APC 离线研究归入后续工程能力。P1 基础功能优先的安排保持不变。
- 本轮继续只更新规划和文档，没有实现求解树、变量浏览器、COM 服务、脚本运行时或间歇模型，也没有启动虚拟机或修改注册信息。12 项 `xtask` 测试、仓库治理（3,197 个文件）、文本、文档体量强制检查与 `git diff --check` 通过，AGENTS / CLAUDE 保持全文一致。

### 2026-09-13 B0 基础能力复核

- 启动检查：`dev` 与 `origin/dev` 同步，工作区干净，HEAD 为上一轮规划提交 `15ac7b97`。先读取当前状态、Studio 主路径和模拟平台规划，再按需读取建模与生命周期专题；本轮只更新文档，不实现 B1。
- 代码复核沿 `rf-ui` command / state mutation、Studio driver / GUI command registry / shell 和真实文件读写展开。确认流股断开、单端断开、唯一端点重连和删除已有实际消费；`DeleteUnit` / `RenameUnit` 只有类型及测试标签，没有对应用户可执行动作。未把历史容器测试误当单元删除测试。
- 真实环境：macOS ARM64、Rust / Cargo 1.96.0、eframe / wgpu Metal。已先告知桌面启动；裸二进制可启动但 CUA 无法识别，因此在 `/private/tmp/RadishFlow-B0.app` 建立临时 Info.plist 与指向同一 debug 二进制的符号链接用于窗口识别，不是产品打包交付。启动使用独立 `/private/tmp/radishflow-b0-preferences.json`；未安装应用、修改依赖、注册 COM 或启动 UTM。

| 步骤 | 操作与期望 | 实际观察 |
| --- | --- | --- |
| 1. 普通空白 | Home → 新建项目，物性未配置时禁止流程图；选择内置二元烃包、Methane、Ethane | 从 0 单元 / 0 流股开始；配置后允许建模，没有使用小案例作者入口 |
| 2. 创建与连接 | Palette 放置 Feed，接受创建 source stream；放置 Flash，接受 inlet 连接和 liquid / vapor 流股创建 | `feed-1`、`flash-1`，3 条流股，4/4 端口绑定；创建后选择与 Inspector 同步 |
| 3. 参数 | Feed 温度输入 `-1 K` 后修正为 `250 K` 并应用；Flash 输入 `200 K / 100000 Pa` 并应用 | 负温度显示 positive finite 错误且不提供应用；修正提交后 Feed Outlet 显示 250 K。源压力 101325 Pa、流量 1 mol/s、Methane / Ethane 各 0.5 为创建时已有默认值，本轮未假称逐项手填 |
| 4. 运行与基本正确性 | 运行当前流程，核对输入、模块结果、结果表与流量 | revision 13、sequence 1 求解成功，2 个步骤、3 条诊断；Flash 出口 200 K / 100000 Pa，液相 0、气相 1 mol/s，总量与入口一致；仅为演示模型软件行为与基本检查 |
| 5. 断开 / 历史 / 重连 | 选中 Feed 流股，断开目标端；命令面板 Undo、Redo；点击重连 | 源端与规格保留，端口 4/4 → 3/4；结果立即过期。Undo 恢复绑定仍保持过期，Redo 再断开；唯一目标重连恢复同一流股，未重新标成当前结果 |
| 6. 删除关联与恢复 | 删除已重连的 Feed 流股，再从命令面板 Undo 并 rerun | 两单元保留、源 / 汇绑定一起清除、选择清空、流股 3 → 2；一次 Undo 恢复 3 条流股和 4/4 绑定。rerun 得到 revision 19、sequence 2 当前快照，结果表保持上述数值 |
| 7. 保存与关闭 | 文件 → 保存；检查反馈；关闭脏项目并取消，再舍弃本轮临时案例 | 未出现保存对话框，消息 `Save As canceled`，仍未保存；非 Windows picker 返回 `None` 与之吻合。关闭确认和取消保留通过，随后关闭；最终只读进程检查未发现验证 Studio 进程。没有本轮 GUI 案例文件可供重开 |

补充观察与分类：

- Feed / Flash 创建、受控连接、目标端断开 / 唯一目标重连、流股删除与命令面板历史具备上述实窗证据。其他单元逐项实窗、源端 / 整股断开实窗、多候选 / 自环 / 占用端口拒绝实窗、F/z 手工编辑、导出、Active 运行交互和 Windows / Linux 本轮运行态保持待验证；相关代码或测试证据不改写为完整 GUI 验收。
- `Ctrl+Z`、`Cmd+Z`、`Ctrl+Y` 注入未观察到文档变化，改用命令面板完成历史验证；尚未区分输入焦点、自动化工具与平台快捷键路径，登记待验证，不登记为已定位产品缺陷。
- 在 Inspector / 模块结果选择实际存在的 Feed 流股后，操作仍可达，但 Canvas 显示 `Canvas navigation anchor expired`。登记导航反馈部分可用，后续定位 anchor / presentation 范围。
- macOS 首次保存 / 另存为与任意文件打开 picker 缺失；本轮 GUI 案例保存后重开因此前置缺口未完成。底层和 mock picker 测试成功不能覆盖这个事实。
- 首个 B1 候选为“单元删除与关联清理”：一次事务删除所选单元，保留流股规格及其他设备绑定，明确影响、取消 / 失败、Undo / Redo、旧结果和持久化。范围与六项验收见 [建模专题](../../topics/flowsheet-modeling-and-solve.md#b1-1-候选单元删除与关联清理待确认未实施)，关联保留策略待项目所有者确认。macOS picker 独立修复，不将变量树、脚本录制或多求解框架提前纳入实现。

验证记录：

- `CARGO_NET_OFFLINE=true cargo test --locked -p rf-ui`：110 项通过。
- `CARGO_NET_OFFLINE=true cargo test --locked -p radishflow-studio --bin radishflow-studio studio_gui_shell::tests::canvas`：24 项通过；覆盖普通空白物性选择、Feed / 单入口调节器 / Mixer 建模、保存重开 rerun、输入与连接旧化及 sidecar。
- 同入口筛选 `studio_gui_shell::tests::project_lifecycle`：31 项通过；真实临时文件覆盖参数 / composition / 重连保存重开、覆盖取消、失败和脏关闭，picker 为测试注入路径。
- Studio lib 首轮 539 项通过、3 项因沙盒禁止绑定本地监听端口报 `PermissionDenied`；最小范围真实环境 `--lib reqwest_transport` 3 项全通过。仓库级沙盒检查遇到相同原因后，以真实环境离线 `CARGO_NET_OFFLINE=true ./scripts/check-repo.sh` 完整通过：1,128 项 Rust 测试、治理、文本、格式、workspace check 和严格 clippy。保留既有 `block 0.1.6` 未来兼容性警告。
- 完成文档回写后另行复核 `xtask`、治理、文本、文档体量及 `git diff --check`；没有新增产品或测试代码、变更 `Cargo.lock`、发布或远端 CI 运行。
- 临时证据位于 `/private/tmp/radishflow-b0-{tests,ui-tests,canvas-tests,lifecycle-tests,http-retest,check-repo,check-repo-real,bundle}.log`；AX 与截图观察在本轮工具记录中，关键事实已写入本文。临时日志、偏好文件和识别外壳可被系统清理，不作为永久验收资产。

## 风险与未完成项

- 真实 OIDC 登录、系统安全存储、控制面服务端和跨项目联调仍未交付。
- 账户归属、准入策略、主体兼容、原生客户端注册和离线撤销窗口属于未来实施前决策，不因文档建立而完成。
- 本周未执行远端三平台 CI、浮动 `stable` 手动 workflow、COM 注册、PME GUI 或独立 UnitOp smoke；9 月 13 日已完成上述 macOS Studio B0 实窗，其他平台 Studio GUI 待验证。Windows ARM64 的 `.NET` contract / Adapter smoke 不替代 x64 CI 或真实 PME 验证。
- `block 0.1.6` 的未来 Rust 兼容性警告仍保留，未进行依赖升级。

## 下周建议

- B0 已形成分层台账，下一迭代先确认 B1-1 单元删除及关联清理；macOS 原生文件选择器单独确定范围，其他未测路径继续补证。数值模型切片实施前再确定工况与独立验收依据。
- 账户联合登录按资源与需求安排 M0 范围和架构决策，再推进服务端与客户端实现。

## 2026-09-13 B1-1 单元删除与 macOS 保存闭环

### 范围与实现

- B0 文档已提交为 `031ac1ca`，随后项目所有者确认继续 B1-1 与独立 macOS 项目选择器修复；在 `dev` 实施，没有创建分支、PR 或提前建设变量树 / 脚本框架。
- 六类现有单元使用统一删除事务；保留流股规格与相邻单元绑定，确认框列出所有受影响端口，取消及文档 / revision / 选择失效不提交。清理选择、草稿和建议，旧结果不能被 Undo 重新标成当前。
- 会话内保留已分配单元 ID，避免删除或历史分支后新单元继承旧身份；布局保留在内存供 Undo，保存 sidecar 只写当前单元。首次保存与另存为同步位置，布局写入失败明确提示且可重试。
- macOS 复用现有 `rfd 0.15` 项目打开 / 保存路径；仅扩展 target dependency，离线构建、`Cargo.lock` 不变。Linux 未接入时明确显示不支持；项目格式、Windows staged write 和结果导出选择器范围不变。

### 真实窗口记录

平台 macOS ARM64，使用临时 `RadishFlow-B0.app` 外壳指向本地 debug 二进制，独立偏好目录 `/private/tmp/radishflow-b1-gui`。启动前告知，全部 UI 操作经 CUA；没有使用 UTM，结束后关闭验收窗口。

1. Home 新建普通空白项目，选择二元烃 Lite、Methane / Ethane。放置 Feed、Heater、Flash Drum，用受控建议创建和连接四条流股，显式提交 Heater `345 K / 101325 Pa` 与 Flash `298.15 K / 101325 Pa`。
2. 手动求解收敛，Feed / Heater 总流量均为 `1 mol/s`；Flash 两出口流量合计 `1 mol/s`，显示有限 SI 数值。使用演示物性检验软件行为，不扩张为真实物性准确性认证。
3. 文件 → 保存打开 macOS 原生对话框，输入中文含空格且不带扩展名的“空白 删除验收”，实际生成 `.rfproj.json` 与 `.rfstudio-layout.json`。
4. 项目树选择 Heater，画布“删除单元”展示 inlet / outlet 两条关联；取消后保留三单元、当前快照和保存状态。再次确认删除变为两单元、四流股、选择清空、快照过期；消息明确关联保留且可撤销。
5. 命令面板 Undo 恢复三单元、六个绑定，结果仍旧化；Redo 再次删除。保存得到 revision 19 的删除版。再次 Undo，通过原生另存为保存“恢复 验收.rfproj.json”，revision 20。
6. 原生打开删除版：两单元、四流股、无当前快照；运行明确报告 `solver.connection_validation.missing_upstream_source`，定位 `stream-heater-1-outlet` / `flash-1:inlet`，没有静默接线。
7. 原生打开恢复版：三单元、四流股、持久化布局；rerun 收敛并形成当前结果。读回两份真实 JSON，删除版仅 Feed / Flash 位置，恢复版包含原 Heater 位置 `(272.84, 50.24)`，其他位置一致。
8. 再次打开原生选择器并取消，当前收敛项目保持。原生输入中文时 CUA 键盘注入未输入中文，改用可访问文本值设置后成功；这是自动化输入路径差异，不记为产品缺陷。

### 验证与剩余边界

- 新增 2 项 rf-ui 测试及 6 项 shell 测试，覆盖六类矩阵、关联与单次历史、无效草稿、失效 / 重复确认、历史分支、删除 / Undo 保存重开、rerun、布局失败重试。Studio binary 219 项通过；初次 lib 测试的 3 项回环 HTTP 监听受沙盒权限限制，最小真实环境复验全部通过。
- 首次全仓测试全部通过，严格 clippy 发现测试的一处多余引用，修正后 clippy 通过；随后统一 shell 保存命令和文件菜单以补齐 sidecar 保存，完成最终全仓复验，结果见当前状态验证基线。
- 六类自动化矩阵不代表六类全部实窗验收；本轮 GUI 重点覆盖中间 Heater 删除。Windows / Linux 新增运行态、快捷键焦点、macOS 原生覆盖按钮组合及真实权限故障继续待验证，既有故障注入 / 生命周期测试维持回归。
- B0 已记录的 `Canvas navigation anchor expired` 在存在的 Heater / Flash 上仍可复现；删除后成功反馈已清理旧提示，其他导航根因未扩入本切片。下一步建议单独确认导航反馈修复，范围是选择、视图锚点与编辑后重新定位，验收不包含自由连接、重命名或通用脚本框架。


## 2026-09-13 B1-2 Canvas 导航反馈修复

- 按项目所有者要求先提交 B1-1：`b05211c2 feat(studio): 完善单元删除与 macOS 项目保存闭环`；随后在已确认的导航范围内继续推进。
- 根因：UI 同帧前半段更新选择后，Canvas drop surface 仍消费旧 window presentation，把新锚点误判为过期；连接后的布局序号变化也使单纯字符串比较失效。
- 修复：Canvas 使用当前同一份 presentation 校验导航并绘制几何；按对象 kind / ID 重新解析锚点，非导航命令保留有效导航。真实删除仍给出失效反馈，Undo 恢复后清除警告，清空选择不误称对象不存在。导航职责从已超过 1500 行的 shell app 文件归入同域模块。
- 自动化新增 3 项回归，覆盖旧帧中的单元 / 流股选择、普通空白 Feed–Heater–Flash 受控连接导致的实际布局重排、清空选择；扩展现有测试覆盖真实流股删除 / Undo 后再次定位。先修正了测试使用错误建议入口的问题，改用现有正式 `canvas.accept_focused` 路径，没有为通过测试新增产品兼容分支。
- 验证：Studio binary 222 项通过；macOS 真实环境、Rust 1.96.0、离线 `check-repo` 全部通过，共 1,139 项测试与严格 clippy。沿用既有回环 HTTP 测试需真实监听的环境范围；依赖、lockfile 和项目格式没有变化。
- 实窗：提前告知后使用临时 app 外壳与 `/private/tmp/radishflow-b1-gui` 独立偏好。重开上一轮从普通空白创建的“恢复 验收”项目，项目树选择 Heater / Feed Outlet / Flash、断开重连均正确显示 located；最终二进制删除 Heater Outlet 显示真实失效，命令面板 Undo 后警告消失，重新选择流股与直接点击 Canvas Feed 正常定位，rerun 收敛。两次验证均舍弃测试编辑并关闭窗口，没有启动 UTM。
- 未声明为完成的路径：Windows / Linux 实窗、快捷键焦点、远距离视口平移，以及其他五类单元的逐项删除 GUI。下一候选为单元重命名，范围与验收写入建模专题，尚未实施。
