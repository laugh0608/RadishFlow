# 2026-W15 Development Log

周期：2026-04-06 至 2026-04-12

## 本周目标

- 把已冻结的 Studio host/controller 边界真正收口成 GUI 可直接消费的宿主适配入口
- 避免 `main.rs` 或未来真实桌面框架继续手工拼装窗口生命周期、command id 派发和宿主副作用读取

## 完成情况

- 在 `apps/radishflow-studio` 中新增 `studio_gui_host` 模块，建立 `StudioGuiHost` 这一层 GUI-facing 宿主适配器
- 为该适配器补出统一结果类型，覆盖窗口打开、窗口派发、UI command id 派发、全局事件派发和窗口关闭
- 让 GUI 侧结果直接携带最新 `ui_commands`，避免上层在命令执行后再手工回查 command registry
- 让 GUI 侧继续直接拿到既有 `runtime_report`、entitlement timer effect、native timer transition 与 ack 摘要，不重新发明第二套副作用模型
- 把仓库级 Rust 集成测试基线继续从“示例项目求解成功/失败”推进到“`*.rfproj.json -> solver snapshot -> rf-ui/AppState -> Studio RunPanel` 诊断映射”闭环
- 新增 `tests/rust-integration` 对 Studio `solver_bridge` 的三类仓库级回归：成功快照写回、`solver.step.execution` 失败映射，以及缺失 property package 时的无稳定 solver code 失败映射
- 继续把仓库级集成基线扩到 `workspace_control` / `run_panel_driver`，补上成功运行、包选择阻塞和失败后恢复动作聚焦目标单元三条应用层闭环回归
- 开始把负向回归夹具从测试内嵌 JSON 提升到 `examples/flowsheets/failures/*.rfproj.json`，首批先覆盖 `step.execution`、`connection_validation` 与 `topological_ordering`
- 继续把剩余连接校验负向场景提成正式夹具，补齐缺失上游 source、缺失 stream 引用和 invalid port signature 三条 `solver.connection_validation` 基线
- 把求解失败上下文提取回收到 `rf-solver`，新增 `SolveFailureContext`，统一解析稳定 `primary_code` 与 `related_unit_ids`；Studio 不再在 bridge/app facade 层扫描整个 flowsheet 猜失败单元
- 继续把 `multi-unit cycle` 也提成正式失败夹具，并补出 Studio 侧循环失败回归，锁定 `solver.topological_ordering -> BreakCycle notice/recovery` 的应用层闭环
- 继续把 `topological_ordering` 从泛化 cycle 提示细化到可执行子码：solver 当前会提前识别单单元自环并挂出 `solver.topological_ordering.self_loop_cycle`
- 上述 self-loop failure 当前也已补出 `related_stream_ids + related_port_targets`，`RunPanel` 会把它进一步落成 `Disconnect self-loop inlet`，并在应用层正式写回 `DocumentCommand::DisconnectPorts`
- `self-loop-cycle` 夹具当前也已补进 `rf-solver`、`flowsheet_examples`、`studio_solver_bridge`、`studio_workspace_control` 与 `rf-ui` 恢复执行回归，锁定“`solver.topological_ordering.self_loop_cycle` -> RunPanel disconnect inlet recovery”闭环
- 继续把双单元互相依赖也从泛化 cycle 提示细化到稳定子码：solver 当前会为 `heater-1 <-> valve-1` 这类互反依赖挂出 `solver.topological_ordering.two_unit_cycle`
- 上述 two-unit cycle failure 当前也已补出 `related_stream_ids + related_port_targets`，`RunPanel` 会优先选择闭环中的最后一个 inlet 端口，直接落成 `Disconnect cycle inlet`，并在应用层正式写回 `DocumentCommand::DisconnectPorts`
- `multi-unit-cycle` 夹具当前也已升级为 `rf-solver`、`flowsheet_examples`、`studio_solver_bridge`、`studio_workspace_control` 与 `rf-ui` 共同消费的双单元 cycle 执行恢复基线，锁定“`solver.topological_ordering.two_unit_cycle` -> RunPanel disconnect inlet recovery”闭环
- 继续把 solver failure 的结构化语义下沉到错误对象本身：`RfError` 当前已可携带 `diagnostic_code + related_unit_ids`，`rf-solver` 在关键失败构造点开始直接写入这份上下文，Studio bridge 不再只能靠 message 反推
- 继续把 Studio workspace run 的应用层诊断收口为结构化映射：`workspace_run_command` 已给 blocked reason 挂稳定 `diagnostic_code`，`solver_bridge` 也开始给本地缓存准备失败挂 `workspace.run.local_cache_unavailable`，`app_facade` 不再依赖 message 文本区分 blocked/failed 原因
- 把这两类 Studio workspace run 结果继续抬到仓库级集成测试：`studio_workspace_control` 当前已覆盖 `ExplicitPackageSelectionRequired`、`EntitlementMismatch` 与 `LocalCacheUnavailable -> RepairLocalCache` 三条应用层闭环
- 把 `solver.connection_validation` 的结构化语义继续下沉到 `rf-flowsheet`：连接校验失败当前也会携带 `related_unit_ids`，并已由 `rf-solver` 与 `flowsheet_examples` 仓库级回归锁定到 `unsupported-unit-kind`、`missing-upstream-source`、`missing-stream-reference` 与 `invalid-port-signature` 四类失败夹具
- 继续把这份 `connection_validation` 相关单元语义抬到应用层行为：`studio_workspace_control` 当前已覆盖 `invalid-port-signature` 恢复动作会把失败单元直接重建回 canonical ports，并继续聚焦到目标 unit inspector
- 继续把 `connection_validation` 从阶段级错误细化为稳定子码：`rf-flowsheet` 当前会为 `unsupported_unit_kind`、`invalid_port_signature`、`missing_stream_reference`、`missing_upstream_source`、`duplicate_*`、`unbound_inlet_port`、`unbound_outlet_port` 与 `orphan_stream` 挂 `flowsheet.connection_validation.*`，`rf-solver` 会保留并映射为对应的 `solver.connection_validation.*`
- `rf-ui::RunPanel` 当前也已按这批子码细化 notice/recovery：`invalid-port-signature` 已从纯检查提示推进为 `Restore canonical ports` 执行型恢复，`missing-upstream-source` 则会落到移除悬空 inlet stream 的定向清理，不再把所有连接校验失败都泛化成同一条 `FixConnections`
- 继续把恢复目标从“只会聚焦 unit”推进到“必要时可直接聚焦 stream”：`RfError`、`SolveFailureContext`、`DiagnosticSummary` 与 `RunPanelRecoveryAction` 当前都已补出 `related_stream_ids / target_stream_id`，`duplicate-downstream-sink` 这类连接失败现在会直接把 RunPanel 恢复动作落到问题流股
- 新增 `examples/flowsheets/failures/duplicate-downstream-sink.rfproj.json`，并由 `flowsheet_examples`、`studio_solver_bridge`、`studio_workspace_control` 三层仓库级回归锁定“`solver.connection_validation.duplicate_downstream_sink` -> RunPanel stream target recovery”闭环
- 继续把连接恢复语义往端口级推进：`RfError`、`SolveFailureContext`、`DiagnosticSummary` 与 `RunPanelRecoveryAction` 当前也已补出 `related_port_targets / target_port_name`，像 `missing_upstream_source` 这类失败现在会把恢复提示稳定细化到具体 `unit.port`
- `RunPanel` 当前在端口级失败上会直接显示 `Suggested target: unit <unit> port <port>`，但实际恢复仍复用现有 unit inspector；这一步先补齐稳定语义和应用层闭环，不额外扩张新的 inspector target 类型
- 继续把 RunPanel 恢复从“只做定位”推进到“第一批可执行修复”：`missing_stream_reference`、`duplicate_upstream_source` 与 `duplicate_downstream_sink` 当前都会下沉为 `DisconnectPort` mutation，并在应用层正式写回 `DocumentCommand::DisconnectPorts`
- `rf-ui` 与 `studio_workspace_control` 当前已补出执行型回归，锁定恢复动作触发后会真正断开目标 `unit.port` 的 `stream_id`、推进文档 revision，并继续聚焦到对应 unit inspector
- 新增 `examples/flowsheets/failures/duplicate-upstream-source.rfproj.json`，并把 `duplicate_upstream_source` 也补进 `flowsheet_examples`、`studio_solver_bridge` 与 `studio_workspace_control` 三层仓库级回归，避免第一批可执行恢复只对 duplicate sink 有正式夹具覆盖
- 继续把可执行连接恢复从“断开冲突端口”扩到“移除孤立对象”：`solver.connection_validation.orphan_stream` 当前也已下沉为稳定 `DeleteStream` 型恢复，不再只停留在 `Reconnect orphan stream` 文案提示
- `rf-ui::RunPanelRecoveryMutation`、`DocumentCommand` 与应用层文档写回当前都已补出 `DeleteStream`，`RunPanel` 对 orphan stream 会直接建议删除目标流股，而不是让用户自行猜测是否该删还是该连
- 新增 `examples/flowsheets/failures/orphan-stream.rfproj.json`，并把 `orphan_stream` 也补进 `flowsheet_examples`、`studio_solver_bridge`、`studio_workspace_control` 与 `rf-ui` 恢复执行回归，锁定“`solver.connection_validation.orphan_stream` -> RunPanel delete stream recovery”闭环
- 继续把 `unbound_material_port` 拆到方向级稳定子码：`solver.connection_validation.unbound_inlet_port` 当前保持端口级检查提示，`solver.connection_validation.unbound_outlet_port` 则进一步下沉为可执行恢复
- `RunPanel` 当前会把 unbound outlet 直接落成 `Create outlet stream`，恢复执行时会自动生成占位 stream id、插入流股并写回 `DocumentCommand::ConnectPorts`，不再要求用户先手工补一条 terminal outlet stream
- 新增 `examples/flowsheets/failures/unbound-outlet-port.rfproj.json`，并把 `unbound_outlet_port` 补进 `flowsheet_examples`、`studio_solver_bridge`、`studio_workspace_control` 与 `rf-ui` 恢复执行回归，锁定“`solver.connection_validation.unbound_outlet_port` -> RunPanel create outlet stream recovery”闭环
- 同时补出 `examples/flowsheets/failures/unbound-inlet-port.rfproj.json`，并把 `unbound_inlet_port` 也补进 `flowsheet_examples`、`studio_solver_bridge` 与 `studio_workspace_control`，锁定这条方向级子码当前仍保持 `Inspect inlet path` 的检查型恢复，不误落成执行型 mutation
- 继续把 `invalid_port_signature` 也从“检查型恢复”推进为“可执行修复”：`RunPanel` 当前会按 builtin unit spec 重建 canonical ports，并优先复用同名或唯一方向/类型匹配端口上的既有 `stream_id`
- `rf-ui`、`studio_solver_bridge` 与 `studio_workspace_control` 当前已补出这一条 canonical port restore 回归，锁定恢复执行后会真正写回 `DocumentCommand::RestoreCanonicalUnitPorts`、推进文档 revision，并保留 `stream-feed -> feed-1.outlet` 这类可稳定继承的绑定
- 继续把 `SolveSessionState` 与快照历史的边界收紧到“当前文档 revision”口径：文档一旦提交新 revision，`latest_snapshot` / `latest_diagnostic` 会立即退出当前会话引用；`RunPanel`、`WorkspaceModeDispatch` 与 `WorkspaceRunDispatch` 不再把旧 revision 的 `snapshot_history.back()` 误显示成当前结果
- 继续把第一批可执行 recovery 真正闭到“修完再跑”主线：`tests/rust-integration::studio_workspace_control` 当前已补出 `invalid_port_signature -> Restore canonical ports -> rerun` 与 `unbound_outlet_port -> Create outlet stream -> rerun` 两条仓库级回归，锁定恢复执行后会先把工作区推回 `Dirty + DocumentRevisionAdvanced`，再由下一次运行产出新的 `rev-1` snapshot
- 继续收紧“重复运行”的结果语义：若同一 revision 已有成功 snapshot、随后再次运行失败，`SolveSessionState` 当前会主动清空旧 `latest_snapshot`；`RunPanel` 与 `WorkspaceRunDispatch` 不再继续展示上一轮成功结果，避免失败后仍把旧 snapshot 冒充成当前结果
- 继续把 `Automatic` 主线从单元层推进到仓库级：`tests/rust-integration::studio_workspace_control` 当前已补出 `Active + DocumentRevisionAdvanced -> automatic preferred solve` 正向回归，以及 `Active + no pending request -> automatic skip before package resolution` 门控回归，锁定应用层会先按 `SimulationMode/pending_reason` 做 Automatic 门控，再决定是否解析 preferred package
- 继续把这条 Automatic 主线前推到入口层：`bootstrap/runtime` 当前已补出 `StudioBootstrapTrigger::AppCommand`，并通过连续触发回归锁定 `SetMode(Active) -> Automatic preferred run` 与“成功后再次 Automatic -> NoPendingRequest skip”两条正式入口时序，避免入口层只能绕道 UI intent 才能演练应用层 Automatic 语义
- 继续把这条 Automatic 主线前推到 GUI 文档写回入口：`StudioAppHostController` 当前会在 `Active + DocumentRevisionAdvanced` 的画布 suggestion 接受后，直接沿既有 `AppCommand(automatic_preferred)` 正式链路补发 automatic solve，不再把“文档已变更但仍停在 Dirty”留给 GUI 壳自己猜测后续动作
- `StudioGuiDriver` 当前已补出这条仓库级 GUI 回归：`Shift+F6` 激活工作区后接受单条补线 suggestion，会自动产出 `example-feed-heater-flash-rev-1-seq-1` 收敛快照，并把运行栏状态稳定刷新到 `Converged`
- 继续把 recovery 后返回主线的 GUI 时序补成正式回归：`StudioGuiDriver` 当前已补出 `failed run -> F8 recovery -> Shift+F5 resume` 连续快捷键测试，锁定恢复动作会把工作区推进到 `Hold + DocumentRevisionAdvanced`，随后 `Resume Workspace` 会沿正式 Automatic 主线产出 `example-unbound-outlet-port-rev-1-seq-1`
- 继续把 canvas suggestion 接受后的主线补跑下沉到共享 session 入口；`studio_runtime / studio_window_host / studio_window_host_manager / studio_app_host` 这条旧 typed 链路当前也已统一复用 `Active + DocumentRevisionAdvanced -> automatic_preferred` 正式主线，不再只在 GUI driver/controller 新入口上成立
- 继续把这条 canvas mainline 收进正式命令面：`StudioAppWindowHostManager`、`StudioAppHost` 与 `StudioGuiHost` 当前都已补出 `DispatchCanvasInteraction`，`StudioGuiDriver` 也改为真实经由这条命令链执行 canvas accept/reject/focus，而不再直接旁路 ad-hoc typed 方法
- 继续把 canvas widget / shortcut / host 三条 GUI 正式命令面统一到稳定 command id：`canvas.accept_focused`、`canvas.reject_focused`、`canvas.focus_next`、`canvas.focus_previous`
- `StudioGuiCanvasWidget` 当前已不再直接产出私有 `CanvasSuggestion*Requested` 主路径，而是统一发 `UiCommandRequested { command_id }`；`StudioGuiCommandRegistry` 与 `StudioGuiShortcutRouter` 也已同步按这组 command id 收口
- 继续把这条命令面里的 canvas 状态语义补齐：`StudioAppHostController` 当前不再对 `DispatchCanvasInteraction` 无条件 `refresh_local_canvas_suggestions()`，避免 `FocusNext/Reject` 刚产生的新焦点又被 local-rules 刷回首条 suggestion
- 上述修正当前已补出 GUI/host 级回归，锁定 `canvas.focus_next -> follow-up reject` 会继续命中新的 focused suggestion，而不是退回命令前焦点
- 同时已把这组变化同步回 `docs/architecture/app-architecture.md`、`docs/radishflow-mvp-roadmap.md` 与 `docs/mvp/scope.md`，避免“代码已收口为统一 command surface，但正式设计/规划文档仍只记录 run panel command registry”的口径滞后
- 在 `apps/radishflow-studio` 中新增 `studio_gui_timer_host`，把底层 `StudioWindowTimerDriverTransition` / ack 收口成 GUI-facing 的 `StudioGuiNativeTimerEffects`
- 继续把 native timer GUI 面从“只看摘要”推进到“可执行 runtime”，新增 `StudioGuiNativeTimerRuntime`
- 让 GUI 侧在消费 `StudioGuiNativeTimerEffects` 后可继续跟踪逻辑 timer handle、`next_due_at` 与一次性 due callback，不必在真实框架里重写同一套 timer 生命周期
- 把窗口打开时已经自动应用的 timer `transition/ack` 一路抬到 `StudioWindowSession -> StudioAppHost -> StudioGuiHost -> StudioGuiDriver` 的 GUI-facing 结果里，避免 parked timer 在“关闭最后窗口 -> 重开 owner”场景下只发生在底层、上层却看不见
- `StudioGuiDriver` 当前也已自动同步这份 native timer runtime，并补出 `next_due_native_timer_at()` / `drain_due_native_timer_events(...)`，让最小入口和未来真实 GUI 宿主都能按统一入口排空到点 timer 事件
- `apps/radishflow-studio/src/main.rs` 当前也已开始直接消费这条 runtime，最小演示入口不再只打印 timer 摘要，而能显式演练“读取下一次到点 -> 排空 due callback -> 回灌生命周期事件”
- 继续把真实平台 callback 入口也前推为正式 GUI 事件，新增 `StudioGuiEvent::NativeTimerElapsed { window_id, handle_id }`
- `StudioGuiDriver` 当前会先用 `StudioGuiNativeTimerRuntime` 校验 callback 是否命中当前有效绑定，只有命中时才继续回灌 `StudioGuiHostLifecycleEvent::TimerElapsed`；错误 handle 或 stale callback 会显式忽略
- 新增测试，锁定“当前 binding callback 会继续派发”和“错误 handle callback 会被忽略”这两条真实宿主接线语义
- 在 `apps/radishflow-studio` 中新增 `studio_gui_platform_host`，建立 `StudioGuiPlatformHost`
- 让平台层在每次 `dispatch_event(...)` 与 `dispatch_due_native_timer_events(...)` 后自动比较前后 pending timer binding，显式产出 `Arm / Rearm / Clear` 三类 `StudioGuiPlatformTimerRequest`
- `StudioGuiPlatformTimerRequest` 当前又已继续携带 `window_id / handle_id / slot / due_at`，真实平台不必再自行补出 callback 身份或追查当前 binding
- 在 `apps/radishflow-studio` 中新增 `studio_gui_platform_timer_driver`，建立 `StudioGuiPlatformTimerDriverState`
- 让这层适配器继续消费 `StudioGuiPlatformTimerRequest`，显式保存 `native_timer_id -> logical binding` 映射，并把平台侧真正需要执行的动作收口为 `Arm / Rearm / Clear` 命令
- 这层适配器当前又已补出显式 `start failure ack`，平台若创建 native timer 失败，不再只能让 pending schedule 悬空
- `StudioGuiPlatformHost` 当前又已正式持有这层 timer adapter，并把 start failure 反馈直接并入 `snapshot/window_model.runtime.log_entries`
- 上述 start failure 当前又已进一步收口为 `runtime.platform_notice`，真实桌面 GUI 不必再扫描日志列表才能看见平台 timer 创建失败
- 同一份 `platform_notice` 当前也已继续前推到 runtime panel 的布局 `summary/badge`，真实桌面 GUI 在面板折叠或 tab strip 态下仍能感知平台异常
- 平台 native timer callback 若命中不存在或过期的 `native_timer_id`，当前也已不再主要表现为 `RfError`；`StudioGuiPlatformHost::dispatch_native_timer_elapsed_by_native_id(...)` 已改为显式返回 `Dispatched / IgnoredUnknownNativeTimer / IgnoredStaleNativeTimer`
- 平台 native timer start success/failure ack 当前也已不再只停留在内部 `status enum`；`StudioGuiPlatformHost` 现在会继续返回正式 outcome，真实平台可直接消费 missing/stale 分支，且成功 ack 若已过期会显式带出需要立即清理的 `native_timer_id`
- 上述 start outcome 当前又已继续补出 `follow_up_command()`；真实平台 glue 可以直接把 stale/missing success ack 映射为 `ClearNativeTimer`，不必自己再从结果字段里二次拼清理动作
- 继续把异步型 start ack 也收成批处理结果面，新增 `acknowledge_platform_timer_started_feedbacks(...)`、`acknowledge_platform_timer_started_feedbacks_and_execute_follow_up_commands(...)` 与 `acknowledge_platform_timer_start_failed_feedbacks(...)`
- 上述 ack batch 当前会在同一份正式返回里直接携带逐条 feedback outcome、聚合后的 follow-up cleanup、最终 `snapshot` 与下一次 native timer schedule；真实宿主不必再自己写循环、收集 cleanup 后回查 host
- 继续把异步型 callback / due-drain 的“不执行平台 API”场景也补成正式 batch 结果，新增 `dispatch_native_timer_elapsed_by_native_ids(...)` 与 `dispatch_due_native_timer_events_batch(...)`
- 上述非执行型 batch 当前会直接携带逐条 outcome、最终 `snapshot`、下一次 native timer schedule，以及 `native_timer_requests()` helper；异步平台宿主不必再自己一边循环结果、一边手工抽取 request
- 继续把异步宿主一轮消息循环也收成正式结果面，新增 `process_async_platform_round(...)`
- 上述 async round 当前会按固定顺序统一消费 `started_feedbacks -> start_failed_feedbacks -> native_timer_ids -> due_at`，并直接返回最终 `snapshot`、聚合后的 `native_timer_requests()` 与 `follow_up_commands()`；真实宿主不必再自己维护同一轮内的处理顺序
- 上述 async round 当前又已继续补出 `actions()`，把 `follow_up cleanup -> timer request` 的宿主执行顺序固定成正式动作清单；真实平台 glue 不必再自己归并两类动作
- 在此基础上，继续把“异步回合输入 + 同步平台 timer API”的宿主组合路径也收成正式结果面，新增 `process_async_platform_round_and_execute_actions(...)`
- 上述组合入口会复用 host 内冻结的 `actions()` 顺序直接执行 cleanup / timer request，并返回执行后的最终 `snapshot/window`；最小宿主样例不再手工拆 round 动作再逐条推进 host
- 针对同步型平台 timer API，`StudioGuiPlatformHost` 当前又已补出 `execute_platform_timer_request(...)` 与 `StudioGuiPlatformTimerExecutor` trait，最小演示入口不再手工串接 `apply_request -> execute -> acknowledge`
- 上述同步型 glue 当前又继续前推到 `dispatch_event_and_execute_platform_timer(...)`、`dispatch_native_timer_elapsed_by_native_id_and_execute_platform_timer(...)` 与 `dispatch_due_native_timer_events_and_execute_platform_timers(...)`，最小演示入口不再手工把 `dispatch` 结果拆开后再单独执行 timer request
- 上述组合入口当前又已补到“执行后刷新 dispatch 里的 `snapshot/window`”；若同步执行里直接触发平台失败 notice 或 runtime log 追加，真实 GUI 可以直接消费组合结果，不必再额外回查 host
- 继续把“真实宿主批量消费”也收成正式结果面，新增 `dispatch_native_timer_elapsed_by_native_ids_and_execute_platform_timers(...)` 与 `drain_due_native_timer_events_and_execute_platform_timers(...)`
- 上述批处理结果当前会在同一份正式返回里直接携带逐条 callback/dispatch outcome、最终 `snapshot` 与下一次 native timer schedule；真实 GUI 宿主不必再自己写循环、累计结果后回查 host
- `apps/radishflow-studio/src/main.rs` 当前也已开始直接消费 callback batch，最小演示入口不再只示范单次 callback glue
- `apps/radishflow-studio/src/main.rs` 当前又已开始通过 `StudioGuiPlatformTimerDriverState` 模拟真实平台 timer 创建与 callback 回灌，不再只演示 request 文本输出
- 新增测试，锁定首次 arm、rearm 时旧 native timer 替换、clear 后 binding 清空，以及 stale start ack 拒绝这四条平台 adapter 语义
- `apps/radishflow-studio/src/main.rs` 当前也已切到 `StudioGuiPlatformHost`，最小演示入口不再手工比较 `next_due_at` 差分，而是直接消费平台 timer request
- 新增测试，锁定“首次出现逻辑 timer 时会产出 arm 请求”“排空 due timer 后会按新的逻辑到点时间产出 rearm 请求”“unknown/stale native_timer_id callback 会返回 ignored outcome”，以及“过期/缺失 pending schedule 的 start ack 会返回显式 cleanup outcome”这几条平台适配语义
- 让 `StudioGuiHostDispatch` 与 `StudioGuiHostCloseWindowResult` 直接携带 `native_timers`，GUI 不必再逐条依赖 window session / app host 的底层 timer transition 类型
- 继续把 `StudioGuiHost` 收口为统一命令面，新增 `StudioGuiHostCommand` / `StudioGuiHostCommandOutcome` 与 `execute_command(...)`
- 让 GUI 侧可以通过单一命令入口驱动窗口打开、原始窗口 trigger、生命周期事件、`command_id` 派发与窗口关闭
- 在 `apps/radishflow-studio` 中新增 `studio_gui_driver`，建立 `StudioGuiDriver + StudioGuiEvent + StudioGuiDriverDispatch`
- 让平台侧窗口事件、网络恢复、timer callback 与菜单/快捷键命令都可以先翻译成 `StudioGuiEvent`，再统一进入 `StudioGuiHostCommand`
- 在 `apps/radishflow-studio` 中新增 `studio_gui_command_registry`，把 `UiCommandModel` 派生成更适合菜单/命令面板消费的 `StudioGuiCommandRegistry`
- 让 `StudioGuiDriverDispatch` 直接携带 `command_registry`，GUI 不必再自己按 `group/sort_order` 做二次整理
- 继续把首批稳定 run panel command id 的默认 GUI 绑定冻结到 `StudioGuiCommandRegistry`：
- 菜单路径 `menu_path`
- 命令面板搜索词 `search_terms`
- 默认快捷键 `shortcut`
- 当前默认快捷键口径收为：
- `run_panel.run_manual` -> `F5`
- `run_panel.resume_workspace` -> `Shift+F5`
- `run_panel.set_hold` -> `F6`
- `run_panel.set_active` -> `Shift+F6`
- `run_panel.recover_failure` -> `F8`
- 继续把 `StudioGuiDriver` 接到快捷键事件层，新增 `StudioGuiEvent::ShortcutPressed`，让 GUI 可按当前 registry 把快捷键直接翻译成稳定 command id，而不是框架层自己手写查表
- 在 `apps/radishflow-studio` 中新增 `studio_gui_shortcut_router`，把“快捷键 + 当前焦点上下文 -> 谁来消费”冻结为正式策略层
- 当前首轮焦点上下文口径包括：
- `Global`
- `Canvas`
- `CanvasSuggestionFocused`
- `InspectorPanel`
- `TextInput`
- `CommandPalette`
- `ModalDialog`
- 当前首轮快捷键竞争规则收为：
- `F5/F6/F8` 这类已绑定宿主命令默认允许跨 `Global/Canvas/InspectorPanel/TextInput` 直接走 command registry
- `CommandPalette` 与 `ModalDialog` 当前优先截获快捷键，不透传宿主命令
- `Tab` 在 `CanvasSuggestionFocused` 时路由为 `CanvasSuggestionAcceptRequested`
- `Tab` 在 `TextInput` 时明确由文本输入焦点占有，不交给宿主命令
- `Tab` 在无 suggestion 的普通画布/全局场景下当前返回显式 ignore reason，而不是静默吞掉
- 继续把 `StudioGuiDriver` 的结果面从“只有 host command outcome”扩成：
- `HostCommand(...)`
- `CanvasSuggestionAccepted(...)`
- `IgnoredShortcut { reason }`
- 继续把画布 suggestion 的最小应用层桥补到 Studio 侧：
- `BootstrapSession / StudioRuntime / StudioRuntimeHostPort / StudioWindowSession / StudioAppWindowHostManager / StudioAppHostController / StudioGuiHost`
- 当前 `Tab` 在 `CanvasSuggestionFocused` 下已不只返回请求，而是会真正落到 `rf-ui::AppState::accept_focused_canvas_suggestion_by_tab()`
- 同时补出 Studio 侧 `replace_canvas_suggestions(...)` 入口，为后续本地规则或 `RadishMind` suggestion 注入保留正式桥接点
- 继续把这条 suggestion 接受闭环补成可观测反馈：
- `rf-ui::AppState` 在 `Tab` 接受高置信 suggestion 后会追加一条日志，并同步刷新 run panel 的 `latest_log_message`
- `StudioGuiHostCanvasSuggestionResult` 现在会直接携带 `latest_log_entry`，GUI 不必为了显示接受反馈再跨层回查运行时状态
- 继续把 suggestion 接受后的最小 UI 落点也接上：
- `rf-ui::AppState` 在接受 suggestion 后会同步更新 `selection`、`drafts.active_target` 与 `inspector_open`
- `StudioGuiHostCanvasSuggestionResult` 现在也会直接携带 `applied_target`，为后续 inspector 聚焦和状态栏提示保留正式桥接结果
- 继续把 suggestion 接受推进到正式文档写回：
- `CanvasSuggestion` 当前已补出正式 `acceptance` payload，不再只靠 `ghost.target_unit_id` 临时猜连接命令
- `rf-ui::AppState::accept_focused_canvas_suggestion_by_tab()` 现在会在命中文档写回型 suggestion 时先执行本地 material connection 写回与连接校验，再提交文档 revision
- `DocumentCommand::ConnectPorts` 当前已补出显式 `stream_id`，并允许 `to_unit_id / to_port` 为空，以覆盖 terminal outlet stream 写回
- 当前已覆盖两条正式接受路径：
- 复用已有 stream 接到缺失 sink
- 新建 terminal outlet stream 并绑定到 source port
- 更新 `apps/radishflow-studio/src/main.rs`，改为直接消费 `StudioGuiHost`，不再手工串接 `StudioAppHostController`
- 继续把 GUI 侧宿主入口从“两套事件通道”收口为单一 `dispatch_lifecycle_event(...)`，统一承接：
- `WindowForegrounded { window_id }`
- `LoginCompleted`
- `NetworkRestored`
- `TimerElapsed`
- `RunPanelRecoveryRequested`
- 让 `main.rs` 的最小入口也改为通过 `StudioGuiHostLifecycleEvent` 演示生命周期派发，而不是继续分别调用 `focus_window(...)` / `dispatch_global_event(...)`
- 让 `main.rs` 的 helper 也改为通过 `StudioGuiHost::execute_command(...)` 取结果，最小入口不再示范旧的多方法拼接方式
- 让 `main.rs` 的最小入口进一步改为通过 `StudioGuiDriver` 分发事件，开始示范“平台事件 -> driver -> host command outcome”的真实宿主桥接路径
- 为新适配层补充单元测试，覆盖：
- 禁用命令时返回当前 command registry
- `run_panel.run_manual` 派发后 command registry 刷新
- 窗口关闭后的 entitlement timer retirement 摘要保持稳定
- `WindowForegrounded` 生命周期事件会正确路由到目标窗口
- `TimerElapsed` 生命周期事件在多窗口场景下会优先路由到当前 entitlement timer owner，而不是前台观察窗口
- native timer GUI 摘要会稳定映射 arm / transfer 等底层 driver transition，并在 GUI host 派发/关闭结果中保持与底层输出一致

## 关键决策

- `StudioAppHostController` 继续作为 app-host 级正式控制入口保留，不把 GUI 直接绑死在更底层的 `window host manager` 或 `runtime` 上
- 新增的 `StudioGuiHost` 只做 GUI-facing 适配与结果整形，不复制 app host 状态机、timer owner 规则或 command registry 逻辑
- runtime 区内与 scheduler / platform / GUI activity 相关的宿主反馈，当前也应优先沉回正式 `snapshot/window model`，而不是继续停留在 `egui` 壳私有状态；后续真实桌面框架不应再复制第二套平台活动或调度说明状态机
- GUI 侧后续优先通过 `dispatch_ui_command(command_id)` 与窗口生命周期入口接线，而不是重新散落 `RunPanelIntent`、foreground 推断和手工副作用读取
- GUI 侧的文档写回型交互当前也应继续保持和应用层一致：若交互已经把工作区推进到 `Active + DocumentRevisionAdvanced`，宿主应直接复用既有 `automatic_preferred` 应用命令链收口后续运行，而不是让 GUI 壳额外维护一套“编辑后是否要自动跑”的私有分支
- failure recovery 当前仍遵守 `Hold/Resume` 分离模型：恢复动作负责修正文档并推进 `DocumentRevisionAdvanced`，真正回到求解主线仍应通过 `Resume Workspace -> Automatic` 这一条正式命令链，而不是让 GUI 或 recovery 分支静默越权改写模式语义
- canvas focus / reject 这类纯交互命令当前不应再顺手触发 controller 级 local-rules 全量刷新；local suggestion refresh 只应保留给真正会改写文档或显式要求重算 suggestion 的路径，否则会破坏正式命令面的连续交互语义
- Studio GUI 壳当前已不再是本周最值得继续扩张的主线；后续仓库级回归优先补强 `examples/flowsheets`、`tests/rust-integration` 与求解诊断映射闭环，再决定下一轮内核或应用层推进点
- GUI 生命周期事件在适配层正式收口为单一入口；“窗口前台变化”虽然底层仍走 `focus_window` 语义，但不再要求 GUI 自己区分调用路径
- native timer 也在 GUI 适配层形成正式消费面；后续真实桌面框架应优先绑定 `StudioGuiNativeTimerEffects`，而不是跨层读取 `StudioWindowTimerDriverTransition`
- Studio workspace run 的 blocked/failed 原因当前也应优先通过稳定 `diagnostic_code` 向上游暴露，不再继续在 app facade 里扩散 message contains 协议
- 连接校验的稳定 code 当前采用“父级阶段 + 子级根因”口径；`solver.connection_validation.*` 既保留阶段分组能力，也允许 RunPanel/Studio 直接按具体根因驱动更精准的恢复动作
- 对连接失败而言，恢复目标当前不再默认只能选第一条相关 unit；若根因更偏向流股对象本身，RunPanel 应优先暴露 stream target，而不是继续把 stream 问题错误地投影成 unit 问题
- 对连接失败而言，恢复提示当前也不应只停在“哪个 unit/stream 有问题”；若根因已经明确落到某个 material port，RunPanel 应优先把这份 `unit.port` 语义抬到界面文案，而不是继续要求上层从 message 文本中二次猜测
- native timer 当前又进一步形成“driver runtime + platform host request”两层边界；后续真实桌面框架应优先消费 `StudioGuiPlatformTimerRequest`，而不是在入口层重复维护 due 时间差分
- 上述 platform timer request 当前也不再只是时间戳，而是正式携带 binding 身份；后续真实桌面框架可以直接据此清理/重建平台 timer，并把 callback 稳定回灌为 `window_id + handle_id`
- native timer 当前又继续形成第三层平台 adapter：真实桌面框架后续只需把各自 `native timer id` 注册/释放 API 接到 `StudioGuiPlatformTimerDriverState`，不必自己再维护逻辑 binding 影子表
- 这层 platform adapter 当前也已显式覆盖“创建失败”分支；真实桌面框架后续应在 native timer 注册失败时优先调用 failure ack，而不是把 pending 状态悬挂在适配层
- 平台 `native_timer_id` callback 当前也已显式覆盖 ignored 分支；真实桌面框架后续应优先消费 `dispatch_native_timer_elapsed_by_native_id(...)` 的 outcome，而不是把 stale/missing callback 当成异常流程
- 平台 start ack 当前也已显式覆盖 ignored + cleanup 分支；真实桌面框架后续应优先消费 `acknowledge_platform_timer_started(...)` 的 outcome 与 `follow_up_command()`，并在 stale/missing 分支立刻回收刚创建的 native timer
- 若平台 start ack 天然以批量 success/failure 回灌，真实桌面框架当前又可继续优先消费 `acknowledge_platform_timer_started_feedbacks(...)` / `acknowledge_platform_timer_start_failed_feedbacks(...)`，并在需要时直接用 `acknowledge_platform_timer_started_feedbacks_and_execute_follow_up_commands(...)` 复用批量 cleanup 顺序
- 若平台 timer API 本身是异步的，真实桌面框架当前又可继续优先消费 `dispatch_native_timer_elapsed_by_native_ids(...)` / `dispatch_due_native_timer_events_batch(...)` 这类非执行型 batch 结果，再统一执行其中 `native_timer_requests()`，不必自己拼循环与 request 抽取样板
- 若真实桌面框架希望把同一轮消息循环内的 start ack / fail ack / callback / due drain 一起提交，当前又可继续优先消费 `process_async_platform_round(...)`，复用 host 内已冻结的批处理顺序，而不是在框架层自己再拼一套 transaction glue
- 若真实桌面框架希望直接执行 host 返回的本轮平台动作，当前又可继续优先消费 `StudioGuiPlatformAsyncRound::actions()`，复用 host 内已冻结的 cleanup-before-request 顺序
- 若真实桌面框架同时具备“异步回灌 + 同步 timer 命令执行”两类平台特征，当前又可继续优先消费 `process_async_platform_round_and_execute_actions(...)`，把整轮回灌和动作执行一起压回 host glue，而不是在框架层重复保留批量 action loop
- 若平台 API 属于同步返回 `native_timer_id` 的风格，真实桌面框架后续可优先实现 `StudioGuiPlatformTimerExecutor`，并直接通过 `execute_platform_timer_request(...)` 复用 host 内已冻结的桥接顺序
- 若平台入口本来就是“收到 GUI 事件后立即调用同步 timer API”的风格，真实桌面框架当前又可继续优先消费 `dispatch_*_and_execute_platform_timer(...)` 这一层组合入口，不必在入口层重复保留 `dispatch + native_timer_request + execute` 三段样板
- 若平台入口天然以批量 callback 或一次性 due drain 的形式工作，真实桌面框架当前又可继续优先消费 `dispatch_native_timer_elapsed_by_native_ids_and_execute_platform_timers(...)` 与 `drain_due_native_timer_events_and_execute_platform_timers(...)`，把批量回灌后的最终 GUI-facing 视图固定在正式结果面里
- 平台失败当前也不再只是 adapter 内部状态；真实桌面 GUI 直接读取既有 runtime 日志区域就能看到平台 timer 注册失败
- 平台失败当前又补出显式 notice 通道，但仍复用既有 runtime 区域承载；当前不单独新造一套平台错误面板或额外窗口级 DTO
- 已把额外协作闸口写回正式规划：在 Studio 真正进入界面布局、交互流和较重 UI 逻辑设计前，需先向用户同步方向并留出干预窗口，不把产品交互方案静默推进成既成事实
- GUI 侧宿主动作当前正式形成“单一命令面 + 稳定结果类型”口径；后续真实桌面框架应优先围绕 `execute_command(...)` 建立事件桥，而不是分别持有多条 method-level 调用路径
- GUI 侧现在已形成“三层桥接”口径：`StudioGuiEvent` 负责平台事件词汇，`StudioGuiHostCommand` 负责宿主命令词汇，`StudioGuiCommandRegistry` 负责菜单/命令面板消费词汇
- drop preview 当前正式形成“stateless query + session preview + apply”三段式 GUI-facing 契约；后续真实桌面框架应优先围绕这三条入口接 hover/release，而不是回退到框架私有拖放影子状态
- drop preview 当前又进一步从“显式按钮 target”推进到“hover lane + panel header hit-test”口径；后续真实桌面框架应优先把 pointer hover/release 接到这层契约，而不是继续扩张临时按钮式交互
- drop preview 当前也开始形成 presentation 层真相源；后续真实桌面框架应优先消费 `window_model.drop_preview.preview_layout / changed_area_ids`，而不是自己从原始 layout state 做重复差分
- drop preview 当前也开始形成 overlay 真相源；后续真实桌面框架应优先消费 `window_model.drop_preview.overlay`，而不是自己从 `drop_target` 或 preview layout 重新拼目标高亮和插入提示
- drop preview 当前又进一步验证了“presentation/overlay 直接喂给 GUI”的方向可行；后续真实桌面框架应继续复用这份模型做局部高亮与插入指示，而不是在前端重算 target 语义
- drop preview 当前又进一步验证了 `anchor_area_id / target_tab_index` 这组字段足以支撑最小插入指示；后续真实桌面框架应继续复用这份正式口径，而不是再造一套 pointer-side 落点推导
- drop preview 当前又进一步证明 GUI 壳可以只消费正式 overlay 字段，就把 tab-strip 插入条、anchor 顶条和新 stack 局部占位直接画到目标位置；后续真实桌面框架应继续沿着“宿主给语义、前端只做轻呈现”的边界推进，而不是在前端重建布局预测逻辑
- GUI 侧当前又进一步形成第四层快捷键仲裁口径：`StudioGuiShortcutRouter` 负责在宿主命令、画布 `Tab` 接受和忽略原因之间做显式分流，而不是让平台框架自己猜测
- 画布 suggestion 当前也已正式纳入 Studio GUI driver 的应用层结果面，不再只是 `rf-ui` 内部状态实验；Studio 侧已经能够承接 suggestion 注入与 `Tab` 接受闭环
- 画布 suggestion 当前又进一步形成正式交互命令面；后续真实 GUI 不应再把“下一个 suggestion / 上一个 suggestion / 拒绝当前 suggestion”写成框架私有状态机
- suggestion 接受结果现在不再只是返回 `accepted` 对象；GUI-facing 结果已经能直接拿到最新日志反馈，便于后续接工具栏、状态栏或日志面板提示
- suggestion 接受的最小 UI follow-up 也不再隐含在内部状态里；当前正式口径是由 `AppState` 负责落 selection / inspector，再由 GUI-facing 结果显式带出 `applied_target`
- suggestion 接受的正式命令载荷当前也已冻结为 UI 状态的一部分；后续 LocalRules / `RadishMind` 若要支持真正落盘，必须生成可直接校验和写回的 `acceptance` payload
- 首批菜单路径、搜索词与默认快捷键当前也已冻结到 Studio 侧；后续真实 GUI 应优先消费这份 registry，而不是在各前端入口自行配置一套重复绑定
- 继续把第一条真实 `LocalRules` suggestion 生成链落到 `apps/radishflow-studio`：
- 新增 `studio_local_rules`，当前先覆盖 `flash_drum` 的 `inlet / liquid / vapor` 最小闭环建议生成
- `StudioAppHostController` 初始化与命令派发后都会自动刷新本地 suggestion，打开窗口和文档变化后不再只依赖测试手工注入
- `BootstrapSession::refresh_local_canvas_suggestions()` 当前只替换 `SuggestionSource::LocalRules`，保留其他 suggestion 来源，避免后续 `RadishMind` 或测试注入被整包覆盖
- `StudioGuiHost` / `StudioGuiDriverDispatch` 当前已正式暴露 `StudioGuiCanvasState`，GUI 可以直接读取当前 suggestion 列表与聚焦项
- `apps/radishflow-studio/src/main.rs` 当前也已把 `opened.canvas` / `dispatch.canvas` / `lifecycle.canvas` / `close.canvas` 接入稳定摘要输出，最小演示入口不再只停留在内部 dispatch 可见
- 继续把这条消费面从演示字符串提升为正式 GUI 契约：新增 `StudioGuiCanvasViewModel / StudioGuiCanvasTextView / StudioGuiCanvasPresentation`，并在 `StudioGuiCanvasState` 上冻结 `view()` / `text()` / `presentation()` 入口
- `main.rs` 当前已改为消费 `StudioGuiCanvasPresentation`，不再自行拼接 suggestion 文本；后续真实桌面 GUI 可直接复用同一份 view model 字段
- 继续把 canvas suggestion 从“可展示”推进到“可正式交互”：
- `rf-ui::CanvasInteractionState` 当前已补出 `focus_next` / `focus_previous`
- `AppState` 与 Studio 宿主链当前也已贯通 `focus next / focus previous / reject focused / accept focused by tab`
- `StudioGuiDriver` 当前已新增显式事件：
- `CanvasSuggestionAcceptRequested`
- `CanvasSuggestionRejectRequested`
- `CanvasSuggestionFocusNextRequested`
- `CanvasSuggestionFocusPreviousRequested`
- `StudioGuiDriverOutcome` 当前已把 suggestion 结果面提升为通用 `CanvasInteraction(...)`，不再只覆盖 accept
- 当前首轮 canvas suggestion 快捷键交互口径收为：
- `Tab` -> 接受当前聚焦 suggestion
- `Ctrl+Tab` -> 聚焦下一条 suggestion
- `Ctrl+Shift+Tab` -> 聚焦上一条 suggestion
- `Escape` -> 拒绝当前聚焦 suggestion
- 继续把这组交互口径提升为真实 GUI 可直接挂控件的契约：新增 `StudioGuiCanvasWidgetModel / StudioGuiCanvasWidgetEvent`
- 当前 widget 动作口径收为：
- `AcceptFocused`
- `RejectFocused`
- `FocusNext`
- `FocusPrevious`
- `StudioGuiCanvasWidgetModel::activate(...)` 当前直接产出稳定 `StudioGuiEvent`，真实桌面 GUI 不必再从 presentation 字段手工翻译事件
- `main.rs` 当前也已开始消费 widget 动作摘要，最小演示入口不再只展示 suggestion 文本
- 继续把 GUI 侧跨模块状态读取收口为单一快照：新增 `StudioGuiSnapshot / StudioGuiRuntimeSnapshot`
- `StudioGuiHost::snapshot()` 与 `StudioGuiDriverDispatch.snapshot` 当前会统一聚合：
- `app_host_state`
- `ui_commands`
- `command_registry`
- `canvas widget`
- `runtime control state`
- `run panel widget`
- `entitlement host output`
- `log entries`
- 后续真实桌面 GUI 可以优先围绕 `StudioGuiSnapshot` 建立窗口级状态绑定，而不是同时横跨 driver dispatch、canvas state、run panel 与 entitlement host 多个结果面取数
- 继续把这份原始 snapshot 收口成正式窗口分区模型：新增 `StudioGuiWindowModel`
- 当前窗口模型口径收为：
- `header`
- `commands`
- `canvas`
- `runtime`
- `StudioGuiSnapshot::window_model()` 与 `StudioGuiDriverDispatch.window` 当前都可直接产出这份区域化模型，真实 GUI 不必再自己把 snapshot 二次拆分成窗口布局语义
- `apps/radishflow-studio/src/main.rs` 当前也已优先消费 `window model` 摘要，最小演示入口不再主要依赖零散 `ui_commands` / `canvas` / runtime 字段逐段拼接输出
- 继续在 `window model` 之上冻结首版 `StudioGuiWindowLayoutModel`
- 当前布局模型口径收为：
- `titlebar`
- `panels`
- `center_area`
- `default_focus_area`
- 当前首版 dock 语义收为：
- `Commands` -> `LeftSidebar`
- `Canvas` -> `CenterStage`
- `Runtime` -> `RightSidebar`
- `main.rs` 当前会同时输出 `window model + layout model` 摘要，最小演示入口开始示范“内容状态 + 布局状态”双层 GUI 契约
- 继续把这份派生布局摘要推进为正式可持久化布局契约：新增 `StudioGuiWindowLayoutState`
- 当前正式布局状态口径收为：
- `scope + layout_key`
- `layout_slot`
- `panel visibility`
- `panel collapsed`
- `region_weights`
- `center_area`
- `default_focus_area`
- `StudioGuiSnapshot` 与 `StudioGuiWindowModel` 当前都会稳定携带 `layout_state`，GUI 后续接持久化时不必再从 `main.rs` 或零散字段临时反推
- 当前多窗口 layout scope/key 口径收为：
- 无窗口时使用 `studio.window.empty`
- 有窗口时按 `window_role + layout_slot` 生成独立 key，并继续带出运行时 `window_id`
- 当前稳定 key 口径收为：
- `studio.window.owner.slot-1`
- `studio.window.observer.slot-N`
- `layout_slot` 由窗口宿主层正式分配，observer 在同一会话内不会因其他 observer 关闭而整体重排
- 当前首轮默认区域权重收为：
- `LeftSidebar` -> `24`
- `CenterStage` -> `52`
- `RightSidebar` -> `24`
- 继续把布局状态从“只读派生”推进到“可更新的 GUI-facing 状态链”：
- 新增 `StudioGuiWindowLayoutMutation`
- 新增 `StudioGuiHostWindowLayoutUpdateResult`
- `StudioGuiHost` 当前开始按 `layout_key` 持有会话级布局覆盖，不再要求 GUI 每次修改后自己缓存一份影子状态
- 当前首轮正式布局变更入口收为：
- `SetCenterArea`
- `StackPanelWith`
- `PlacePanelInDockRegion`
- `SetPanelDockRegion`
- `SetPanelVisibility`
- `SetPanelCollapsed`
- `SetPanelOrder`
- `SetRegionWeight`
- `StudioGuiDriver` 当前已新增 `WindowLayoutMutationRequested` 事件与 `WindowLayoutUpdated(...)` 结果，真实桌面 GUI 后续可直接把停靠栏折叠/显隐/尺寸拖拽翻译到这条链
- `center_area` 与 panel `order` 当前也已接入同一条正式变更链，不再只是 sidecar 中“可存但不可正式更新”的字段
- `panel dock_region` 当前也已正式进入同一条变更链，并补出“跨区域移动后保持 `CenterStage` 不为空”的最小状态机规则
- 当前又已补出按区域插入的 `PlacePanelInDockRegion`，把“拖到某区域后落在谁前/谁后”冻结为正式 placement 契约，并由布局状态机统一重排 region 内顺序
- 当前又已补出正式 `stack_group` 状态与 `StackPanelWith` mutation，把“加入锚点所在 stack，并在 stack 内插到谁前/谁后”冻结为正式契约
- 当前又已补出每个 `dock_region + stack_group` 的 active tab 真相源，并新增 `SetActivePanelInStack` mutation，把“同一 stack 内切换当前激活 panel”收口到正式布局状态机
- `StudioGuiWindowLayoutModel` 当前也已开始正式暴露 stack group tabs 与 active tab，最小入口、host/driver 与 sidecar round-trip 已能消费这层语义
- `StudioGuiWindowPanelLayout` 当前也已补出 `Standalone / ActiveTab / InactiveTab` 展示角色，真实 GUI 后续不必自己再猜非 active tab 应按主面板还是次级 tab 渲染
- 当前又已补出 `ActivateNextPanelInStack` / `ActivatePreviousPanelInStack` 与 `UnstackPanelFromGroup`，把 tab strip 的最小循环切换和“从 shared stack 拆回独立 group”一并纳入正式 mutation
- 当前又已补出 `MovePanelWithinStack`，把“同一 stack 内 tab 顺序重排”也收口到正式 mutation，避免真实 GUI 为 tab strip 拖拽另写一套私有排序状态
- `main.rs` 当前也已开始演示最小布局变更派发，避免这条链只停留在测试中
- 当前又已补出 `StudioGuiWindowDropTarget` 摘要层，并由 `StudioGuiWindowLayoutState / Model` 提供只读推导入口
- 当前 drop target 摘要口径收为：
- `DockRegionGroup`：预览“落入某个 dock region 后形成独立 stack group”的语义
- `StackTab`：预览“并入既有 stack 并插入 tab strip 某个位置”的语义
- 当前 drop target 摘要至少显式带出：
- `dock_region`
- `anchor_area_id`
- `target_stack_group`
- `group_index / tab_index`
- `creates_new_stack / merges_into_existing_stack`
- `preview_active_area_id / preview_area_ids`
- 当前这层摘要直接复用既有 `PlacePanelInDockRegion / StackPanelWith / MovePanelWithinStack / UnstackPanelFromGroup` mutation 语义，不额外复制第二套拖拽状态机
- `apps/radishflow-studio/src/main.rs` 当前也已开始打印最小 drop target 摘要，避免这层契约只存在于测试中
- 当前又已补出正式 `StudioGuiWindowDropTargetQuery`，把 GUI 侧“按 hover/anchor/placement 请求 drop preview”的查询载荷冻结为显式契约
- `StudioGuiHost` 当前也已补出 `query_window_drop_target(...)` 与 `StudioGuiHostCommand::QueryWindowDropTarget`，让真实 GUI 不必再先抓 `layout_state` 自己拼 mutation 再反查摘要
- `StudioGuiDriver` 当前也已补出 `WindowDropTargetQueryRequested` 事件，并把查询结果继续收口到 `WindowDropTargetQueried(...)`，保持“平台事件 -> driver -> host command outcome”这条 GUI-facing 路径
- `apps/radishflow-studio/src/main.rs` 当前也已改为通过 `StudioGuiDriver` 的 drop target query 事件打印预览，不再直接从 `window.layout()` 读取只读摘要
- 当前又已把 query 结果进一步扩成 `preview_layout_state / preview_window`，让真实 GUI 在 hover 时可以直接消费预览态布局和窗口模型，而不必自己从 target 摘要反推整份 layout
- `StudioGuiHost` 当前又已补出 `apply_window_drop_target(...)` 与 `StudioGuiHostCommand::ApplyWindowDropTarget`，让 drop release 可以直接复用同一份 query 契约落地布局
- `StudioGuiDriver` 当前又已补出 `WindowDropTargetApplyRequested` 事件，并把落地结果继续收口到 `WindowDropTargetApplied(...)`
- `apps/radishflow-studio/src/main.rs` 当前也已把 stack / reorder / unstack 这些典型拖放动作改为通过 drop-target apply 事件执行，不再只演示 mutation 入口
- `StudioGuiHost` 当前又已补出轻量 preview 会话态，新增 `SetWindowDropTargetPreview / ClearWindowDropTargetPreview`，由 host 非持久化保存当前 hover 预览，不把这层状态写进 layout sidecar
- `StudioGuiDriver` 当前也已补出 `WindowDropTargetPreviewRequested / WindowDropTargetPreviewCleared` 事件，并把结果继续收口到 `WindowDropTargetPreviewUpdated(...) / WindowDropTargetPreviewCleared(...)`
- `StudioGuiSnapshot` 与 `StudioGuiWindowModel` 当前又已直接携带 `drop_preview`，真实 GUI 后续可直接从正常 `snapshot/window_model` 读取当前 hover 预览，而不必自己维护影子布局缓存
- `StudioGuiWindowModel.drop_preview` 当前又已继续收口为 GUI-facing presentation，直接携带 `preview_layout + changed_area_ids`，让真实 GUI 不必自己从当前态/预览态重建整份布局或手工算变化集
- `StudioGuiWindowModel.drop_preview` 当前又已继续补出 `overlay`，直接携带目标 region/stack、tab 插入位、目标 tabs 与高亮 area 集，让真实 GUI 不必再从 `drop_target + preview_layout` 手工拼 overlay 提示
- `apps/radishflow-studio/src/main.rs` 当前也已改为通过 preview 事件打印 hover 预览，并在演示后显式清理 preview，会话态消费路径不再只存在于测试中
- 继续把这条链推进到真实持久化：新增 `rf-store` 侧 `StoredStudioLayoutFile`
- 当前布局 sidecar 路径口径收为：
- `<project>.rfproj.json` 同目录下的 `<project>.rfstudio-layout.json`
- 当前 sidecar 只保存真正需要恢复的用户偏好：
- `layout_key`
- `center_area`
- `panel visibility/collapsed/order`
- `region_weights`
- 不把 `default_focus_area` 这类运行时派生焦点直接落盘，避免把瞬时状态误写成持久偏好
- `StudioGuiHost::new(...)` 当前会在有 `document_path` 时自动加载 sidecar，`update_window_layout(...)` 后会立即写回 sidecar
- 当前已补出“更新布局 -> 写 sidecar -> 重建 host -> 恢复布局”的跨会话测试闭环
- 新增 Studio 级测试，验证加载缺失 `flash_drum` 连接的项目后会自动生成 inlet/liquid/vapor 三条本地 suggestion
- 新增 `StudioGuiCanvasPresentation` 级测试，锁定“真实 `StudioGuiDriverDispatch.canvas` -> GUI 摘要文本 / view model”这条消费链，避免后续展示层退回只读内部状态
- 新增 `rf-ui` 与 Studio 级测试，锁定 suggestion 聚焦轮转、拒绝后的焦点推进，以及显式事件 / 快捷键到 driver outcome 的完整闭环
- 新增 widget 级测试，锁定 action availability、`widget event -> StudioGuiEvent` 映射，以及 `widget event -> driver dispatch` 的真实消费链
- 新增 snapshot 级测试，锁定 `StudioGuiDriverDispatch.snapshot` 会稳定聚合 command registry、canvas widget 与 runtime-facing 状态
- 新增布局状态级测试，锁定 dock 区域映射、默认区域权重、空工作区 key，以及多窗口独立 scope key
- 新增布局更新级测试，锁定 panel visible/collapsed、region weight 的写回，以及不同窗口 scope 间的隔离
- 继续扩展布局更新级测试，锁定 `center_area` 与 panel `order` 的事件派发、落盘恢复和排序一致性
- 新增 `rf-store` 与 Studio 级持久化测试，锁定 sidecar 文件名、JSON round-trip、落盘读回以及 host 重建后的布局恢复
- 继续把布局 key 从基于运行时 `window_id` 的临时 key 收敛为基于稳定 `layout_slot` 的 key，并补出旧 sidecar key 的兼容读取
- 新增测试，锁定 observer 槽位在同会话关闭同伴窗口后不漂移，以及 legacy `studio.window.owner.1` sidecar 在新 key 口径下仍可恢复
- 继续扩展布局更新级测试，锁定 `dock_region` 跨区域移动、sidecar round-trip 与 host/driver 恢复链的一致性
- 继续扩展布局更新级测试，锁定 region 内 stack placement、锚点前后插入和 `main.rs` 最小演示入口的消费链
- 继续扩展布局更新级测试，锁定 `stack_group` 的持久化、独立 group 到 shared stack 的切换，以及 host/driver 对 stack query 的消费链
- `apps/radishflow-studio` 当前已引入 `eframe/egui`，把原来的控制台演示入口替换为第一版真实 GUI 宿主壳
- 这版 GUI 当前已直接消费 `StudioGuiDriver / StudioGuiSnapshot / StudioGuiWindowModel`，能渲染标题栏、逻辑窗口切换、命令区、画布建议区与运行区，而不再只打印文本摘要
- 这版 GUI 当前也已开始真实消费 `RunPanelWidgetModel`、`StudioGuiCommandRegistry`、`StudioGuiCanvasWidgetModel` 与 `StudioGuiWindowLayoutMutation`，至少能执行运行栏命令、画布 suggestion 动作、面板显隐/折叠、跨区域移动、stack/unstack、tab 前后切换与 region weight 调节
- 这版 GUI 当前又已开始直接消费 `WindowDropTargetPreviewRequested / WindowDropTargetPreviewCleared / WindowDropTargetApplyRequested`，形成“pick up panel -> hover drop target -> 预览 -> click apply -> 自动清理 preview”的最小 drop preview 生命周期
- 这版 GUI 当前已把 drop target 从“顶栏按钮”下沉到 region lane、tabbed stack lane 与 panel header，更接近真实 pointer hover 命中层
- 当前 hover 命中会直接驱动 preview query，鼠标离开所有 target 时会清理 preview，click 会直接 apply 对应 drop target
- preview 清理当前也已按 active preview 的真实所属窗口归位，避免拖拽过程中切换逻辑窗口后把 clear 事件误发到错误 scope
- 这版 GUI 当前又已开始直接消费 `window.drop_preview.overlay / changed_area_ids` 做展示层高亮，target region、target stack 与受影响 panel 不再只停留在顶栏摘要里
- 当前预览摘要也已从 debug 风格 `{:?}` 文本收口为 GUI 可读的区域/stack/changed panel 摘要，便于后续继续替换成真正的插入指示与局部 overlay
- 这版 GUI 当前又已把 `anchor_area_id / target_tab_index` 继续前推成显式插入提示：target stack 会显示 `insert before/after ...`，anchor tab/panel 会明确打标
- 这版 GUI 当前又继续把插入提示压到真实落点上：target stack 的 tab strip 会直接绘制局部插入竖条，panel header 会直接绘制 anchor 顶部插入横条与 changed/highlight overlay
- 若 hover preview 会创建新 stack，这版 GUI 当前也会按 `target_group_index` 在对应 dock region 内插入局部占位 overlay，而不再只在区域顶部显示泛化提示
- 顶栏上的 preview 文本当前也已降权为紧凑状态提示，拖拽时的主提示改为 pointer 附近的浮动 overlay，避免用户必须来回看标题栏才能判断当前落点
- 上述浮动 overlay 当前又已继续收口为“优先贴住真实 target rect”：anchor panel header 的优先级最高，其次是 target stack tab strip / new stack 占位，只有取不到目标 rect 时才回退到 pointer 偏移
- target stack 顶部原先额外占行的 `insert before/after ...` 文案当前也已继续下沉为贴在插入竖条 / anchor 顶线附近的局部 pill，进一步减少“看说明行”而改为“看落点本身”
- 继续为这批 shell 侧 preview helper 补出最小测试，当前已锁定插入邻居推导与 overlay 定位钳制，避免后续 GUI 壳继续迭代时把局部插入语义悄悄弄偏
- 这版 GUI 当前也已开始直接消费 `StudioGuiNativeTimerRuntime` 的到点排空能力，让 entitlement timer 不再只停留在控制台样例里的人工演练
- 当前 GUI 壳又已把 entitlement panel 动作从只读状态 chip 推进为正式按钮事件，新增 `EntitlementPrimaryActionRequested / EntitlementActionRequested`
- 上述 entitlement GUI 动作当前会先经过 `StudioGuiDriver / StudioGuiHost` 的正式事件链，再复用既有 foreground window 与 `StudioRuntimeTrigger::{EntitlementWidgetPrimaryAction, EntitlementWidgetAction}` 语义回到 runtime，不在壳层绕过宿主边界直调控制面逻辑
- 已同步更新 `docs/architecture/overview.md`、`docs/mvp/scope.md` 与 `docs/radishflow-mvp-roadmap.md`，把 GUI-facing 窗口布局契约、sidecar 持久化边界与下一步 dock 契约推进方向写回正式文档
- 今天收尾阶段又已回顾当日四个 Studio 提交，并把“第一版 `eframe/egui` GUI 壳已直接消费 `drop_preview.overlay`、当前仍保持单原生窗口宿主”这两条正式口径同步写回设计/规划文档，避免周志与真相源文档继续漂移
- 今天收尾阶段又已继续把 runtime 区剩余卡片往更真实的 GUI-facing 宿主反馈面收口，新增 `host_actions / platform_timer_lines / gui_activity_lines` 这组三层 DTO，不再主要依赖 `egui` 壳私有字符串缓存 scheduler、平台 timer 与活动记录
- `StudioGuiPlatformHost` 当前也已把平台 timer request、执行结果、ignored callback 与失败路径统一前推到 `gui_activity_lines`，并把当前 schedule/native binding 摘要继续前推到 snapshot/window model；runtime 区的 `Platform / GUI activity` 卡片已开始直接消费正式宿主输出
- 今天收尾阶段也已把当日新增这笔 runtime host feedback 收口同步写回 `docs/radishflow-mvp-roadmap.md`，让设计/规划/周志三处口径保持一致
- 今天又继续把第一版 `eframe/egui` GUI 壳往真实宿主 glue 推进一步：
- `EguiPlatformTimerExecutor` 不再只分配 `native_timer_id`，而是会保存 native timer schedule、按到期时间排空，并以真实 `native_timer_id` 回灌 `StudioGuiPlatformHost`
- `studio_gui_shell` 当前会优先按 executor 侧 native timer 注册表请求下一次 repaint，不再直接把平台侧 due 判定偷看回 `platform_host.current_platform_timer_binding()`
- `studio_gui_shell` 当前也已接上 viewport focus 变化；原生窗口重新获得焦点时会自动派发 `WindowForegrounded`，不再只依赖标题栏按钮演示这条生命周期事件
- panel header 当前也已支持 `click_and_drag` 触发最小拖放会话；hover target 继续走既有 query/preview 语义，pointer release 命中 preview 时会自动 apply，不再只支持“先 pick up 再点一下落点”这一条更偏演示态的路径
- 已补充 shell 级测试，锁定 executor 按 native schedule 排空 due timer 的行为，并确认现有 due callback -> rearm 闭环保持稳定
- `studio_gui_shell` 当前又已接上 root viewport 的 `close_requested`；原生窗口关闭请求会先转成 `CloseWindowRequested` 走现有 host/driver 状态机，只有逻辑窗口已清空时才继续关闭 root viewport
- 拖放生命周期当前又已补上 `Escape` 取消路径；有活跃 drag session 时，shell 会优先清理 preview/session，而不是继续把 `Escape` 下发给普通快捷键路由
- tab strip 当前也已成为正式拖放起点；shell 会在 tab `drag_started()` 时直接启动 panel drag session，不再只支持从 panel header 或显式 `Pick up` 按钮起拖
- panel 工具行中的 `Pick up` 演示按钮当前已移除，改为直接提示“Drag header or tab to move”，让移动入口继续向真实 GUI 交互收口
- 标题栏当前也已移除 `Login completed / Network restored / Foreground current / Close window` 这组更偏演示态的按钮，只保留 `New logical window` 与逻辑窗口 chip
- 逻辑窗口切换/关闭当前都已并入 chip 行为：点击 chip 聚焦目标逻辑窗口，点击 chip 右侧 `x` 走 `CloseWindowRequested`，不再把窗口管理拆成额外标题栏控制条
- `StudioGuiWindowRuntimeAreaModel` 当前也已移除 `host_actions` 这组仅供 shell 调试按钮消费的 DTO；runtime 窗口模型继续只保留 run panel、entitlement host、platform notice、timer lines、activity lines 与日志
- `studio_gui_shell` 当前的 Scheduler 区也已改成纯状态/说明文本，不再额外消费一组“模拟 LoginCompleted / NetworkRestored / TimerElapsed”的运行期按钮
- 继续把 GUI command surface 的 shell 级等价测试补齐：`run_panel.set_active`、`run_panel.recover_failure`、`canvas.accept_focused`、`canvas.reject_focused`、`canvas.focus_next`、`canvas.focus_previous`、命令列表与快捷键这批入口当前都已锁到共享 command surface；disabled 状态下 menu / toolbar / palette 也已正式锁定为 no-op
- 继续把 `apps/radishflow-studio/src` 的超长文件治理落到正式目录边界：`bootstrap`、`studio_gui_shell`、`studio_gui_host`、`studio_gui_driver`、`studio_gui_window_layout`、`studio_window_host_manager`、`entitlement_session_host`、`property_package_download_client`、`auth_cache_sync`、`app_facade` 与 `control_plane_client` 当前都已拆成浅层子目录模块，并保持最小模块测试与 `cargo check` 通过
- 今天收尾阶段又已回顾当日 command surface 主线与工程治理提交，并把“GUI 入口 disabled no-op 语义已冻结”“`src/` 正在按职责做浅层目录治理”这两条正式口径同步写回 `docs/architecture/app-architecture.md` 与 `docs/radishflow-mvp-roadmap.md`

## 风险与未完成项

- 当前仍是“GUI 宿主适配层已落地”，不是“真实桌面框架已完全接通”；原生窗口事件源、后台任务宿主和真正的 UI 控件绑定还未接入
- `main.rs` 目前仍是最小 bootstrap / 演示入口，尚未替换为最终的图形界面宿主
- 真实 GUI 后续仍需决定如何把窗口创建销毁、原生 timer 生命周期与后台任务入口接到 `StudioGuiHost`
- 当前 native timer 已形成 GUI-facing runtime，但真实 GUI 框架的实际 timer handle 创建/释放与平台事件循环仍未正式接线
- 当前平台 timer request 已形成正式宿主适配层，但真实 GUI 仍需把各框架自己的 timer API 映射到 `Arm / Rearm / Clear`，并决定 OS/native handle 与当前 `handle_id` 的关联存放位置
- 当前平台 adapter 已形成“request -> native timer command -> callback 反查”的最小闭环，但真实 GUI 仍需把各框架自己的实际窗口句柄、消息循环和 timer 注册失败路径接进来
- 当前平台 adapter 已形成“request -> native timer command -> start success/failure ack -> callback 反查”的最小闭环，但真实 GUI 仍需把各框架自己的实际窗口句柄、消息循环和错误上报路径接进来
- 当前统一命令面已经建立，但真实 GUI 的事件循环、原生窗口句柄与平台 timer callback 仍未正式桥接到这条命令面
- 当前虽然已有第一版 `eframe/egui` GUI 宿主壳，但仍是“单原生窗口承载逻辑窗口切换”的阶段，还没有进入真正的多原生窗口宿主
- 当前 command registry 已是 GUI-facing 结构，但快捷键默认值、菜单树布局和命令面板搜索字段仍未冻结
- 当前默认绑定已经冻结到 registry，但真正的平台级快捷键冲突策略、焦点竞争规则和用户自定义覆盖仍未建立
- 当前窗口模型已经把 snapshot 整理成区域语义，但还没有进一步冻结真实桌面 GUI 所需的窗口布局状态、面板显隐与尺寸持久化契约
- 当前布局状态已接上 sidecar 持久化，并已从 `window_id` 收口到稳定 `layout_slot`；如果后续要支持更复杂窗口编排、命名工作区或显式多视图类型，仍需要继续扩展 scope 语义
- 当前 `dock_region`、`stack_group`、stack active tab、tab strip 循环切换、stack 内重排、独立 group 拆分与 region 内 placement 已进入正式 mutation，但更复杂的 dock 编排，例如 tabbed group 标题/可关闭策略、拖拽预览落点细分与跨窗口布局模板仍未建立
- 当前 `drop target` 仍是“摘要推导层 + 轻量 preview 会话态”，不包含真实 pointer hit-test、hover 生命周期状态机、拖拽占位指示条或跨窗口拖放编排
- 当前已明确的下一层边界是“GUI 可显式发 query / set preview / clear preview / apply”；这仍不等于完整 pointer drag lifecycle 状态机
- 当前 `Tab` suggestion 接受已能覆盖 material connection 的最小正式写回，但更复杂的 suggestion 形态、批量接受和更多文档命令族仍未建立
- 当前自动 suggestion 刷新只覆盖 `LocalRules`；未来若接入 `RadishMind` 并需要排序合流，还需要进一步冻结多来源 suggestion 合并策略
- 当前 runtime 区虽然已把宿主动作、平台 timer 状态和活动记录前推到正式 DTO，但 shell 内仍保留少量纯展示态辅助状态；后续继续推进真实桌面框架接线时，仍要警惕这部分再次长成第二套影子真相源
- `apps/radishflow-studio/src/studio_app_host.rs` 与 `apps/radishflow-studio/src/studio_gui_platform_host.rs` 当前仍超过单文件阈值；这两者已不再适合继续用“只外提测试”的轻量手法，下一轮需要按职责明确切子模块

## 下周建议

- 在现有 `StudioGuiHost` 之上继续接真实桌面框架的窗口生命周期、timer 句柄管理与 hover hit-test 输入
- 在现有 `StudioGuiPlatformHost` 之上接真实桌面框架 timer API，把 `native_timer_request` 映射为具体平台 `set_timer/kill_timer/rearm` 调用
- 在现有 `StudioGuiPlatformTimerDriverState` 之上接真实桌面框架 timer 注册/释放 API，并明确 native timer 创建失败时如何把平台错误回写到 Studio 宿主层
- 把首批稳定 command id 接到菜单、快捷键或工具栏绑定，而不是继续只在最小入口中打印
- 在 `StudioGuiWindowModel` 之上继续补真实窗口布局模型，例如 panel 可见性、dock 区域、当前 drop preview 与窗口标题栏状态，而不是让 GUI 自己再发明一层布局状态机
- 在现有 runtime host feedback DTO 基础上，继续把 shell 内剩余宿主私有展示状态压回正式 snapshot/window model，避免未来真实桌面框架复制 `Platform / GUI activity / Scheduler` 的第二套影子状态
- 让 layout model 从“派生摘要”继续推进到“可持久化窗口布局状态”，补面板折叠、尺寸比例和多窗口独立布局键
- 在不扩张架构层级的前提下，继续补 GUI 侧运行结果派发和 entitlement notice 的正式消费面
- 在现有 `flash_drum` 最小闭环之上继续扩展更多本地规则，但保持以 canonical port 和正式文档写回校验为边界
- 把 `studio_app_host` 与 `studio_gui_platform_host` 作为下一轮工程治理优先项，先切出最小可验证职责模块，再继续推进 GUI 宿主主线
