# 2026-W19 Development Log

周期：2026-05-04 至 2026-05-10

## 本周当前摘要

- 当前默认恢复上下文入口已收口到 `docs/status/current.md`，不再要求新会话默认读取 `overview.md`、`scope.md` 和整篇当前周志。
- 本周主线已从 Canvas 最短路径收口转回数值与结果审阅：常热容显热焓、phase comparison、unit / stream 上下游审阅和诊断定位已经进入结构化 DTO。
- 2026-05-06 已完成 Docs 入口收口、mole fraction 数值契约收紧，以及 Stream Inspector composition normalize / discard / add / remove 的正式 command surface；下一步优先回到 `SolveSnapshot`、`rf-thermo`、`rf-flash` 和结果消费面的可验证缺口。

## 本周目标

- 把主线从 Canvas 只读扫读层继续推进到“放置单元后补齐连接/必要流股”的最短可操作路径。
- 优先验证 `Feed -> Flash Drum` 这一条最小建模闭环，不扩完整画布编辑器、拖拽布局、自由连线或结果导出。
- 继续保持 `cargo check` 与相关 focused tests 稳定。

## 完成情况

- 2026-05-04：补出未绑定 `Feed.outlet` 的本地 Canvas suggestion，可创建默认 source stream，并在已有组分体系下给新 feed stream 初始化等摩尔组成与基础 `T / P / F`，让后续 Stream Inspector 可直接继续调整参数。
- 2026-05-04：Flash Drum 入口连接建议现在会排除已有 `Flash Drum.liquid/vapor` terminal product streams，只把真正可继续下游连接的 source-only stream 作为候选，避免在已有完整示例项目中误判入口来源。
- 2026-05-04：`CanvasSuggestionAcceptance::MaterialConnection` 不再要求整张 flowsheet 立即通过完整连接校验；当前改为校验本次 source/sink/stream 的局部一致性，允许用户按顺序补齐仍有未绑定端口的半成品流程。
- 2026-05-04：Canvas pending placement 提交成功后会立即刷新本地 suggestions，因此用户放置 `Feed` 后能立刻看到创建 outlet stream 的下一步建议。
- 2026-05-04：新增回归测试锁定 `Feed -> Flash Drum` 最短路径：放置 Feed、接受 Feed outlet 建议、放置 Flash Drum、依次接受 inlet / liquid / vapor 三个建议，最终 Canvas 暴露三条新 material stream line 且项目进入未保存状态。
- 2026-05-04：继续把同一套本地 suggestion 规则扩到 `Heater / Cooler / Valve` 这类一进一出单元：入口只在唯一可连接 source-only stream 存在时建议连接，出口会创建下一段 source stream，并按单元类型给 outlet 模板设置基础 `T / P`。
- 2026-05-04：新增 `Feed -> Heater -> Flash Drum` shell 回归路径，覆盖放置 Feed、创建 Feed outlet、放置 Heater、连接 Heater inlet、创建 Heater outlet、放置 Flash Drum、补齐 Flash inlet / liquid / vapor，并在最后触发 `run_panel.run_manual` 后确认求解收敛。
- 2026-05-04：继续补出 `Mixer` 两进一出本地 suggestion：只有可连接 source-only stream 数量与未绑定 mixer inlet 数量一致时才按确定性顺序建议 `inlet_a / inlet_b`，source 数量不匹配时只保留 outlet 创建建议，避免多来源场景静默猜错入口。
- 2026-05-04：新增 `Feed + Feed -> Mixer -> Flash Drum` shell 回归路径，覆盖两个 Feed outlet、Mixer 两个 inlet、Mixer outlet、Flash inlet / liquid / vapor 全部通过 suggestion 补齐，并在最后手动运行求解收敛。
- 2026-05-04：继续补齐真正空白项目的前置缺口：Studio bootstrap 会在项目无组件时初始化 MVP 默认二元组件 `component-a / component-b`，并用初始化后的 flowsheet 生成本地 `binary-hydrocarbon-lite-v1` 物性包缓存；新建 Feed source stream 因此会持久化 `T=298.15 K / P=101325 Pa / F=1 mol/s / z=[0.5, 0.5]` 默认值。
- 2026-05-04：新增空白项目最短闭环回归：从空白 `*.rfproj.json` 打开放置 `Feed -> Flash Drum`、接受本地 suggestions、手动运行收敛、保存、重新打开，再次手动运行收敛，并确认保存文件中组件与 Feed source stream 默认组成完整落盘。
- 2026-05-04：继续补出 Canvas suggestion 显式选择入口：每条可写回的本地建议都会暴露独立 Apply 动作，用户可不依赖当前 focused suggestion，直接按 suggestion id 接受指定连接或 outlet stream 创建；新增 core / widget / shell 回归锁定非 focused suggestion 也能显式接受并运行 `Feed -> Flash Drum`。
- 2026-05-04：继续补出 Canvas placement 坐标最小持久化：`CommitPendingEditAt` 的落点会写入项目同目录 `<project>.rfstudio-layout.json` sidecar，重开项目后 Canvas 先使用已保存坐标渲染单元块；新增回归锁定 `Feed -> Flash Drum` 保存、重开、位置恢复、继续 Apply suggestions 并运行收敛。
- 2026-05-04：继续补出 Active Inspector 的单元最新执行结果审阅：选中已运行单元时，Runtime 详情会显示来自最新 `SolveSnapshot` 的执行状态、step 序号、summary 和产出流股跳转；新增 window model / shell locale 回归锁定该 presentation。
- 2026-05-04：收口 Studio layout sidecar 读取边界：项目同目录 `.rfstudio-layout.json` 缺失时按空布局处理，避免测试并发或用户清理 sidecar 后阻塞项目打开。
- 2026-05-04：继续收口结果审阅导航：`StudioGuiWindowSolveStepModel` 现在携带单元与产出流股 command action，Runtime 全局求解步骤、Active Inspector 关联步骤和 Result Inspector 关联步骤都能复用同一跳转 presentation。
- 2026-05-04：继续补齐失败路径定位：`StudioGuiWindowFailureResultModel` 现在携带 recovery action 与 recovery target action，失败摘要可直接触发既有修复命令或定位到相关 Inspector 目标。
- 2026-05-04：继续把结果审阅/错误定位动作汇总成统一 presentation：新增 `StudioGuiWindowDiagnosticTargetActionModel`，失败摘要、Result Inspector、Active Inspector 与求解步骤都会暴露同一类诊断目标 action，shell 只负责渲染并继续通过既有 command id 派发。
- 2026-05-04：继续补出失败详情结构化展示：`WorkspaceControlState` 会带出 latest diagnostic summary，失败结果 presentation 可直接显示 revision、severity、primary code、diagnostic count、相关 unit / stream / port target；port target 仅复用所属 unit 的 Inspector command，不新增端口级命令或 message 反解析。
- 2026-05-04：开始收口布局编辑边界：新增 `CanvasUnitLayoutMoveRequested -> MoveCanvasUnitLayout` GUI-facing 事件，只验证单元存在并更新 Canvas unit position sidecar；真实 shell 对当前选中且已有 sidecar 坐标的单元提供离散 nudge 按钮，不修改项目文档、不进入 revision/history。
- 2026-05-04：继续把 Canvas layout nudge 从 shell 私有按钮前推到正式 command surface / widget action：新增 `canvas.move_selected_unit.left/right/up/down`，选中单元后会生成 `Move left/right/up/down` 命令，shell 只派发 command id；缺少 sidecar 坐标时先按当前 transient grid slot pin 出初始位置，再写入 `<project>.rfstudio-layout.json`。
- 2026-05-04：继续补出 Canvas layout state 摘要：选中单元的 selection presentation 会明确显示当前来自 `sidecar position` 还是 `transient grid`，首次 nudge 的 command result 会说明“先从 transient grid slot pin 到 sidecar，再移动”，避免用户误以为项目文档几何被修改。
- 2026-05-04：继续把失败诊断与 Canvas attention 联动：Canvas unit / stream hover、material port hover 和对象列表 attention summary 会从结构化 `related_port_targets` 生成只读摘要，例如 `ports unit:port` 与 diagnostic code，不从错误 message 反解析，也不新增端口点击编辑、端口级命令或错误处理状态机。
- 2026-05-04：继续把同一套 port attention 摘要接到 Active Inspector 的 unit port 列表：选中相关单元后，对应 port 行会显示来自 `DiagnosticSummary.related_port_targets` 的只读 `attention` 摘要；端口行仍只保留既有 stream Inspector 跳转，不新增 port command。
- 2026-05-04：Result Inspector 当前补出第二个消费维度——unit-centric 视图：`StudioGuiWindowResultInspectorModel` 现在同时携带 `unit_options / selected_unit_id / selected_unit / unit_related_steps / unit_related_diagnostics / unit_diagnostic_actions / has_stale_unit_selection`；新增 `result_inspector_with_unit(stream_id, comparison_stream_id, unit_id)` 入口，未指定单元时按求解步骤顺序取首个 unit，缺失/失效单元会回落到首个 unit 并把 `has_stale_unit_selection` 置为 true。`egui` Runtime 面板在结果区流股详情下新增 `Unit results` 折叠区，展示 unit selector、最新 unit execution result、关联求解步骤、关联诊断与诊断目标动作；shell 仅消费 DTO 并继续派发既有 `inspector.focus_unit:*` / `inspector.focus_stream:*` 命令，不引入新的 unit 编辑或导出能力。
- 2026-05-04：Result Inspector unit-centric 视图与 stream-centric 视图共存为同一 snapshot 下的两个并列消费面：unit selector 与 stream selector 互不联动，`ResultInspectorState` 的 unit selection 只在 snapshot 切换或单元从快照消失时复位，不会因为切换流股或对比流股被清理；unit-related diagnostics 复用 produced stream id 集合补出与该 unit 输出相关的诊断条目，避免与 `Active Inspector` 单元详情形成第二套规则。
- 2026-05-04：继续治理 Studio window model 文件体积：将超长 `studio_gui_window_model.rs` 拆为 `studio_gui_window_model/mod.rs`、`drop_preview.rs`、`result_inspector.rs` 与 `tests.rs`，保持对外模块名和 DTO/API 不变；拆分后该模块内单个源码文件均低于 1500 行。
- 2026-05-04：继续完成仓库源码文件体积治理：将 `studio_gui_command_registry`、`studio_gui_canvas_presentation`、`studio_gui_platform_host`、`studio_gui_shell` tests/panels、`studio_app_host`、`rf-solver` tests、`rf-ui` tests/state 以及 `.NET` CAPE-OPEN Unit Operation / ContractTests 超长文件按职责拆成浅层子模块或 partial 文件；当前扫描范围内源码文件均低于 1500 行。
- 2026-05-05：继续收口 Result Inspector 导航入口：stream result option 与 comparison stream option 现在和 unit option 一样显式携带 `focus_action`，`egui` Runtime 面板会在流股/单元结果选择旁直接渲染该 action，并统一派发既有 `inspector.focus_stream:*` / `inspector.focus_unit:*` command；新增窗口模型回归锁定流股结果选项、对比流股选项和单元结果选项都暴露同一套 Inspector focus command。
- 2026-05-05：继续补出 driver 级 diagnostic action 派发一致性回归：成功路径会从 Result Inspector stream/unit actions、求解步骤 actions 与 Active Inspector actions 逐个派发到既有 Inspector focus command；失败路径会校验 failure diagnostic actions 只允许走 `run_panel.recover_failure` 或 `inspector.focus_*`，并分别确认 focus 不触发修复、recovery 才推进文档修复和 Dirty/Resume 状态。
- 2026-05-05：继续锁定 Canvas layout nudge 的非文档编辑边界：新增 shell 回归覆盖无 sidecar 坐标的已有单元通过 `canvas.move_selected_unit.right` 从 transient grid slot 首次 pin 到 sidecar，再执行 nudge；测试同时确认项目 JSON 原文不变、document revision / saved revision / dirty 状态不变、command result 明确显示 transient grid pin，并且重开项目后从 `<project>.rfstudio-layout.json` 恢复新坐标。
- 2026-05-05：本轮收口后执行 `pwsh ./scripts/check-repo.ps1` 仓库级验证；首次失败仅为若干拆分后源码文件残留 CRLF 换行，统一归一化为 LF 后复跑通过 text checks、`cargo fmt --all --check`、`cargo check --workspace`、`cargo test --workspace` 与 `cargo clippy --workspace --all-targets -- -D warnings`。
- 2026-05-05：结束 Canvas layout 兜底后转回数值主线，补出 `rf-thermo` 常热容基础相焓：`PlaceholderThermoProvider::phase_molar_enthalpy` 现在按物性包中的 liquid/vapor heat capacity 计算相对 `298.15 K` 的显热焓，并补充非有限温压/组成校验。
- 2026-05-05：`rf-flash` 的 `TP Flash` 结果现在会为 liquid/vapor phase 写入 `molar_enthalpy_j_per_mol`，并按相分率给 overall phase 写入加权 molar enthalpy；`Flash Drum` 拆分液/汽出口时会继续把相焓传递到 outlet stream phases。`tests/thermo-golden` 与 `tests/flash-golden` 已补齐热容和焓值 golden 断言。
- 2026-05-05：继续把基础焓值接到 Studio 结果消费面：`StudioGuiWindowStreamResultModel` 现在会从 overall phase 提取 molar enthalpy 并作为 `H / Molar enthalpy` summary row 暴露，phase table 继续显示各相焓；Result Inspector 的 stream comparison 在两侧都有焓值时会显示焓差，中文 shell 标签补出“摩尔焓”。
- 2026-05-05：继续收口 Result Inspector 的相结果比较边界：stream comparison 现在新增正式 `phase_rows` DTO，按同名 phase 比较 phase fraction 与 molar enthalpy，shell 只消费 DTO 渲染，不在界面层反解析 phase 文本或重新计算热力学。
- 2026-05-05：继续补齐 Result Inspector 单元结果审阅的最小结构化缺口：`rf-ui::StepSnapshot` 现在保留 solver 已有的 consumed stream 快照，Studio solve step / unit execution result DTO 同时暴露 consumed stream ids 与 Inspector focus actions；Result Inspector unit-centric 视图、Active Inspector 最新单元结果和求解步骤展示都能按“输入流股 -> 单元 -> 产出流股”审阅当前快照，不新增报表、导出、完整结果表格或 Canvas 连线编辑能力。
- 2026-05-05：继续收口 Result Inspector unit-centric 诊断归集：选中单元的关联诊断现在同时匹配该单元当前 step 的 consumed streams 与 produced streams，因此入口流股相关诊断也能出现在单元结果审阅中；诊断 action 仍只复用既有 `InspectorTarget` focus command，不新增诊断处理状态或结果报表。
- 2026-05-05：继续补齐 stream-centric 审阅的上下游上下文：Result Inspector 和 Active Inspector 的 stream related steps 现在同时匹配 consumed streams 与 produced streams，选中中间流股时可看到上游产出单元和下游消费单元的同一套 solve step presentation；空状态文案同步从“无产出步骤”调整为“无消费或产出步骤”。
- 2026-05-05：继续把焓值可读性补到 Result Inspector 选择器：stream result option 与 comparison stream option 的 hover summary 在流股已有 overall molar enthalpy 时追加 `H ... J/mol`，无焓值的流股继续只显示 `T / P / F`，避免把未物化焓值伪装成结果。
- 2026-05-05：继续把单元上下文补到 Result Inspector 选择器：unit result option 的 hover summary 现在会显示当前最新 step 的输入流股与产出流股，例如 `in stream-feed | out stream-heated`，只消费已结构化的 solve step ids，不新增单元结果表格、导出或 Canvas 连线语义。
- 2026-05-05：继续收口 Result Inspector stream comparison 导航一致性：comparison DTO 现在为 base stream 与 compared stream 显式携带 Inspector focus action，shell 在比较正文旁直接渲染这两个既有 `inspector.focus_stream:*` command；这只补结果审阅定位入口，不新增比较报表或导出。
- 2026-05-06：更新协作文档与 Docs 入口约束：新会话默认只读取最小入口文档，关键入口文档应保持简约并只描述当前阶段、最近进度、稳定边界和下一步；同时补充语言与代码实践约束，禁止新增含义不明的方法、类型或晦涩抽象封装，要求新增抽象必须服务明确领域职责、稳定边界或真实重复治理。
- 2026-05-06：新增 `docs/development/code-style.md`，将语言与代码实践约束下沉为专题规范；`AGENTS.md`、`CLAUDE.md` 与 `docs/README.md` 只保留索引和高层规则，避免入口文档继续膨胀。
- 2026-05-06：新增 `docs/status/current.md` 作为“今天做什么”的轻量状态入口，并将 `AGENTS.md`、`CLAUDE.md` 与 `docs/README.md` 的默认阅读链改为先读当前状态文档，再按需补读专题文档。
- 2026-05-07：继续把协作入口去阶段化：`AGENTS.md`、`CLAUDE.md` 回收“当前阶段/当前优先项/当前验证基线/当前实现约定”等易过期内容，只保留长期规则，并明确 `docs/status/current.md` 与专题文档是阶段性口径真相源。
- 2026-05-07：继续收口仓库基础文件：根 `README.md` 改为稳定入口并显式说明 UTF-8 / LF 文本约束，`.gitattributes` 补入 `*.pdf` / `*.tlb` 二进制声明，`.gitignore` 补齐常见 `.NET` / 测试本地产物，`xtask` 与 `normalize-text-files.ps1` 同步把 `*.idl` 纳入文本门禁。
- 2026-05-06：回到数值主线收口 mole fraction 输入契约：`rf-thermo` 的热力学状态和相态焓计算现在要求 mole fractions 在有限、非负之外必须归一到 1；`rf-flash` 直接入口继承该校验并新增未归一 overall composition 失败回归，unit operation 层继续在调用 flash 前归一化文档流股组成。
- 2026-05-06：补出 Stream Inspector composition normalize 正式 command surface：`rf-ui::AppState` 会基于当前有效组成草稿和文档值计算归一化总体摩尔分率，并通过既有 `SetStreamSpecifications` 一次写回；Studio 增加独立 normalize command id、driver/host/runtime dispatch、窗口 presentation 与 shell 按钮，shell 不私下计算或改写其他组分字段。
- 2026-05-06：继续补齐 Stream Inspector 组成编辑可读性：active inspector detail 现在暴露当前总体摩尔分率总和、归一化预览和状态标签，shell 只渲染该 presentation；用户仍需显式点击 normalize action 才写回文档。
- 2026-05-06：继续把 Stream Inspector 组成编辑边界前推到运行入口：`WorkspaceRun` 会在存在未提交/无效 Inspector 草稿或文档流股总体 mole fraction sum 非 `1.000000` 时返回既有 `Blocked` outcome，并通过 run panel notice 明确提示需先应用/修正/归一化；该检查只读取 `InspectorDraftState` 与 `FlowsheetDocument`，不在 shell 中新增私有状态，也不做隐式自动补偿。
- 2026-05-06：继续补出 Stream Inspector 草稿丢弃的正式 command surface：字段级与整股流股级 discard action 会清理对应草稿并刷新 presentation，不修改项目文档、不推进 revision、不写入 undo/redo 历史；无效草稿因此也有明确退出路径，而不是依赖 shell 私有状态或隐式自动补偿。
- 2026-05-06：继续补出 Stream Inspector 受控组分添加边界：active stream detail 会从 flowsheet 组件清单中列出尚未进入当前 stream overall composition 的组件，并通过 `inspector.add_stream_composition_component:*` 正式 command surface 写入 `overall_mole_fraction:<component_id>`；已有组成非空时新增组分固定以 `0.0` 写入，不自动调整其他组分、不新增 shell 私有选择状态。为支持该编辑边界，Studio bootstrap 只要求 flowsheet 至少有两个组件来生成当前 MVP 二元物性包，不再因项目额外定义编辑候选组件而拒绝打开。
- 2026-05-06：继续补出 Stream Inspector 受控组分删除边界：每个可删除的 overall composition 字段会暴露 `inspector.remove_stream_composition_component:*` 正式 command surface；删除只移除当前显式组成条目，不能删除最后一个组成条目，不重算、不归一、不按差值补偿其他组分，后续运行仍复用现有 run-blocking / normalize 机制。

## 关键决策

- Canvas suggestion acceptance 属于增量建模命令，不应复用完整求解前连接校验作为门槛；完整性错误继续由运行诊断和 recovery path 承担。
- 本轮只补 `Feed -> Flash Drum` 与 `Feed -> Heater/Cooler/Valve -> Flash Drum` 这类最短路径，不把该实现扩张成通用自由连线编辑器。
- `Heater / Cooler / Valve` 当前按同一类一进一出 canonical port 规则处理，但只补本地建议与 outlet 模板，不引入单元级参数面板或自由连线编辑器。
- `Mixer` 当前只在来源数量与未绑定 inlet 数量匹配时生成入口建议；多来源歧义先显式不处理，后续若要支持应引入可见的选择/确认边界。
- 真正空白项目当前只做 MVP 默认二元热力学基线初始化，不引入完整组件选择器、完整物性包选择器或项目向导；自动初始化会使空白文件打开后进入未保存状态，用户保存后才把该基线写回项目真相源。
- 显式 suggestion Apply 仍复用 `CanvasSuggestionAcceptance -> DocumentCommand::ConnectPorts`，只解决“选择哪条建议”的可控性，不引入端口点击或自由拖线。
- Canvas placement 坐标持久化只记录已提交单元的落点，并复用现有 Studio layout sidecar；它不进入 `*.rfproj.json`，不进入文档历史，也不代表已实现拖拽布局编辑器。
- 单元最新执行结果只作为 `StudioGuiWindowModel` 的只读 presentation 暴露，不回写 `FlowsheetDocument`，也不扩成结果导出、报表系统或跨会话快照历史持久化。
- 求解步骤导航继续复用 `InspectorTarget` command surface；这只是把已有结果、诊断和 Inspector 目标之间的关系前推为可点击 presentation，不改变求解器、文档模型或项目文件。
- 失败摘要的修复动作继续复用 `run_panel.recover_failure`，目标定位继续复用 `InspectorTarget` command；统一 diagnostic target action 只是 presentation 汇总，不新增另一套错误处理状态机。
- 失败详情结构化展示只消费 `DiagnosticSummary` 已有字段；port target 当前按“unit + port name”展示并定位到 unit，后续若要端口级焦点应先补正式 Inspector target 语义，而不是让 shell 私有拼 command。
- Canvas layout move 当前只作用于 Studio layout sidecar，是 GUI 布局状态，不是 `DocumentCommand::MoveUnit` 文档语义；它不触发求解 dirty，也不代表已进入自由拖拽布局编辑器。离散 nudge 的正式入口为 `canvas.move_selected_unit.left/right/up/down`，由 widget action、command registry 与 `dispatch_ui_command(command_id)` 统一派发；缺少 sidecar 坐标时允许先 pin 到 transient grid slot，避免用户必须先拖拽或重新放置单元。selection 与 command result 必须显式暴露 sidecar/transient 来源，防止该边界变成隐式 shell 行为。
- transient grid pin 仍只属于 Studio layout sidecar 初始化，不是项目几何语义迁移；即使第一次 nudge 会创建 sidecar 坐标，也必须保持项目 JSON、文档 revision、保存态和 undo/redo 历史不变。
- Canvas attention 摘要当前只属于 presentation 层：它消费 `DiagnosticSummary` / `DiagnosticSnapshot` 已经提供的 unit、stream 与 port target 结构化字段，把 port target 明确显示到 Canvas 和 object list，但 command surface 仍只定位到现有 `InspectorTarget`。
- Active Inspector 的 port attention 摘要同样只消费当前文档修订号匹配的 `DiagnosticSummary`，按当前 unit 和 `port_name` 精确匹配；它不改变 `StudioGuiInspectorTargetPortSnapshot` 的文档语义，也不引入端口级焦点或端口级编辑命令。
- Result Inspector unit-centric 视图属于 presentation 层第二个消费面，不是新的 inspector 焦点真相源：unit selection 仍是 shell 临时态，命令仍走既有 `inspector.focus_unit:*` / `inspector.focus_stream:*`；后续若要扩展更完整的结果导出、跨快照对比或单元结果历史，应继续沿正式 presentation / command 边界推进，而不是把这些语义塞进 unit-centric 折叠区。
- Result Inspector unit selection 与 stream selection 互不联动：unit-centric 视图独立显示选中单元的 unit execution result、关联步骤、关联诊断和诊断目标动作；切换流股不影响 unit selection，切换 unit 也不影响 stream selection 与对比流股，避免在一个 snapshot 下两个消费面互相覆写。
- Result Inspector 的“选择结果对象”和“定位 Inspector”继续保持为两个独立动作：选择 stream / comparison stream / unit 只改变结果审阅临时态，旁边的 `focus_action` 才进入既有 Inspector focus command，避免结果区选择行为隐式改写全局 Inspector 焦点。
- diagnostic action 当前应被视为正式 command surface 的薄引用，不承担执行语义本身；成功路径只允许落到 `InspectorTarget`，失败路径只允许落到 `InspectorTarget` 或既有 run panel recovery，避免未来在结果区、Active Inspector、求解步骤或失败摘要中各自分叉导航/修复逻辑。
- Canvas layout 当前已收口到 sidecar placement、离散 nudge、transient grid pin 与非文档 dirty 边界；本轮验证通过后不继续扩 shell/UI 细节。后续若继续推进 Canvas layout，应先补新的正式 layout state / validation 语义，而不是直接进入拖拽布局、视口持久化或自由画布编辑器。
- 当前新增的焓值只是 MVP 常热容显热基线，参考温度固定为 `298.15 K`；它用于补齐 M2/M3 结果消费面，不代表已进入完整焓基准、相变潜热、真实 EOS/活度模型或物性包可配置参考态。
- Studio 结果区只消费求解快照中 already-materialized 的 overall/phase enthalpy，不在 shell 或 window model 里重新求热力学；缺失焓值的流股继续不显示 H summary row，避免把未求解或未闪蒸流股伪装成完整热力学结果。
- Result Inspector 的 phase comparison 只比较当前快照中已有的同名 phase 行，缺失相或缺失焓值显示为 `-`；这一步仍属于结构化结果审阅，不引入结果表格、导出或报表系统。
- Result Inspector 的 unit-centric 审阅当前只消费 solver step 中已经存在的 consumed / produced stream ids，并将它们包装成既有 `InspectorTarget` focus action；这一步只是补齐单元执行上下文，不改变求解器、项目文档、历史持久化或 Canvas 连线语义。
- Result Inspector unit-centric 的关联诊断也只消费同一组 consumed / produced stream ids，不反解析 diagnostic message，也不把入口流股诊断提升为单元编辑或自动修复语义。
- Result Inspector 和 Active Inspector 的 stream related steps 同样只使用 `StudioGuiWindowSolveStepModel` 已有的 consumed / produced stream ids；这只是让结果审阅能看到上下游执行上下文，不等同于流程图遍历、结果表格或自动布线。
- Result Inspector selector summary 只消费 `StudioGuiWindowStreamResultModel` 已经 materialized 的文本字段；`H` 行只在 `molar_enthalpy_text` 存在时出现，不在 window model 或 shell 中补算热力学。
- Result Inspector unit selector summary 同样只消费 `StudioGuiWindowSolveStepModel` 已有的 consumed / produced stream ids；输入/输出流股摘要只是结果对象选择器的只读上下文，不改变 unit selection、Inspector focus command 或项目文档语义。
- Result Inspector stream comparison 的 base / compared stream action 只复用既有 `InspectorTarget` command surface；comparison model 不承担编辑、导出或跨快照历史语义。
- Docs 关键入口文档当前正式承担“快速恢复上下文”的职责，不再作为完整历史背景容器；详细过程、长篇推演和逐日实现记录应进入专题文档或周志。
- 语言与代码风格约束当前正式前置为协作规则：新增抽象必须有清楚领域职责、稳定调用面、错误语义和必要测试，不能用泛化命名或多层包装掩盖模型边界不清。
- 代码规范当前采用“专题文档 + 入口索引”的组织方式；实现风格争议、命名争议或抽象边界判断优先参考 `docs/development/code-style.md`，但新会话默认阅读入口不因此增加长文档。
- “今天做什么”类问题当前以 `docs/status/current.md` 为唯一默认状态入口；`overview.md`、`scope.md` 和周志只作为按需依据，不再承担默认上下文恢复职责。
- `AGENTS.md` / `CLAUDE.md` 只承担长期协作规则；当前阶段、当前重点、当前验证基线、暂不推进项和实现冻结细节应优先写入 `docs/status/current.md` 与对应专题文档，而不是继续堆回协作入口。
- 根 `README.md` 只承担稳定入口职责：当前阶段、当前重点、当前验证基线和阶段性进度默认以 `docs/status/current.md` 与专题文档为准，不再在根文档重复维护易过期状态描述。
- 仓库自有文本资产继续统一为 `UTF-8` 无 BOM、`LF` 换行和文件末尾换行；这一规则现在同时覆盖 `*.idl`，外部参考目录 `adapters/reference/` 继续保留上游编码与换行格式。
- 热力学 / 闪蒸直接数值 API 的组成入参按 mole fractions 语义处理，必须归一到 1；文档流股的 Inspector 草稿和 unit operation 桥接可继续允许编辑态或文档态被归一化处理，但进入 `rf-thermo` / `rf-flash` 前必须落到明确的数值契约。
- Stream Inspector 的组成归一化是正式 command surface：它复用 `SetStreamSpecifications` 作为文档命令和历史记录，不在 shell 层重算组成，也不新增第二套流股编辑状态机。
- Stream Inspector 的组成总和与归一化预览属于只读 presentation，用于解释当前草稿会如何被 normalize；它不自动提交、不自动补偿其他组分，也不改变 `SolveSnapshot`。
- Workspace run 前置检查现在把 Inspector 草稿和文档组成归一性视为运行入口契约：未提交草稿不会被求解器读取，未归一文档组成不会由运行命令自动补偿；用户必须通过既有 Inspector apply / normalize command surface 显式写回后再运行。
- Stream Inspector 草稿丢弃同样属于正式 command surface，但它只操作 `InspectorDraftState`；字段级或整股流股级 discard 都不能改写 `FlowsheetDocument`、不能推进 document revision，也不能进入 command history。
- Stream Inspector 的受控组分添加只允许选择 flowsheet 已定义且当前流股组成尚未包含的组件；该动作复用 `SetStreamSpecification` 写入显式 `overall_mole_fraction:<component_id>`，默认值不承担差值补偿语义，后续求解仍必须依赖用户显式 normalize / apply 后的文档状态。
- Stream Inspector 的受控组分删除只允许删除当前流股已存在的显式组成条目，且必须保留至少一个组成条目；删除动作只表达移除该 component entry，不承担归一化或差值分摊语义。

## 风险与未完成项

- 当前只覆盖 source stream、`Heater / Cooler / Valve` 一进一出链路、`Mixer` 两进一出确定性链路与 Flash Drum canonical ports 的本地规则；更复杂拓扑仍需后续按同一边界补齐。
- 空白项目的默认组件与本地物性包缓存已能支撑最短建模运行闭环；但这仍是固定 MVP 二元基线，不代表已实现真实组件库、物性包浏览/切换、组分删除迁移或多物性包项目配置。
- 当前仍没有端口点击连线、拖拽布局或自动布线；显式 Apply 只是把已有本地建议从 focused-only 操作扩成可逐条选择，坐标持久化与 layout nudge 也只覆盖 sidecar 中的单元落点保存、重载和离散调整。transient grid pin 只是首次生成 sidecar 坐标的边界处理，不等同于可视拖拽或视口持久化。
- 当前结果审阅增强只补 Active Inspector 的单元执行摘要、求解步骤跳转、失败摘要动作、统一诊断目标 action 汇总、失败详情结构化展示、Canvas/object list 只读 attention 摘要、Active Inspector port attention 摘要、Result Inspector unit-centric 视图、当前快照内单元 consumed / produced stream 审阅、对应诊断归集、stream-centric 上下游 solve step 审阅、stream comparison 正文定位 action，以及选择器中的已物化焓值与单元输入/输出摘要；更完整的结果表格、导出、历史对比和报表视图仍未进入本轮范围。
- 文件体积治理本轮已覆盖当前扫描范围内超过 1500 行的源码文件；后续新增实现仍需优先落在已拆出的职责模块中，避免 UI host、shell、canvas presentation、solver、`rf-ui` 与 `.NET` 适配层再次向单文件堆叠。
- 当前 composition normalize / draft discard / component add / component remove 已覆盖已有条目归一化、草稿退出、从 flowsheet 组件清单显式添加缺失组分，以及删除非最后组成条目；仍没有完整组件库、项目级组件创建/删除、组分删除迁移或自动按差值调整其他组分的编辑体验。

## 2026-05-06 日终回顾

- 日终文档提交前共核对 9 个提交：先把协作入口与代码规范专题文档瘦身到 `docs/status/current.md` 作为默认恢复入口，再回到数值契约与 Stream Inspector composition 编辑边界。
- 数值侧完成 mole fraction 直接 API 契约收紧：`rf-thermo` 与 `rf-flash` 会拒绝未归一组成，unit operation 层仍在调用 flash 前显式归一化文档流股组成，避免数值 API 静默接受含义不清的输入。
- Studio 侧补齐 Stream Inspector 组成编辑正式路径：normalize、draft notice、run-blocking、discard、受控添加缺失组分、受控删除非最后组分均通过 presentation / command / driver / runtime 边界执行；shell 只派发 command，不保存私有组成状态。
- 今日边界保持一致：新增或删除组分都不做隐式差值补偿，运行入口只阻塞未提交/无效草稿或未归一文档组成，不替用户自动修正；完整组件库、项目级组件创建/删除、组分删除迁移和复杂物性包选择器仍未进入范围。
- 文档同步状态：`docs/status/current.md`、`docs/devlogs/2026-W19.md`、`docs/mvp/scope.md`、`docs/radishflow-mvp-roadmap.md` 与 `docs/thermo/mvp-model.md` 已覆盖今天的数值契约、Stream Inspector command surface、非目标和下一步建议。
- 今日验证记录：功能提交过程中已执行相关 focused tests；日终前 `pwsh ./scripts/check-repo.ps1` 已通过文本门禁、`cargo fmt --all --check`、`cargo check --workspace`、`cargo test --workspace` 与 `cargo clippy --workspace --all-targets -- -D warnings`。

## 2026-05-05 日终回顾

- 今日主线先完成 Canvas layout nudge 的非文档编辑边界验证和拆分后仓库级验证收口，随后明确暂停继续扩 Canvas 周边 UI，把重点转回数值与结果审阅。
- 数值侧完成 M2 基础焓值闭环：`rf-thermo` 补出常热容相焓，`rf-flash` 与 `Flash Drum` outlet 传递 liquid/vapor/overall molar enthalpy，golden tests 与 Studio stream result presentation 已同步覆盖。
- 结果审阅侧围绕同一份 `SolveSnapshot` 继续收口：Result Inspector 增加 phase comparison rows、stream / comparison selector 焓值摘要、unit consumed / produced stream 审阅、unit 关联诊断、stream-centric 上下游 solve steps、unit selector 输入/产出摘要，以及 stream comparison 正文 base / compared stream focus action。
- 今日边界保持一致：所有新增入口都只消费已结构化 DTO，并复用既有 `InspectorTarget` / `run_panel.recover_failure` command；没有新增结果报表、导出、完整结果表格、跨快照历史、Canvas 自由连线或拖拽布局语义。
- 文档同步状态：`docs/devlogs/2026-W19.md`、`docs/mvp/scope.md`、`docs/radishflow-mvp-roadmap.md`、`docs/architecture/overview.md` 与 `docs/thermo/mvp-model.md` 已对齐今天 13 个提交；其中 `docs/thermo/mvp-model.md` 已修正“焓模型未实现”的过期表述。
- 今日验证记录：功能提交过程中已执行相关 `radishflow-studio` focused tests 与多次 `pwsh ./scripts/check-repo.ps1`；日终文档同步为 docs-only，提交前执行仓库文本检查与状态确认。

## 2026-05-04 日终回顾

- 今日主线可归为五组：Canvas 最短建模路径、空白项目与 placement 持久化、结果审阅导航/失败定位、结构化 port attention 摘要、Result Inspector unit-centric 视图。unit-centric 视图让结果区按单元维度复用既有 unit execution result / 求解步骤 / 关联诊断 / 诊断目标动作 presentation。
- 文档同步状态：`docs/radishflow-mvp-roadmap.md`、`docs/mvp/scope.md`、`docs/architecture/overview.md` 与本周周志均已补齐 2026-05-04 的 Studio / Canvas / diagnostic 边界；`docs/radishflow-startup-checklist.md` 仍是启动期清单，未按每日功能进展继续膨胀。
- 今日最终边界：结构化 port target 只作为只读 presentation 进入 Canvas、object list 与 Active Inspector；定位继续复用 `InspectorTarget`，修复继续复用 `run_panel.recover_failure`，没有新增端口点击编辑、自由连线、拖拽布局或第二套错误处理状态机。
- 今日验证记录：功能提交过程中已执行 `cargo fmt --check`、相关 `radishflow-studio` focused tests 与 `cargo check`；本次日终文档同步为 docs-only，提交前只做文本 diff 检查和工作区状态确认。

## 下周建议

- 下一步可继续在当前 `SolveSnapshot` 边界内收口结果审阅导航/错误定位的遗漏项；若再转向 Canvas，也应先补正式 command / validation / layout state 边界，不直接堆自由连线编辑器或拖拽布局编辑器。
