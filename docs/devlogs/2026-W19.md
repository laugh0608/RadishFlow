# 2026-W19 Development Log

周期：2026-05-04 至 2026-05-10

## 本周目标

- 把主线从 Canvas 只读扫读层继续推进到“放置单元后补齐连接/必要流股”的最短可操作路径。
- 优先验证 `Feed -> Flash Drum` 这一条最小建模闭环，不扩完整画布编辑器、拖拽布局、自由连线或结果导出。
- 继续保持 `cargo check` 与相关 focused tests 稳定。

## 完成情况

- 2026-05-04：补出未绑定 `Feed.outlet` 的本地 Canvas suggestion，可创建默认 source stream，并在已有组分体系下给新 feed stream 初始化等摩尔组成与基础 `T / P / F`，让后续 Stream Inspector 可直接继续调整参数。
- 2026-05-04：Flash Drum 入口连接建议现在会排除已有 `Flash Drum.liquid/vapor` terminal product streams，只把真正可继续下游连接的 source-only stream 作为候选，避免在已有完整示例项目中误判入口来源。
- 2026-05-04：`CanvasSuggestionAcceptance::MaterialConnection` 不再要求整张 flowsheet 立即通过完整连接校验；当前改为校验本次 source/sink/stream 的局部一致性，允许用户按顺序补齐仍有未绑定端口的半成品流程。
- 2026-05-04：Canvas pending placement 提交成功后会立即刷新本地 suggestions，因此用户放置 `Feed` 后能立刻看到创建 outlet stream 的下一步建议。
- 2026-05-04：新增回归测试锁定 `Feed -> Flash Drum` 最短路径：放置 Feed、接受 Feed outlet 建议、放置 Flash Drum、依次接受 inlet / liquid / vapor 三个建议，最终 Canvas 暴露三条新 material stream line 且项目进入未保存状态。
- 2026-05-04：继续把同一套本地 suggestion 规则扩到 `Heater / Cooler / Valve` 这类一进一出单元：入口只在唯一可连接 source-only stream 存在时建议连接，出口会创建下一段 source stream，并按单元类型给 outlet 模板设置基础 `T / P`。
- 2026-05-04：新增 `Feed -> Heater -> Flash Drum` shell 回归路径，覆盖放置 Feed、创建 Feed outlet、放置 Heater、连接 Heater inlet、创建 Heater outlet、放置 Flash Drum、补齐 Flash inlet / liquid / vapor，并在最后触发 `run_panel.run_manual` 后确认求解收敛。
- 2026-05-04：继续补出 `Mixer` 两进一出本地 suggestion：只有可连接 source-only stream 数量与未绑定 mixer inlet 数量一致时才按确定性顺序建议 `inlet_a / inlet_b`，source 数量不匹配时只保留 outlet 创建建议，避免多来源场景静默猜错入口。
- 2026-05-04：新增 `Feed + Feed -> Mixer -> Flash Drum` shell 回归路径，覆盖两个 Feed outlet、Mixer 两个 inlet、Mixer outlet、Flash inlet / liquid / vapor 全部通过 suggestion 补齐，并在最后手动运行求解收敛。
- 2026-05-04：继续补齐真正空白项目的前置缺口：Studio bootstrap 会在项目无组件时初始化 MVP 默认二元组件 `component-a / component-b`，并用初始化后的 flowsheet 生成本地 `binary-hydrocarbon-lite-v1` 物性包缓存；新建 Feed source stream 因此会持久化 `T=298.15 K / P=101325 Pa / F=1 mol/s / z=[0.5, 0.5]` 默认值。
- 2026-05-04：新增空白项目最短闭环回归：从空白 `*.rfproj.json` 打开放置 `Feed -> Flash Drum`、接受本地 suggestions、手动运行收敛、保存、重新打开，再次手动运行收敛，并确认保存文件中组件与 Feed source stream 默认组成完整落盘。
- 2026-05-04：继续补出 Canvas suggestion 显式选择入口：每条可写回的本地建议都会暴露独立 Apply 动作，用户可不依赖当前 focused suggestion，直接按 suggestion id 接受指定连接或 outlet stream 创建；新增 core / widget / shell 回归锁定非 focused suggestion 也能显式接受并运行 `Feed -> Flash Drum`。
- 2026-05-04：继续补出 Canvas placement 坐标最小持久化：`CommitPendingEditAt` 的落点会写入项目同目录 `<project>.rfstudio-layout.json` sidecar，重开项目后 Canvas 先使用已保存坐标渲染单元块；新增回归锁定 `Feed -> Flash Drum` 保存、重开、位置恢复、继续 Apply suggestions 并运行收敛。
- 2026-05-04：继续补出 Active Inspector 的单元最新执行结果审阅：选中已运行单元时，Runtime 详情会显示来自最新 `SolveSnapshot` 的执行状态、step 序号、summary 和产出流股跳转；新增 window model / shell locale 回归锁定该 presentation。
- 2026-05-04：收口 Studio layout sidecar 读取边界：项目同目录 `.rfstudio-layout.json` 缺失时按空布局处理，避免测试并发或用户清理 sidecar 后阻塞项目打开。
- 2026-05-04：继续收口结果审阅导航：`StudioGuiWindowSolveStepModel` 现在携带单元与产出流股 command action，Runtime 全局求解步骤、Active Inspector 关联步骤和 Result Inspector 关联步骤都能复用同一跳转 presentation。
- 2026-05-04：继续补齐失败路径定位：`StudioGuiWindowFailureResultModel` 现在携带 recovery action 与 recovery target action，失败摘要可直接触发既有修复命令或定位到相关 Inspector 目标。
- 2026-05-04：继续把结果审阅/错误定位动作汇总成统一 presentation：新增 `StudioGuiWindowDiagnosticTargetActionModel`，失败摘要、Result Inspector、Active Inspector 与求解步骤都会暴露同一类诊断目标 action，shell 只负责渲染并继续通过既有 command id 派发。
- 2026-05-04：继续补出失败详情结构化展示：`WorkspaceControlState` 会带出 latest diagnostic summary，失败结果 presentation 可直接显示 revision、severity、primary code、diagnostic count、相关 unit / stream / port target；port target 仅复用所属 unit 的 Inspector command，不新增端口级命令或 message 反解析。
- 2026-05-04：开始收口布局编辑边界：新增 `CanvasUnitLayoutMoveRequested -> MoveCanvasUnitLayout` GUI-facing 事件，只验证单元存在并更新 Canvas unit position sidecar；真实 shell 对当前选中且已有 sidecar 坐标的单元提供离散 nudge 按钮，不修改项目文档、不进入 revision/history。
- 2026-05-04：继续把 Canvas layout nudge 从 shell 私有按钮前推到正式 command surface / widget action：新增 `canvas.move_selected_unit.left/right/up/down`，选中单元后会生成 `Move left/right/up/down` 命令，shell 只派发 command id；缺少 sidecar 坐标时先按当前 transient grid slot pin 出初始位置，再写入 `<project>.rfstudio-layout.json`。
- 2026-05-04：继续补出 Canvas layout state 摘要：选中单元的 selection presentation 会明确显示当前来自 `sidecar position` 还是 `transient grid`，首次 nudge 的 command result 会说明“先从 transient grid slot pin 到 sidecar，再移动”，避免用户误以为项目文档几何被修改。
- 2026-05-04：继续把失败诊断与 Canvas attention 联动：Canvas unit / stream hover、material port hover 和对象列表 attention summary 会从结构化 `related_port_targets` 生成只读摘要，例如 `ports unit:port` 与 diagnostic code，不从错误 message 反解析，也不新增端口点击编辑、端口级命令或错误处理状态机。
- 2026-05-04：继续把同一套 port attention 摘要接到 Active Inspector 的 unit port 列表：选中相关单元后，对应 port 行会显示来自 `DiagnosticSummary.related_port_targets` 的只读 `attention` 摘要；端口行仍只保留既有 stream Inspector 跳转，不新增 port command。
- 2026-05-04：Result Inspector 当前补出第二个消费维度——unit-centric 视图：`StudioGuiWindowResultInspectorModel` 现在同时携带 `unit_options / selected_unit_id / selected_unit / unit_related_steps / unit_related_diagnostics / unit_diagnostic_actions / has_stale_unit_selection`；新增 `result_inspector_with_unit(stream_id, comparison_stream_id, unit_id)` 入口，未指定单元时按求解步骤顺序取首个 unit，缺失/失效单元会回落到首个 unit 并把 `has_stale_unit_selection` 置为 true。`egui` Runtime 面板在结果区流股详情下新增 `Unit results` 折叠区，展示 unit selector、最新 unit execution result、关联求解步骤、关联诊断与诊断目标动作；shell 仅消费 DTO 并继续派发既有 `inspector.focus_unit:*` / `inspector.focus_stream:*` 命令，不引入新的 unit 编辑或导出能力。
- 2026-05-04：Result Inspector unit-centric 视图与 stream-centric 视图共存为同一 snapshot 下的两个并列消费面：unit selector 与 stream selector 互不联动，`ResultInspectorState` 的 unit selection 只在 snapshot 切换或单元从快照消失时复位，不会因为切换流股或对比流股被清理；unit-related diagnostics 复用 produced stream id 集合补出与该 unit 输出相关的诊断条目，避免与 `Active Inspector` 单元详情形成第二套规则。
- 2026-05-04：继续治理 Studio window model 文件体积：将超长 `studio_gui_window_model.rs` 拆为 `studio_gui_window_model/mod.rs`、`drop_preview.rs`、`result_inspector.rs` 与 `tests.rs`，保持对外模块名和 DTO/API 不变；拆分后该模块内单个源码文件均低于 1500 行。

## 关键决策

- Canvas suggestion acceptance 属于增量建模命令，不应复用完整求解前连接校验作为门槛；完整性错误继续由运行诊断和 recovery path 承担。
- 本轮只补 `Feed -> Flash Drum` 与 `Feed -> Heater/Cooler/Valve -> Flash Drum` 这类最短路径，不把该实现扩张成通用自由连线编辑器。
- `Heater / Cooler / Valve` 当前按同一类一进一出 canonical port 规则处理，但只补本地建议与 outlet 模板，不引入单元级参数面板或自由连线编辑器。
- `Mixer` 当前只在来源数量与未绑定 inlet 数量匹配时生成入口建议；多来源歧义先显式不处理，后续若要支持应引入可见的选择/确认边界。
- 真正空白项目当前只做 MVP 默认二元热力学基线初始化，不引入完整组件选择器、完整物性包选择器或项目向导；自动初始化会使空白文件打开后进入未保存状态，用户保存后才把该基线写回项目真相源。
- 显式 suggestion Apply 仍复用 `CanvasSuggestionAcceptance -> DocumentCommand::ConnectPorts`，只解决“选择哪条建议”的可控性，不引入端口点击或自由拖线。
- Canvas placement 坐标持久化只记录已提交单元的落点，并复用现有 Studio layout sidecar；它不进入 `*.rfproj.json`，不进入文档历史，也不代表已实现拖拽布局编辑器。
- 单元最新执行结果只作为 `StudioGuiWindowModel` 的只读 presentation 暴露，不回写 `FlowsheetDocument`，也不扩成结果导出、报表系统或跨会话快照历史持久化。
- 求解步骤导航继续复用 `InspectorTarget` command surface；这只是把已有结果、诊断和 Inspector 目标之间的关系前推为可点击 presentation，不改变求解器、文档模型或项目文件。
- 失败摘要的修复动作继续复用 `run_panel.recover_failure`，目标定位继续复用 `InspectorTarget` command；统一 diagnostic target action 只是 presentation 汇总，不新增另一套错误处理状态机。
- 失败详情结构化展示只消费 `DiagnosticSummary` 已有字段；port target 当前按“unit + port name”展示并定位到 unit，后续若要端口级焦点应先补正式 Inspector target 语义，而不是让 shell 私有拼 command。
- Canvas layout move 当前只作用于 Studio layout sidecar，是 GUI 布局状态，不是 `DocumentCommand::MoveUnit` 文档语义；它不触发求解 dirty，也不代表已进入自由拖拽布局编辑器。离散 nudge 的正式入口为 `canvas.move_selected_unit.left/right/up/down`，由 widget action、command registry 与 `dispatch_ui_command(command_id)` 统一派发；缺少 sidecar 坐标时允许先 pin 到 transient grid slot，避免用户必须先拖拽或重新放置单元。selection 与 command result 必须显式暴露 sidecar/transient 来源，防止该边界变成隐式 shell 行为。
- Canvas attention 摘要当前只属于 presentation 层：它消费 `DiagnosticSummary` / `DiagnosticSnapshot` 已经提供的 unit、stream 与 port target 结构化字段，把 port target 明确显示到 Canvas 和 object list，但 command surface 仍只定位到现有 `InspectorTarget`。
- Active Inspector 的 port attention 摘要同样只消费当前文档修订号匹配的 `DiagnosticSummary`，按当前 unit 和 `port_name` 精确匹配；它不改变 `StudioGuiInspectorTargetPortSnapshot` 的文档语义，也不引入端口级焦点或端口级编辑命令。
- Result Inspector unit-centric 视图属于 presentation 层第二个消费面，不是新的 inspector 焦点真相源：unit selection 仍是 shell 临时态，命令仍走既有 `inspector.focus_unit:*` / `inspector.focus_stream:*`；后续若要扩展更完整的结果导出、跨快照对比或单元结果历史，应继续沿正式 presentation / command 边界推进，而不是把这些语义塞进 unit-centric 折叠区。
- Result Inspector unit selection 与 stream selection 互不联动：unit-centric 视图独立显示选中单元的 unit execution result、关联步骤、关联诊断和诊断目标动作；切换流股不影响 unit selection，切换 unit 也不影响 stream selection 与对比流股，避免在一个 snapshot 下两个消费面互相覆写。

## 风险与未完成项

- 当前只覆盖 source stream、`Heater / Cooler / Valve` 一进一出链路、`Mixer` 两进一出确定性链路与 Flash Drum canonical ports 的本地规则；更复杂拓扑仍需后续按同一边界补齐。
- 空白项目的默认组件与本地物性包缓存已能支撑最短建模运行闭环；但这仍是固定 MVP 二元基线，不代表已实现真实组件库、物性包浏览/切换、组分删除迁移或多物性包项目配置。
- 当前仍没有端口点击连线、拖拽布局或自动布线；显式 Apply 只是把已有本地建议从 focused-only 操作扩成可逐条选择，坐标持久化与 layout nudge 也只覆盖 sidecar 中的单元落点保存、重载和离散调整。transient grid pin 只是首次生成 sidecar 坐标的边界处理，不等同于可视拖拽或视口持久化。
- 当前结果审阅增强只补 Active Inspector 的单元执行摘要、求解步骤跳转、失败摘要动作、统一诊断目标 action 汇总、失败详情结构化展示、Canvas/object list 只读 attention 摘要、Active Inspector port attention 摘要与 Result Inspector unit-centric 视图；更完整的结果表格、导出、历史对比和报表视图仍未进入本轮范围。
- 文件体积治理仍未完成：本次只处理 `studio_gui_window_model`，仓库内仍存在若干超过 1500 行的源码文件，后续应按职责边界继续拆分，避免在 UI host、shell、canvas presentation、solver、`rf-ui` 主模块和 `.NET` 适配层中继续堆叠实现。

## 2026-05-04 日终回顾

- 今日主线可归为五组：Canvas 最短建模路径、空白项目与 placement 持久化、结果审阅导航/失败定位、结构化 port attention 摘要、Result Inspector unit-centric 视图。unit-centric 视图让结果区按单元维度复用既有 unit execution result / 求解步骤 / 关联诊断 / 诊断目标动作 presentation。
- 文档同步状态：`docs/radishflow-mvp-roadmap.md`、`docs/mvp/scope.md`、`docs/architecture/overview.md` 与本周周志均已补齐 2026-05-04 的 Studio / Canvas / diagnostic 边界；`docs/radishflow-startup-checklist.md` 仍是启动期清单，未按每日功能进展继续膨胀。
- 今日最终边界：结构化 port target 只作为只读 presentation 进入 Canvas、object list 与 Active Inspector；定位继续复用 `InspectorTarget`，修复继续复用 `run_panel.recover_failure`，没有新增端口点击编辑、自由连线、拖拽布局或第二套错误处理状态机。
- 今日验证记录：功能提交过程中已执行 `cargo fmt --check`、相关 `radishflow-studio` focused tests 与 `cargo check`；本次日终文档同步为 docs-only，提交前只做文本 diff 检查和工作区状态确认。

## 下周建议

- 下一步优先把结果审阅继续收口到流股/单元结果的统一导航和错误定位，或转向更明确的布局编辑边界；若继续扩 Canvas，仍应先补正式 command / validation / layout state 边界，不直接堆自由连线编辑器或拖拽布局编辑器。
