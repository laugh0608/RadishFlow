# 2026-W21 Development Log

周期：2026-05-18 至 2026-05-24

## 本周当前摘要

- 2026-05-18 已按 `docs/status/current.md` 的下一步建议，优先收口 Studio 首版 demo 前保留的两个 UI blocker。
- Canvas 中间连接流股在短线段空间不足时不再绘制名称标签，避免 `Feed -> Valve -> Flash Drum` 这类紧凑链路中的标签遮挡单元块、端口或被裁切；终端流股标签仍保留在端口外侧。
- 右侧 Result Inspector 的默认组成 / 相态摘要不再生成 `z:` / `phases:` 原始前缀，改为由 UI 标签承载语义、内容保持结构化短值；结果面继续只读消费 `SolveSnapshot`，没有新增 shell-local 结果缓存或完整报表系统。
- 今日仓库级验证在真实环境通过。沙盒内默认 `target/debug/.cargo-lock` 出现访问拒绝，临时目标目录若放在仓库根又会被文本门禁扫描；最终使用已忽略的 `target/codex` 作为 `CARGO_TARGET_DIR` 并提权到真实环境复验。

## 本周目标

- 继续做 Studio 人工视觉 smoke，确认 Home Dashboard、打开示例、运行、Canvas 标签、右侧结果摘要、底部结果表、消息 / 物性包入口和关闭窗口路径没有回归。
- 只处理真实 demo blocker，不把 UI 收口扩张为自由连线、自动布线、完整拖拽布局、视口持久化或完整结果报表。
- 继续保持当前便携包为内部验证资产；暂不创建 tag，不推进对外发布自动化。

## 当前完成情况

- 2026-05-18：短线段连接流股标签增加空间阈值判断；相邻单元之间没有足够标签空间时隐藏中间标签，避免紧凑链路遮挡。新增 Canvas viewport geometry 回归覆盖短线隐藏、长线保留和终端标签保留。
- 2026-05-18：窗口模型中的组成 / 相态摘要去掉 `z:` / `phases:` 前缀；Result Inspector 默认正文改为带 `总体组成` / `相态结果` 标签的短行。相关 runtime 渲染测试新增断言，确保默认结果面不再出现原始前缀。
- 2026-05-18：同步更新 `docs/status/current.md` 和 `docs/mvp/alpha-acceptance-checklist.md`，把昨天日终两个 UI blocker 从“下一步”移入完成记录。

## 验证记录

- `cargo fmt --all`
- `cargo fmt --all --check`
- `cargo test -p radishflow-studio viewport_geometry_tests`
- `cargo test -p radishflow-studio studio_gui_window_model`
- `cargo test -p radishflow-studio runtime_panel_renders_summary_rows_and_context_for_non_flash_intermediate_streams`
- `cargo test -p radishflow-studio studio_gui_shell`
- 真实环境复验：`CARGO_TARGET_DIR=target/codex pwsh ./scripts/check-repo.ps1`，最终输出 `Repository checks passed.`

## 明天事项

1. 人工启动 Studio 做视觉 smoke，重点看短线段中间流股标签是否按预期隐藏、终端流股标签是否仍清晰、结果检查器默认正文是否没有 `z:` / `phases:`。
2. 若 smoke 仍有中文或按钮语义问题，只按高频路径收口，不展开完整本地化体系。
3. 继续暂缓 `v26.5.1-dev` tag；便携包保持内部验证资产定位。

## 风险与后续

- 今天的 Canvas 标签策略选择“空间不足时隐藏”，是 demo 可读性治理，不是自动布线或布局求解。若后续需要更高保真标签避让，应先作为 Canvas presentation 专题设计，不直接改 flowsheet 语义。
- 结果摘要仍使用组件 id 和相标签等领域标识，默认不翻译这些稳定数据字段；本轮只清理调试式前缀和默认正文结构。
