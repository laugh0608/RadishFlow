# 2026-W17 Development Log

周期：2026-04-20 至 2026-04-26

## 本周目标

- 继续推进 `UnitOp.Mvp` 主线，但不提前进入 COM 注册、PME 自动化互调或第三方 CAPE-OPEN 模型加载
- 评估 smoke host 中已稳定的 action execution 编排是否应上移为正式库内 helper / read model
- 保持 `cargo check` 与 `.NET 10` contract / smoke 验证基线稳定

## 完成情况

- 检查当前 `dev` 分支状态，工作区干净，本地领先 `origin/dev` 4 个提交
- 回顾 `UnitOp.Mvp` 与 `SmokeTests` README、action mutation bridge、action execution dispatcher、object mutation dispatcher 与 `UnitOperationSmokeHostDriver`
- 判断 `UnitOperationSmokeHostDriver` 仍不适合整体上移，因为它同时承担 smoke 默认输入、完整调用顺序、失败分类与场景验证职责
- 将已稳定的 action plan 到 execution request 的公共编排上移为 `UnitOperationHostActionExecutionRequestPlanner`
- 新增 `UnitOperationHostActionExecutionInputSet`、`UnitOperationHostActionExecutionRequestPlan` 与 `UnitOperationHostActionExecutionRequestPlanEntry`
- request planning 当前显式区分 `RequestReady / MissingInputs / LifecycleOperationRequired / Unsupported`
- `SmokeTests` 的 driver 当前改为只准备 parameter values / port objects 输入集合，再由库内 planner 生成 ready requests，最后交给 `UnitOperationHostActionExecutionDispatcher`
- `ContractTests` 新增 action execution request planning contract，覆盖 constructed、initialized、companion mismatch 与 terminated 下的规划语义
- 同步更新 `UnitOp.Mvp` README、`SmokeTests` README、`docs/capeopen/boundary.md`、`docs/mvp/scope.md`、`docs/radishflow-mvp-roadmap.md` 与 `docs/radishflow-architecture-draft.md`
- 在 request planning 之上继续新增 `UnitOperationHostActionExecutionOrchestrator`，把 request plan、execution batch outcome 与刷新后的 configuration / action plan / session 收口到正式 orchestration result
- 在 orchestration result 之上继续新增正式 `FollowUp` 模型，显式区分 `LifecycleOperation / ProvideInputs / Validate / Calculate / Terminated`
- `SmokeTests` 当前已开始最小消费 orchestration result / follow-up，不再在配置动作后私有维护“下一步该 validate 还是 calculate”的判断
- `ContractTests` 当前也已新增 orchestration contract，锁住执行后刷新与 follow-up 语义
- 在 action execution 之外，又继续补出 `UnitOperationHostViewReader`、`UnitOperationHostValidationRunner` 与 `UnitOperationHostCalculationRunner`，把 `Validate()` / `Calculate()` 后的正式 host views 与 next-step follow-up 也前推到库内
- `FollowUp` 当前又扩成 `LifecycleOperation / ProvideInputs / Validate / Calculate / CurrentResults / Terminated` 六态，明确区分“还需要操作”和“结果已可消费”
- `SmokeTests` 当前已开始通过 driver 消费 validation/calculation round outcome，而不再在 `Validate()` / `Calculate()` 之后手工补读 session/report/execution 再判断下一步
- `ContractTests` 当前也已新增 validation round / calculation round contract，锁住前置调用、ready、failure、success 与 terminated 下的正式结果语义
- 又继续补出 `UnitOperationHostRoundOrchestrator`、`UnitOperationHostRoundRequest`、`UnitOperationHostRoundOutcome` 与 `UnitOperationHostRoundStopKind`，把“可选 action execution -> 可选 validate -> 可选 calculate”这一条最常见宿主 round 主路径继续收口到库内
- `SmokeTests` 当前已开始在 `UnitOperationSmokeSession` 中优先消费 round outcome，减少“配置动作 -> validate -> calculate -> 再读 session/report”这类重复流程判断
- `ContractTests` 当前也已新增 host-round contract，锁住 lifecycle gate、missing inputs、success、native failure 与 terminated 下的 stop/follow-up 语义
- `HostRoundRequest` 当前又新增 supplemental mutation phase，用于承载这类“不属于 blocking action plan、但宿主仍希望在 validate/calculate 前显式写入”的对象修改；`SmokeTests` 已用它替换先前私有的 optional package-file 补丁
- 新增独立 `RadishFlow.CapeOpen.UnitOp.Mvp.SampleHost` console，直接复用正式 `host view / request planner / host round` 消费面演示最小外部 host 路径，不再要求未来 PME host 参考 smoke DSL
- 基于 `SampleHost` 已验证正式消费路径，继续新增 `PmeLikeUnitOperationHost / PmeLikeUnitOperationSession / PmeLikeUnitOperationInput` 薄宿主入口
- `SampleHost` console 当前已改为通过 PME-like session 执行“创建组件、初始化、读取视图、提交参数/端口对象、执行 validate/calculate round、读取正式结果面、终止”，不再在 `Program.cs` 内直接持有 unit operation 并手工组装 round request
- 该薄宿主入口仍只复用 `UnitOp.Mvp` 正式 `host view / request planner / host round / session-execution-port-material-report` 消费面，不复用 `SmokeTests` driver DSL，也不引入 COM 注册、PME 自动化互调或第三方模型加载
- 新增 `UnitOperationComIdentity`，冻结自有 MVP Unit Operation PMC 的 `CLSID / ProgID / Versioned ProgID / DisplayName / Description`
- 将 `RadishFlowCapeOpenUnitOperation` 标记为 `ComVisible(true)`、固定 `Guid`、`ProgId` 与 `ClassInterface(None)`，只作为注册前置元数据，不执行注册
- 初始化 `RadishFlow.CapeOpen.Registration` dry-run/preflight console，输出组件身份、CAPE-OPEN categories、最小已实现接口和当前边界标志；该工具当前不写注册表、不注册 COM、不启动 PME、不加载第三方 CAPE-OPEN 模型
- `Registration` 当前继续扩展为 dry-run registry plan，可按 `register / unregister` 与 `current-user / local-machine` 输出将来会写入或删除的 registry key；当前仍只打印计划，不执行系统写入
- registration plan 当前又把 `.NET comhost` 路径解析列为 `Verify` 步骤，避免把旧 `.NET Framework` `mscoree.dll` 注册口径误写成未来实现事实

## 关键决策

- 本轮不把 `UnitOperationSmokeHostDriver` 整体上移为正式库 API
- 正式上移的边界是“action execution request planning”，不是“完整宿主生命周期编排”
- request planner 只消费宿主显式提供的 parameter values 与 port objects，不替宿主选择 flowsheet JSON、package id、连接对象命名或 lifecycle 调用时机
- smoke host / 未来 PME host / 其他宿主入口后续应优先复用 action plan、request planner、dispatcher 与 session snapshot，而不是各自重写 action 分类和输入缺失判断
- smoke host / 未来 PME host / 其他宿主入口后续应优先复用 action plan、request planner、host view reader、orchestrator、validation/calculation runner、host round outcome、supplemental mutation phase、follow-up 与 session snapshot，而不是各自重写 action 分类、输入缺失判断、optional mutation 注入、phase gating 和执行后下一步建议
- 未来 PME host / 其他宿主入口的最小接线应优先以 `SampleHost` 演示的正式消费路径为蓝本，而不是以 smoke driver 的验证 DSL 为蓝本
- 当前 PME-like 薄宿主入口已经证明 `SampleHost` 路径可以从“console 演示”前推为“薄宿主 session 形状”；host round 兜底子任务到这里应继续视为结束，不需要继续深挖
- 当前注册前置口径已先冻结身份和 dry-run 计划；下一步的必须补口不再是 CLSID/ProgID 选择，而是执行型 COM 注册工具的安全边界、安装/反安装路径与目标 PME 人工验证说明
- dry-run registry plan 当前用于先审查写入范围，后续若实现执行型注册，必须显式加入权限检查、路径存在性检查、备份/回滚策略和用户确认，不应直接从 plan 变成静默写注册表

## 验证

- `cargo check`
- `dotnet build adapters/dotnet-capeopen/RadishFlow.CapeOpen.UnitOp.Mvp.SampleHost/RadishFlow.CapeOpen.UnitOp.Mvp.SampleHost.csproj -v minimal`
- `adapters/dotnet-capeopen/RadishFlow.CapeOpen.UnitOp.Mvp.SampleHost/bin/Debug/net10.0/RadishFlow.CapeOpen.UnitOp.Mvp.SampleHost.exe --native-lib-dir D:\Code\RadishFlow\target\debug`
- `dotnet build adapters/dotnet-capeopen/RadishFlow.CapeOpen.Registration/RadishFlow.CapeOpen.Registration.csproj -v minimal`
- `adapters/dotnet-capeopen/RadishFlow.CapeOpen.Registration/bin/Debug/net10.0/RadishFlow.CapeOpen.Registration.exe`
- `adapters/dotnet-capeopen/RadishFlow.CapeOpen.Registration/bin/Debug/net10.0/RadishFlow.CapeOpen.Registration.exe --json`
- `adapters/dotnet-capeopen/RadishFlow.CapeOpen.Registration/bin/Debug/net10.0/RadishFlow.CapeOpen.Registration.exe --action unregister`
- `adapters/dotnet-capeopen/RadishFlow.CapeOpen.Registration/bin/Debug/net10.0/RadishFlow.CapeOpen.Registration.exe --scope local-machine`
- `dotnet build adapters/dotnet-capeopen/RadishFlow.CapeOpen.UnitOp.Mvp.ContractTests/RadishFlow.CapeOpen.UnitOp.Mvp.ContractTests.csproj -v minimal`
- `adapters/dotnet-capeopen/RadishFlow.CapeOpen.UnitOp.Mvp.ContractTests/bin/Debug/net10.0/RadishFlow.CapeOpen.UnitOp.Mvp.ContractTests.exe --native-lib-dir D:\Code\RadishFlow\target\debug`
- `adapters/dotnet-capeopen/RadishFlow.CapeOpen.SmokeTests/bin/Debug/net10.0/RadishFlow.CapeOpen.SmokeTests.exe --mode unitop --native-lib-dir D:\Code\RadishFlow\target\debug`

## 风险与未完成项

- 当前 request planner 仍只覆盖 MVP action kinds，不承诺完整 CAPE-OPEN PME 生命周期编排
- 当前 orchestration / validation / calculation / host-round outcome 与 follow-up 仍只覆盖 MVP host interaction 主线，不承诺完整 PME 生命周期驱动或更高层工作流编排
- smoke driver 仍是验证型 helper，后续如果继续上移，需要先拆清真实宿主 API 与 smoke 场景 DSL 的边界
- `SampleHost` 当前虽已具备 PME-like 薄宿主 session，但仍不代表已经进入 COM 注册、PME 自动化互调或完整宿主管理框架
- `Registration` 当前仍是 dry-run/preflight，不代表已经进入 COM 注册执行、PME 互调或第三方模型加载

## 下周建议

- 继续深化 `UnitOp.Mvp` 对象运行时与宿主交互语义一致性
- 优先补能被 contract tests 与 smoke host 共同消费的正式库内 reader / planner / snapshot，而不是新增测试私有 helper
- 若继续推进 M5，优先明确执行型 COM 注册工具的安全边界、安装/反安装脚本、目标 PME 人工验证路径与失败回滚说明；除非发现正式消费面仍有具体缺口，否则不再继续围绕 host round 或 smoke driver 做扩展
