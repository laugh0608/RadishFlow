# RadishFlow v26.5.1-dev Release Notes

更新时间：2026-05-18

## 用途

用途：记录 MVP α `v26.5.1-dev` 内部便携包的说明、验证结果和包内边界。
读者：准备执行 MVP α 内部验收或归档便携包的开发者、验证人员和 AI / Agent。
不包含：完整历史流水、未来功能规划、安装器设计、COM 注册步骤和 PME 自动化说明。

## 发布定位

`v26.5.1-dev` 当前仅作为 MVP α 内部便携包和内部验收 tag 使用，不代表首版 demo 或对外发布。当前交付形态是 Windows 便携压缩包 / staging 目录，不是安装器。

Git tag：

```text
v26.5.1-dev -> 7479e82
```

候选包入口：

```text
artifacts/packages/RadishFlow-v26.5.1-dev-windows-x64/
artifacts/packages/RadishFlow-v26.5.1-dev-windows-x64.zip
```

包内启动入口：

```powershell
.\radishflow-studio.exe
```

## 主要内容

- Rust Studio MVP α 最小工作台：打开示例、新建空白、最短建模、运行、审阅结果、保存 / 另存为和重开项目。
- Canvas 覆盖 `Feed -> Flash Drum`、`Feed -> Heater/Cooler/Valve -> Flash Drum`、`Feed + Feed -> Mixer -> Flash Drum` 三条最短可求解路径。
- Result Inspector / Active Inspector 只读消费同一份 `SolveSnapshot`，可审阅 stream-centric / unit-centric 结果、相结果、`bubble_dew_window`、overall enthalpy、关联步骤和诊断目标。
- Stream Inspector 支持 composition draft、显式提交、normalize、discard、受控组件添加 / 删除和运行前阻断。
- 中文界面资源覆盖 MVP α smoke 高频路径。
- Home Dashboard 与 Workbench 第一轮 UI 收口已覆盖示例 / 最近项目入口、Canvas 初始居中、短线段流股标签隐藏、运行后结果切换和 Result Inspector 结构化摘要。
- Windows 便携包包含 Studio exe、正向 flowsheet 示例、样例物性包、quick start、结果审阅说明、验收清单、版本说明、release notes 和许可文件。

## 验证记录

| 项目 | 状态 | 记录 |
| --- | --- | --- |
| 仓库级验证 | Pass | 2026-05-16 `pwsh ./scripts/check-repo.ps1` 已通过；最终输出 `Repository checks passed.` |
| Studio 用户视角 Smoke A / B / C | Pass | 2026-05-16 人工从 IDE 启动 Studio 执行通过，覆盖打开示例 / 运行 / 审阅 / 保存重开、空白建模最短闭环、Stream Inspector 草稿与未归一组成阻断 |
| 中文界面资源 | Pass | 2026-05-16 shell 高频路径中文资源已纳入 focused 回归 |
| Studio UI blocker 复验 | Pass | 2026-05-18 人工视觉 smoke 通过；短线段中间流股标签不再遮挡，Result Inspector 默认正文不再显示 `z:` / `phases:` 原始摘要 |
| 仓库级验证复验 | Pass | 2026-05-18 在真实环境以 `CARGO_TARGET_DIR=target/codex pwsh ./scripts/check-repo.ps1` 通过；最终输出 `Repository checks passed.` |
| 便携包 dry run | Pass | 2026-05-18 `pwsh ./scripts/package.ps1 -Version v26.5.1-dev -Clean` 已通过，生成 staging 目录与同名 `.zip` |
| 包内示例发现 | Pass | 2026-05-18 人工直接从 staging 目录启动 `radishflow-studio.exe`，确认 exe 同目录 `examples/flowsheets` 可被发现，打开示例、运行和结果审阅主路径未发现 blocker |
| 包 manifest | Pass | 最终 staging manifest 记录 `version=v26.5.1-dev`、`gitCommit=7479e82`、`gitDirty=false`，入口为 `radishflow-studio.exe` |
| 内部验收 tag | Pass | 2026-05-18 已创建并推送 `v26.5.1-dev`，tag 指向 `7479e82`；该 tag 只代表内部 `-dev` 验收轨道 |

## 包内边界

当前便携包包含：

- `radishflow-studio.exe`
- `examples/flowsheets`
- `examples/sample-components`
- `README.md`、`LICENSE`
- `docs/guides/studio-quick-start.md`
- `docs/guides/run-first-flowsheet.md`
- `docs/guides/review-solve-results.md`
- `docs/reference/units-and-conventions.md`
- `docs/reference/solve-snapshot-results.md`
- `docs/mvp/alpha-acceptance-checklist.md`
- `docs/architecture/versioning.md`
- `docs/releases/v26.5.1-dev.md`
- `PACKAGE-MANIFEST.txt`
- `PACKAGE-README.md`

当前便携包不包含：

- 安装器
- COM 注册或反注册
- Windows Registry 写入
- PME 自动化启动
- 第三方 CAPE-OPEN 模型加载
- 自由连线编辑器、完整拖拽布局编辑器、完整结果报表或新的求解范围

## 版本节点口径

已创建内部验收 tag：

```text
v26.5.1-dev
```

口径：

- 当前 Studio 已有最小闭环，Home / Workbench 第一轮 UI blocker 已收口，且 staging 目录包内启动 smoke 未发现主路径 blocker。
- tag 指向 `7479e82`，与最终便携包 manifest 中的 `gitCommit` 一致。
- 当前 tag 只代表内部验收候选，不应被误解为对外发布候选、正式 demo 或安装包节点。

后续保留事项：

1. 继续把当前包作为内部验证资产归档。
2. 若后续发现 release blocker，应在新提交和新版本节点中处理，不移动已推送的 `v26.5.1-dev` tag。
3. 对外发布、安装器、COM 注册和 PME 自动化仍不属于本节点范围。
