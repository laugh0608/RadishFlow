# 气液分离器尺寸与能力校核

更新时间：2026-09-16

## 用途与状态

用途：规划闪蒸罐 / 气液分离器的初步尺寸推荐和已有设备能力校核。
读者：维护分离设备、物性、工程规格、UI 和验证的协作者。
不包含：完整三相分离、夹带动力学、机械强度认证或已验证的工业选型算法。

- 层级：二级功能；父专题：[设备设计与性能校核](../equipment-design-and-rating.md)。
- 关联：[现有 Flash Drum](../unitops/flash-drum.md)、[单位系统](../units-and-quantity-system.md)、[规格分析](../modeling-assistance-and-specifications.md)。
- 状态：Draft / 规划已确认 / 未实现。当前 TP Flash 无尺寸、液位、停留时间及设备效率能力。

## 目标与首版范围

从可靠工况与显式设计准则出发，给出可解释的初步尺寸候选；用户也可输入已有几何，检查选定准则下的能力和裕量。结果标为“初步工艺尺寸 / 准则校核”，不自动代表制造设计或所有工况下的分离效率保证。

首版先选定一种结构和有限的气液体系 / 工况，候选为立式气液分离器；卧式、多工况包络、三相、泡沫、固体和复杂内件各自扩展。具体结构、准则、数据来源和误差要求由 S0 确认，本文不冻结经验系数。

## 输入、结果与工作流

| 内容 | 要求 |
| --- | --- |
| 工况 | 明确温压、各相流量与组成、相密度；按所用方法补黏度、表面张力、粒径等数据 |
| 设计准则 | 允许气速 / 分离方法、液体停留或缓冲要求、内件、液位区间、结构比例及设计裕量；给出来源与适用范围 |
| 校核输入 | 几何尺寸、有效截面、液位及可用工作容积，区分总容积、正常持液量和缓冲容积 |
| 结果 | 候选直径 / 长度或高度、各部分容积、控制条件、准则余量、不满足项、假设与计算依据 |

用户从独立设备任务或流程 Flash Drum 进入，选择尺寸估算 / 校核，填入或引用当前工况，补齐准则后计算。比较候选，再明确采纳设备参数；保存和导出保留方法与来源。流量使用质量 / 摩尔 / 体积何种基准必须显式，密度不能由单位换算器猜出。

不能只按液体停留时间计算总体积后声称完成气液分离器设计；气相负荷与液体容积等约束需按所选方法共同检查。若某相缺席、密度差不满足方法条件、工况越界或数据不完整，返回明确的不适用 / 缺项结果。经验相关式的系数随物系、内件和单位定义可能变化，必须有来源与量纲约定。

## 数据与接口边界

- 相流量、密度等来自明确输入或有身份的物性 / 流程结果；记录方法、版本和适用范围。
- 几何与准则是正式输入，推荐尺寸是计算结果，采纳走正式命令；独立任务与流程关联共用模型。
- 上游变更使关联计算过期，用户手改尺寸后以新几何重新校核；禁止用旧候选覆盖当前参数。
- 动态复用需要显式映射液位—容积及操作持液量，几何数据不代替动态初始条件。
- 首版不加入完整制造图、规范证书、泄放设计和无依据的“安全”结论。UI 内容在重设计的设备任务区域中评审。

## 切片与验收

| 切片 | 内容 | 退出标准 |
| --- | --- | --- |
| S0 方法与案例 | 选定结构、准则、物性要求及独立案例 | 输入 / 输出 / 范围和公式来源可复核；现有物性缺口有具体补齐计划 |
| S1 独立尺寸估算 | 首个真实气液案例、规格检查和候选结果 | 手算 / 独立基准一致，气相与液体约束均可解释；非法 / 单相 / 越界路径明确 |
| S2 已有设备校核与流程关联 | 给定几何、工况来源、采纳 / 撤销和过期管理 | 独立 / 流程条件相同结果一致；保存重开、单位转换与来源保留 |
| S3 扩展结构与工况 | 卧式、内件、多工况等按需求扩展 | 每一方法有独立适用域和边界验证，不沿用未经证明的默认系数 |

最小验证包含单位等价、流量与密度基准、极限 / 非有限输入、不同控制条件、尺寸变化后的准则响应、无可行方案、旧工况失效及命令历史。工程准确性使用独立依据，软件 golden 只证明实现回归。进入实现后按核心和跨层风险执行正式检查及所需实窗验证。

下一步：E0 / S0 明确首个结构与物性来源；本专题不把当前演示 Flash 输出标为可直接用于工业设备选型。
