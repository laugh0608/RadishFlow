# RadishFlow 协作约定

本文件为 RadishFlow 仓库中的 AI 与人工协作者提供启动级长期约束。

## 定位与优先级

- `docs/` 是仓库正式文档源；本文件只保留任何任务开始时都必须知道、跨任务且跨阶段成立的约束。
- 当前维护状态、优先级、临时门禁和“当前不做”以 `docs/status/current.md` 为准；架构、接口、实现、验证和领域细则以对应专题文档为准。
- 项目所有者在当前任务中的明确要求优先于仓库默认流程；若要求会改变架构、规则、接口、依赖或运行时边界，仍应先说明影响并确认范围。
- 只读取当前任务需要的文档；不要默认展开整篇周志、历史草案、验收记录或发布材料。

## 称呼与语言

- 对话开始或结束总结时，请称呼项目所有者为 `萝卜SAMA`。
- 默认使用中文讨论、说明、编写文档和提交总结。
- 代码、命令、路径、配置键、类型名和接口名保留原文。

## 协作原则

- 开始任务前检查仓库状态，并按任务路由读取最少的相关文档。
- 回答、解释、审查或诊断请求以只读工作为边界；用户明确要求修改时，再实施范围内的代码或文档变更。
- 用户未明确要求直接修改时，编写代码前必须先说明方案并等待批准；需求不清或改动会影响架构、阶段边界、接口口径、依赖或验证基线时，也必须先确认。
- 优先治理根因、长期维护性和系统一致性；不要把最小修复、临时兼容或层层兜底当作默认方案。
- 在完整满足需求和质量要求的前提下控制修改范围；不做无关重构，不为“架构感”增加无真实收益的抽象。
- 每完成一个可分割步骤都做与风险相称的最小验证；重要阶段决策同步更新对应文档，属于本周重要推进时追加到周志。
- 更完整的任务推进、授权、验证和文档归位规则见 `docs/development/agent-collaboration.md`。

## 操作边界

### 可直接执行

- 用户已要求修改时，进行范围内的代码、文档和配置读写。
- `cargo check`、`cargo test`、`cargo fmt --check`、`cargo clippy --workspace --all-targets` 及与改动直接相关的仓库检查。
- `git status`、`git diff`、`git log` 等只读 Git 操作，以及范围清楚、验证已完成的常规提交。

### 必须先告知用户

- 长时间运行或需要人工交互的命令，例如 `cargo run`、桌面 UI 启动和真实窗口 smoke。
- 可能修改本机环境的操作，例如 COM 注册或反注册、证书安装和系统注册表写入。
- 依赖网络或会改变依赖声明、lockfile 的操作，例如 `cargo add`、`cargo update`、`dotnet add package` 和需要联网的 restore。
- 打包、发布、安装，以及因沙盒限制需要在真实环境复验的重要构建、测试或 smoke。

### 默认不执行

- 跨工作区修改其他项目，或整包迁入旧仓库代码。
- 未经明确要求执行破坏性 Git 操作。
- 在 Rust Core 中引入 COM 语义或类型。

## 任务文档路由

| 任务 | 优先读取 |
| --- | --- |
| 当前状态、优先级、临时门禁、下一步 | `docs/status/current.md` |
| 功能范围、阶段目标、非目标 | `docs/topics/README.md`、对应专题、`docs/mvp/scope.md` |
| 仓库分层与 App 边界 | `docs/architecture/overview.md`、`docs/architecture/app-architecture.md` |
| 命名、抽象和实现风格 | `docs/development/code-style.md` |
| Agent 协作、操作授权、验证与文档归位 | `docs/development/agent-collaboration.md` |
| CAPE-OPEN / COM 边界与验证 | `docs/capeopen/boundary.md`、`docs/capeopen/pme-validation.md` |
| 分支、PR 和合并治理 | `docs/adr/0001-branch-and-pr-governance.md` |
| 文档分类、索引和篇幅 | `docs/README.md` |
| 外部参考与许可边界 | `docs/architecture/open-source-references.md`、根 `LICENSE` |

## 项目长期边界

- 产品定位：以 Rust Core + Rust UI + `.NET 10` CAPE-OPEN / COM 适配层构建稳态流程模拟软件。
- 单位统一使用 SI 基本单位；温度使用 K，压力使用 Pa，摩尔流量使用 mol/s。
- Rust 不直接处理 COM；CAPE-OPEN / COM 语义只留在 `.NET` 适配层。
- `rf-model` 只承载对象模型，不提前承载求解策略或 COM 语义。
- 更具体的组成表达、相标签、项目格式、工作区模型、结果模型和 UI 边界，以 MVP、架构和 CAPE-OPEN 专题文档为准。
- `CapeOpenCore` 与 `DWSIM` 只作为语义、架构和测试组织参考；不跨工作区修改，也不直接迁移实现代码。

## 实现与验证红线

- 新增 Rust、C#、脚本或前端代码时遵循对应语言的主流、清晰、可维护实践，以及 `docs/development/code-style.md`。
- 命名必须表达真实领域职责；抽象必须减少真实重复、隔离稳定边界、表达领域概念或稳定跨层契约。
- 优先使用类型系统、结构化错误和清楚的模块边界；不通过字符串标记、全局状态、吞异常或多层 fallback 掩盖模型问题。
- 单个源码文件接近 1500 行时，后续实现应优先按职责拆分同域模块或测试 helper，而不是继续膨胀或机械切片。
- 验证按风险分层；当前正式基线以 `docs/status/current.md` 为准，具体选择和环境性失败处理见 Agent 协作指南。
- 修改架构、接口、项目格式、流程、阶段边界或协作规则时，同步更新对应 `docs/` 真相源。

## Git 与文档约束

- 提交信息使用简洁明确的 Conventional Commits；复杂提交补充 `3-6` 条简短说明。
- 代码、文档和治理改动按主题组织，提交前确认最小验证已完成；不添加 AI 协作者署名。
- 分支角色、PR 门禁和合并策略只在对应 ADR 与仓库配置中维护，不复制到本文件。
- 优先更新已有文档，不为一次性讨论创建散文档；历史过程和验证流水进入周志、记录或归档，不堆入入口文档。

## 入口文件维护

- 一条规则只有在跨任务成立、跨阶段成立、必须在读取专题前生效，并且无法仅靠文档路由可靠承载时，才应加入 `AGENTS.md` / `CLAUDE.md`。
- 阶段状态、临时门禁、“当前不做”、命令级验证细节、目录清单和历史事实应更新其 `docs/` 真相源，不得复制回根入口。
- 两份文件必须全文一致；修改任一文件时同步另一份，并执行仓库治理检查。
- 详细维护判定与内容归位规则见 `docs/development/agent-collaboration.md` 和 `docs/README.md`。
