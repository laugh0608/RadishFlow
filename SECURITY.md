# RadishFlow 安全策略

RadishFlow 已停止公开业务功能维护，当前没有受支持的稳定版本、固定安全响应 SLA 或公开发布周期。尽管如此，涉及恶意输入、项目文件、FFI、CAPE-OPEN / COM 边界、构建供应链和敏感信息泄露的问题仍应优先私下报告。

## 私下报告

远程仓库已启用 [GitHub Private Vulnerability Reporting](https://github.com/laugh0608/RadishFlow/security/advisories/new)，请优先通过该入口私下提交安全漏洞。若该入口不可用，可发送邮件至 `laugh0608@foxmail.com`，主题包含 `[RadishFlow Security]`。

请不要为未修复漏洞创建公开 Issue 或 Pull Request，也不要提交真实密钥、凭据、个人数据、任职单位资料、专有流程数据或未经授权取得的第三方材料。

报告应尽量包含：

- 受影响的提交、模块、平台和运行条件；
- 最小复现步骤、输入文件、预期行为和实际行为；
- 对机密性、完整性、可用性或计算结果可信度的影响；
- 已做脱敏的日志、崩溃信息或概念验证；
- 已知利用条件、临时缓解措施和建议披露时间；
- 是否可以公开致谢及希望使用的署名。

若复现需要敏感材料，请先说明材料类型并等待安全传输安排，不要直接通过公开渠道或普通附件发送。

## 安全问题范围

- 恶意项目文件、组分数据、JSON、字符串或其他输入导致任意代码执行、路径穿越、越权访问、拒绝服务或资源失控；
- Rust Core、C ABI、native library 与 `.NET 10` 适配层之间的内存安全、类型混淆、句柄生命周期或边界校验问题；
- CAPE-OPEN / COM 注册、调用、异常映射或进程边界造成的权限提升、持久化或不受控代码执行；
- 项目文件、日志、崩溃信息、示例、测试产物或遥测泄露凭据、个人数据、专有输入或其他敏感信息；
- 恶意输入或边界缺陷可静默篡改 flowsheet、求解输入、单位、组分、物性状态或结果归属，进而产生具有安全影响的错误结论；
- 依赖、构建、CI、绑定生成、打包、发布产物或第三方资产的供应链完整性问题。

普通数值精度问题、无安全影响的崩溃、功能缺陷、兼容性问题和文档错误不属于安全漏洞。项目已停止公开产品问题处理；此类报告可能不会被受理或修复。

## 处理与披露

维护者会按 best-effort 方式确认报告、评估影响和复现条件，但不承诺响应时间、修复时间、补丁版本或向后移植。是否修复取决于问题严重性、维护状态、合规边界和可安全实施性。

公开时间应由报告者与维护者协调。在修复或缓解措施可用前，请勿披露可直接利用的细节。安全修复不得扩大已停止的业务功能范围，也不得把真实秘密、用户数据或专有流程材料带入长期测试样例。
